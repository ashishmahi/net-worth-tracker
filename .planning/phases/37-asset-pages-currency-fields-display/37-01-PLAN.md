---
phase: 37-asset-pages-currency-fields-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/DualCurrencyAmount.tsx
  - src/components/__tests__/DualCurrencyAmount.test.tsx
  - src/pages/BankSavingsPage.tsx
  - src/pages/MutualFundsPage.tsx
  - src/pages/StocksPage.tsx
  - src/pages/GoldPage.tsx
  - src/pages/BitcoinPage.tsx
  - src/pages/CommoditiesPage.tsx
  - src/pages/PropertyPage.tsx
  - src/pages/LiabilitiesPage.tsx
  - src/types/data.ts
  - src/context/AppDataContext.tsx
  - src/lib/propertyValidation.ts
autonomous: true
requirements:
  - AP-01
  - AP-02
  - DSP-02
must_haves:
  truths:
    - DualCurrencyAmount owns toReportingCurrency + Rate unavailable + primary/secondary stack per 37-UI-SPEC; parents pass amount, record currency, reporting currency, rate snapshot (D-04–D-06)
    - Add forms default currency select to settings.reportingCurrency; legacy missing currency shows reportingCurrency in editor (D-01, D-02)
    - Stored JSON keeps original numeric units; never persist converted reporting-currency numbers into native amount fields (AP-02)
    - Retirement page has no currency dropdown and stays INR-oriented (D-03)
    - Gold holdings table has no Currency column this phase (D-12); Gold form still collects currency for AP-01/AP-02
    - MF/Stocks tables include Currency column and dual-line Value per D-11; section totals reporting-only per D-10
    - Property and standalone liabilities use one currency per record for all monetary fields (D-07, D-08); persisted keys neutralized per D-09 with migration + tests
    - DashboardPage is not refactored to use DualCurrencyAmount in Phase 37 (D-05)
  artifacts:
    - path: "src/components/DualCurrencyAmount.tsx"
      provides: "Shared dual-stack + degraded display for asset pages"
    - path: "src/pages/BankSavingsPage.tsx"
      provides: "Reporting default on Add + aligned list display"
    - path: "src/types/data.ts"
      provides: "Neutral property/liability amount keys + optional currency on records"
    - path: "src/context/AppDataContext.tsx"
      provides: "Load-time migration *Inr → neutral keys"
---

<objective>
Deliver **Phase 37** (**AP-01**, **AP-02**, **DSP-02**): **DualCurrencyAmount** shared component; **currency `<select>`** and **correct persistence** on **Bank, MF, Stocks, Gold, Bitcoin, Commodities, Property, Liabilities**; **Retirement** unchanged (**D-03**); **neutral JSON keys** for property/liabilities (**D-09**); detail views and tables match **`37-UI-SPEC.md`** and **`docs/multi-currency.md`** §3 (with **Gold** table column exception **D-12**).
</objective>

<threat_model>
**Assets:** Local wealth data; user trust that balances are not silently converted in storage.

| Threat | Mitigation |
|--------|------------|
| **T-37-01** | Persisted amounts overwritten with converted values | **AP-02:** Save user-entered native amounts only; `currency` field explicit; code review greps for `toReportingCurrency` on **assign** to stored props |
| **T-37-02** | Wrong FX shows misleading primary line | Use **`toReportingCurrency`** only inside **`DualCurrencyAmount`** for **display**; reuse **`Rate unavailable`** when `!ok` (**FX-03**) |

**Block severity:** **T-37-01** **high** (data integrity).

</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/37-asset-pages-currency-fields-display/37-CONTEXT.md
@.planning/phases/37-asset-pages-currency-fields-display/37-RESEARCH.md
@.planning/phases/37-asset-pages-currency-fields-display/37-UI-SPEC.md
@.planning/phases/37-asset-pages-currency-fields-display/37-PATTERNS.md
@docs/multi-currency.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>37-01-01 — DualCurrencyAmount + Vitest</name>
  <files>src/components/DualCurrencyAmount.tsx, src/components/__tests__/DualCurrencyAmount.test.tsx</files>
  <read_first>
    - src/lib/currencyConversion.ts
    - src/types/currency.ts
    - .planning/phases/37-asset-pages-currency-fields-display/37-UI-SPEC.md
    - src/pages/BankSavingsPage.tsx
  </read_first>
  <action>
    1. Add **`src/components/DualCurrencyAmount.tsx`** exporting a React component with props **`amount: number`**, **`recordCurrency: CurrencyCode`**, **`reportingCurrency: CurrencyCode`**, **`rates: ForexRateSnapshot`** (import types from **`currencyConversion`** / **`currency.ts`**). Call **`toReportingCurrency(amount, recordCurrency, reportingCurrency, rates)`**.
    2. **Layout:** **`flex flex-col items-end gap-px`** (or **`gap-0`**) right-aligned; **primary** `text-sm font-semibold tabular-nums text-foreground`; **secondary** `text-[11px] font-normal tabular-nums text-muted-foreground` when **`recordCurrency !== reportingCurrency`** and conversion **`ok`**.
    3. When **`recordCurrency === reportingCurrency`** OR secondary would duplicate meaning: render **primary line only** (no secondary).
    4. When **`!result.ok`**: show **muted** line(s) per **37-UI-SPEC** — include exact substring **`Rate unavailable`** (match Phase 36/Dashboard copy).
    5. Use existing formatters (**`wealthFormat`** / **`fmtCompactForReporting`** / page patterns) consistently — **do not** pass preformatted strings from parents (**D-06**).
    6. Add **`src/components/__tests__/DualCurrencyAmount.test.tsx`** with cases: same currency → single line; USD vs INR with mocked snapshot → primary + secondary; missing rate → **Rate unavailable** text present.
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx tsc -b --pretty false && npm test -- --run src/components/__tests__/DualCurrencyAmount.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/DualCurrencyAmount.tsx` exits 0
    - `grep -q 'Rate unavailable' src/components/DualCurrencyAmount.tsx` exits 0
    - `grep -q 'toReportingCurrency' src/components/DualCurrencyAmount.tsx` exits 0
    - `npm test -- --run src/components/__tests__/DualCurrencyAmount.test.tsx` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>Shared display primitive ready for all asset pages.</done>
</task>

<task type="auto">
  <name>37-01-02 — Bank Savings defaults + consume DualCurrencyAmount</name>
  <files>src/pages/BankSavingsPage.tsx</files>
  <read_first>
    - src/pages/BankSavingsPage.tsx
    - src/context/AppDataContext.tsx
    - src/components/DualCurrencyAmount.tsx
    - .planning/phases/37-asset-pages-currency-fields-display/37-CONTEXT.md
  </read_first>
  <action>
    1. Reuse **`const reportingCurrency = data.settings.reportingCurrency ?? 'INR'`** (already on the page).
    2. In **`openAdd`**, change **`reset({ label: '', currency: 'INR', balance: '' })`** to **`reset({ label: '', currency: reportingCurrency, balance: '' })`** (**D-01**).
    3. Change **`useForm`** **`defaultValues: { currency: 'INR' }`** to use **`reportingCurrency`** (e.g. **`defaultValues: { currency: reportingCurrency }`**) or an equivalent pattern that does not hardcode **`INR`** for new forms; if RHF dislikes non-stable **`defaultValues`**, document the chosen pattern in a one-line comment referencing **D-01**.
    4. Replace inline dual-line **`toReportingCurrency`** list-cell JSX with **`<DualCurrencyAmount amount={a.balance} recordCurrency={a.currency} reportingCurrency={reportingCurrency} rates={rateSnapshot} />`**, preserving existing row structure and **`aria`** usage.
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx tsc -b --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'DualCurrencyAmount' src/pages/BankSavingsPage.tsx` exits 0
    - `grep -q 'currency: reportingCurrency' src/pages/BankSavingsPage.tsx` exits 0
    - `grep -q "reset({ label: '', currency: 'INR'" src/pages/BankSavingsPage.tsx` exits 1 (old **`openAdd`** pattern removed)
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>Bank aligns with D-01 and shared component (**DSP-02** reference implementation).</done>
</task>

<task type="auto">
  <name>37-01-03 — Mutual Funds currency form + table</name>
  <files>src/pages/MutualFundsPage.tsx</files>
  <read_first>
    - src/pages/MutualFundsPage.tsx
    - src/types/data.ts
    - src/components/DualCurrencyAmount.tsx
    - .planning/phases/37-asset-pages-currency-fields-display/37-UI-SPEC.md
  </read_first>
  <action>
    1. Extend **`mfFormSchema`** with **`currency: CurrencySchema`** (import **`CurrencySchema`** from **`@/types/currency`**). Register **`currency`** with the same **Currency** fieldset pattern as Bank (**legend**, native **`<select>`**, **`aria-invalid`**, error **`role="alert"`**).
    2. **`openAdd`:** **`reset`** includes **`currency: reportingCurrency`** (**D-01**). **`openEdit`:** use **`item.currency ?? reportingCurrency`** for display default (**D-02**).
    3. **`onSubmit`:** persist **`currency`** on each **`MfPlatform`** — **`MfPlatformSchema`** already allows optional **`currency`**; new platforms **must** save explicit code; edits preserve **AP-02** native **`currentValue`**.
    4. **Table:** add **Currency** column (**D-11**). **Value** column: render **`DualCurrencyAmount`** with **`currentValue`**, **`platform.currency ?? reportingCurrency`**, **`reportingCurrency`**, snapshot.
    5. **Section total:** single line in reporting currency only (**D-10**) — convert each platform’s **`currentValue`** from its stored currency to **`reportingCurrency`** for the headline sum (reuse **`toReportingCurrency`** + **`roundCurrency`**); do **not** add dual stack to the total row.
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx tsc -b --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'CurrencySchema' src/pages/MutualFundsPage.tsx` exits 0
    - `grep -q 'DualCurrencyAmount' src/pages/MutualFundsPage.tsx` exits 0
    - `grep -qi 'Currency' src/pages/MutualFundsPage.tsx` exits 0
    - `grep -q 'currency:' src/pages/MutualFundsPage.tsx` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>MF satisfies AP-01/AP-02/DSP-02 for platforms.</done>
</task>

<task type="auto">
  <name>37-01-04 — Stocks currency form + table</name>
  <files>src/pages/StocksPage.tsx</files>
  <read_first>
    - src/pages/StocksPage.tsx
    - src/pages/MutualFundsPage.tsx
    - .planning/phases/37-asset-pages-currency-fields-display/37-CONTEXT.md
  </read_first>
  <action>
    Mirror **37-01-03** patterns for **`StockPlatform`**: Zod **`currency`**, add/edit defaults (**D-01**/**D-02**), persist **`currency`**, **Currency** + dual-line **Value** columns (**D-11**), reporting-only section total (**D-10**).
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx tsc -b --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'DualCurrencyAmount' src/pages/StocksPage.tsx` exits 0
    - `grep -q 'CurrencySchema' src/pages/StocksPage.tsx` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>Stocks parity with MF (**D-11**).</done>
</task>

<task type="auto">
  <name>37-01-05 — Gold form + table (no Currency column)</name>
  <files>src/pages/GoldPage.tsx</files>
  <read_first>
    - src/pages/GoldPage.tsx
    - src/types/data.ts
    - .planning/phases/37-asset-pages-currency-fields-display/37-CONTEXT.md
  </read_first>
  <action>
    1. Add **currency** to relevant **gold holding** form/schema per **`data.ts`** (follow existing **`Gold`** types — read file for exact shape). **Sheet** must include **Currency** fieldset (**AP-01**).
    2. Persist **native** fiat amounts with **`currency`** (**AP-02**); do **not** store INR-converted figures in place of user USD/AED inputs.
    3. **Holdings table:** **do not** add a **Currency** column (**D-12**). Where **Value** / fiat display exists, use **`DualCurrencyAmount`** or equivalent dual-line for **DSP-02**; **grams/karat** semantics unchanged.
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx tsc -b --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi 'Currency' src/pages/GoldPage.tsx` exits 0 (form includes currency control **AP-01**)
    - `grep -q 'DualCurrencyAmount' src/pages/GoldPage.tsx` exits 0 OR `grep -q 'toReportingCurrency' src/pages/GoldPage.tsx` exits 0
    - `grep '<th' src/pages/GoldPage.tsx | grep -qi Currency` exits 1 (no `<th>…Currency…</th>` column — **D-12**)
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>Gold meets AP-01/AP-02 and D-12.</done>
</task>

<task type="auto">
  <name>37-01-06 — Bitcoin + Commodities currency UX</name>
  <files>src/pages/BitcoinPage.tsx, src/pages/CommoditiesPage.tsx</files>
  <read_first>
    - src/pages/BitcoinPage.tsx
    - src/pages/CommoditiesPage.tsx
    - src/types/data.ts
    - src/components/DualCurrencyAmount.tsx
  </read_first>
  <action>
    For each page: ensure **add/edit** includes **Currency** control (**AP-01**), defaults **D-01**/**D-02**, persistence **AP-02** ( **`BitcoinSchema`** already has optional **`currency`** — wire UI ). **Detail/table/list** amounts that are fiat-denominated use **`DualCurrencyAmount`** (**DSP-02**). Preserve commodity-specific units (oz, g) per existing UX.
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx tsc -b --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'CurrencySchema\|currency' src/pages/BitcoinPage.tsx` exits 0
    - `grep -q 'CurrencySchema\|currency' src/pages/CommoditiesPage.tsx` exits 0
    - `grep -q 'DualCurrencyAmount' src/pages/BitcoinPage.tsx` exits 0 || `grep -q 'DualCurrencyAmount' src/pages/CommoditiesPage.tsx` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>Bitcoin and Commodities aligned with spec.</done>
</task>

<task type="auto">
  <name>37-01-07 — Property + Liabilities neutral keys + UI</name>
  <files>src/types/data.ts, src/context/AppDataContext.tsx, src/pages/PropertyPage.tsx, src/pages/LiabilitiesPage.tsx, src/lib/propertyValidation.ts</files>
  <read_first>
    - src/types/data.ts
    - src/context/AppDataContext.tsx
    - src/lib/propertyValidation.ts
    - src/pages/PropertyPage.tsx
    - src/pages/LiabilitiesPage.tsx
    - .planning/phases/37-asset-pages-currency-fields-display/37-CONTEXT.md
  </read_first>
  <action>
    1. **Rename persisted fields** (example target names — adjust to one consistent scheme): **`agreementInr` → `agreementAmount`**, **`outstandingLoanInr` → `outstandingLoan`**, **`emiInr` → `emi`**, **`amountInr` (milestones) → `amount`**, **`outstandingInr` → `outstanding`**, liability **`emiInr` → `emi`** — **all** numeric semantics remain **native units in `currency`** (**D-07**, **D-08**, **D-09**). Update **`PropertyMilestoneRowSchema`**, **`PropertyItemSchema`**, **`LiabilityItemSchema`**, **`getPropertyValidationIssues`**, and **Zod** paths (`superRefine` **path** arrays).
    2. **`AppDataContext`** (or central migration): on load, if legacy **`*Inr`** keys present, map to neutral keys and strip legacy keys; bump **`schemaVersion`** if project uses it.
    3. **Property/Liability sheets:** one **Currency** select governing all amounts on record (**37-UI-SPEC**). Labels reference **neutral** field names in user copy.
    4. **Liabilities list rows:** **`DualCurrencyAmount`** for principal lines (**DSP-02**).
    5. Add **Vitest** coverage for migration or validation (**minimum:** one test file proving milestone sum validation still fires after rename).
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx tsc -b --pretty false && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'agreementAmount\|agreementInr' src/types/data.ts` exits 0 (neutral **or** transitional — **grep proves** new schema field names exist)
    - `grep -q 'AppDataContext' src/context/AppDataContext.tsx` exits 0
    - `npm test -- --run` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>Property/liability data model and UI match D-07–D-09.</done>
</task>

<task type="auto">
  <name>37-01-08 — Retirement guard + full suite</name>
  <files>src/pages/RetirementPage.tsx</files>
  <read_first>
    - src/pages/RetirementPage.tsx
    - .planning/phases/37-asset-pages-currency-fields-display/37-CONTEXT.md
  </read_first>
  <action>
    Verify **no** **Currency** fieldset or **`CurrencySchema`** is added to **`RetirementPage`** this phase (**D-03**). If any shared form accidentally imports currency controls, remove. Run **`npm test -- --run`** and fix regressions from migration tasks.
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run && npx tsc -b --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'CurrencySchema' src/pages/RetirementPage.tsx` exits 0 OR `grep -q 'CurrencySchema' src/pages/RetirementPage.tsx` exits 1
    - `npm test -- --run` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>Retirement exception enforced; workspace green.</done>
</task>

</tasks>

<verification>
<criteria>
- `npm test -- --run` exits 0
- `npx tsc -b --pretty false` exits 0
- Manual: add MF in USD — DevTools localStorage / exported JSON shows **`currency":"USD"`** and **`currentValue`** unchanged in USD numeric units (**AP-02**)
- Manual: Property single currency drives agreement + milestones + loan fields; dual display where **DSP-02** applies
</criteria>
</verification>
