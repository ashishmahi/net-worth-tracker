---
phase: 03-live-prices-bitcoin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/priceApi.ts
  - src/context/LivePricesContext.tsx
  - src/main.tsx
  - CLAUDE.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-10

must_haves:
  truths:
    - "All remote price HTTP calls go through src/lib/priceApi.ts (grep: no other file under src/lib/ or src/context/ may call fetch except priceApi and existing AppDataContext / this file as documented)"
    - "useLivePrices() exposes live values, loading, error, refetch, and session override setters/clear per 03-CONTEXT"
    - "TTL: BTC cache 5 min; forex cache ~1 hour (documented constants in priceApi or context)"
    - "npx tsc --noEmit exits 0"
  artifacts:
    - path: "src/lib/priceApi.ts"
      provides: "fetch helpers for BTC/USD, USD/INR, AED/INR with documented API choice and fallbacks"
      contains: "export", "fetch("
    - path: "src/context/LivePricesContext.tsx"
      provides: "LivePricesProvider + useLivePrices with session-only override state (not persisted)"
      contains: "createContext", "useLivePrices"
    - path: "src/main.tsx"
      provides: "Provider wraps App inside AppDataProvider"
      contains: "LivePricesProvider"
  key_links:
    - from: "src/context/LivePricesContext.tsx"
      to: "src/lib/priceApi.ts"
      via: "import"
      pattern: "fetchBtcUsd|fetchForex|getLivePrices"
---

<objective>
Implement the **central price API module** (`priceApi.ts`), a **LivePricesProvider** + **`useLivePrices()`** hook that applies D-02 TTLs, D-04 recovery (clear session overrides when live succeeds), and D-10 refetch behavior (document in code: e.g. mount + interval and/or `document.visibilitychange` / focus — pick one clear strategy). Wire the provider in `main.tsx`. Update `CLAUDE.md` live-price TTL line to match Phase 3 decisions (forex ~1 hr).
</objective>

<threat_model>
**Scope:** Local-only SPA; no auth. **Assets at risk:** User’s financial view; no server-side data.

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-03-01 | Open redirect / SSRF via fetch | Low (no user URL input) | Hard-code API base URLs in `priceApi.ts` constants only; no parameters that accept arbitrary URLs from UI |
| T-03-02 | Dependency confusion | Low | Use only documented public APIs; no install in this plan |

**Block on:** none for L1 local app — document fixed endpoints in source.
</threat_model>

<context>
@/Users/a.mahindrakar/personal/fin/.planning/phases/03-live-prices-bitcoin/03-CONTEXT.md
@/Users/a.mahindrakar/personal/fin/.planning/phases/03-live-prices-bitcoin/03-RESEARCH.md
@/Users/a.mahindrakar/personal/fin/.planning/phases/03-live-prices-bitcoin/03-UI-SPEC.md
@/Users/a.mahindrakar/personal/fin/CLAUDE.md
@/Users/a.mahindrakar/personal/fin/src/main.tsx
@/Users/a.mahindrakar/personal/fin/src/context/AppDataContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement priceApi.ts</name>
  <files>src/lib/priceApi.ts</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/.planning/phases/03-live-prices-bitcoin/03-RESEARCH.md — §1 APIs
    - /Users/a.mahindrakar/personal/fin/CLAUDE.md — live prices convention
  </read_first>
  <action>
Create `src/lib/priceApi.ts`. At the top, add a comment block listing **primary** endpoints (e.g. CoinGecko BTC/USD; open.er-api.com or equivalent for USD→INR and USD→AED with **aedInr = rates.INR / rates.AED**). Export async functions that return **numeric rates** or throw on failure. No React imports. Use `fetch` only in this file for market data.
  </action>
  <acceptance_criteria>
    - File `src/lib/priceApi.ts` exists and contains the substring `CoinGecko` OR `coingecko` in a comment documenting the BTC source
    - Same file contains a comment explaining how **AED/INR** is derived from USD-based rates
    - `cd /Users/a.mahindrakar/personal/fin && npx tsc --noEmit` exits with code 0 after this file is added (if tsc fails for missing exports from other planned files, complete Task 2 in the same branch so tsc passes before marking plan done)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: LivePricesContext + useLivePrices + main.tsx</name>
  <files>src/context/LivePricesContext.tsx, src/main.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/main.tsx
    - /Users/a.mahindrakar/personal/fin/src/context/AppDataContext.tsx — provider pattern
    - /Users/a.mahindrakar/personal/fin/.planning/phases/03-live-prices-bitcoin/03-CONTEXT.md — D-04, D-06, D-10
  </read_first>
  <action>
1. Create `src/context/LivePricesContext.tsx` exporting `LivePricesProvider` and `useLivePrices`. The provider should:
   - Call `priceApi` functions on a schedule / event pattern per D-10 (must be described in a 3–6 line comment at top of provider).
   - Keep **in-memory** session manual overrides for `btcUsd`, `usdInr`, `aedInr` (numbers or null when cleared). When a live fetch **succeeds** for a given channel, **clear** that channel’s session override (D-04).
   - Expose: `btcUsd`, `usdInr`, `aedInr` as **effective** numbers (session OR live), booleans or union for loading/error, `refetch`, and setters like `setSessionRates(partial)` / `clearSessionRates` as needed for Settings in Plan 02.
2. Wrap `<App />` with `<LivePricesProvider>` **inside** `<AppDataProvider>` in `src/main.tsx`.
3. Ensure `useLivePrices` throws if used outside provider (same pattern as `useAppData`).
  </action>
  <acceptance_criteria>
    - `src/main.tsx` contains the substring `LivePricesProvider` nested inside `AppDataProvider`
    - `src/context/LivePricesContext.tsx` contains `useLivePrices` and `createContext`
    - `rg "fetch\\(" src/pages` from project root returns **no** matches (page components must not call fetch) — *if* `rg` is not installed, use `grep -r "fetch(" src/pages` and expect no output
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Align CLAUDE.md TTL wording with Phase 3</name>
  <files>CLAUDE.md</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/CLAUDE.md
  </read_first>
  <action>
Update the “Live price caching” bullet so **forex** TTL reflects **~1 hour** (D-02) and **BTC** remains **5 min**, and reference that all fetches go through the central `priceApi` + `useLivePrices` path.
  </action>
  <acceptance_criteria>
    - `CLAUDE.md` contains the word `priceApi` or `useLivePrices` in that bullet
    - `CLAUDE.md` no longer states forex TTL is 24 hr as the only value (wording may mention Phase 3 hourly forex explicitly)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Run: `cd /Users/a.mahindrakar/personal/fin && npx tsc --noEmit` → exit 0
- Run: `grep -R "fetch(" src/pages` → expect no output
- Manual: `npm run dev` loads without runtime error in console (provider wraps app)
</verification>

## PLANNING COMPLETE
