---
phase: 28-section-routing-home-header
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/main.tsx
  - src/lib/sectionRoutes.ts
  - src/lib/__tests__/sectionRoutes.test.ts
  - src/App.tsx
  - src/components/AppSidebar.tsx
  - src/components/MobileTopBar.tsx
  - src/pages/DashboardPage.tsx
  - .planning/phases/28-section-routing-home-header/28-UAT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "BrowserRouter uses basename import.meta.env.BASE_URL"
    - "Each SectionKey maps to exactly one path; refresh on /gold stays on GoldPage"
    - "MobileTopBar shows a Home affordance to dashboard when not on index route"
    - "Unknown paths redirect to dashboard with Navigate replace"
  artifacts:
    - path: "src/lib/sectionRoutes.ts"
      provides: "sectionToPath, pathToSection, listSectionPaths"
    - path: ".planning/phases/28-section-routing-home-header/28-UAT.md"
      provides: "manual refresh + mobile Home checklist"
  key_links:
    - from: "src/components/MobileTopBar.tsx"
      to: "28-UI-SPEC.md"
      via: "Menu · Home · Theme"
      pattern: "mobile header"
---

<objective>
Execute Phase 28: add **`react-router-dom`**, canonical **`sectionRoutes`** helpers + tests, refactor **`App`** to layout **`Routes`**/**`Outlet`** with **`basename`**, convert **`AppSidebar`** to URL-driven navigation, add **Home** on **`MobileTopBar`**, wire **`DashboardPage`** **`onNavigate`** to **`navigate`**, document **28-UAT.md**, run **`npm test`** + **`npm run build`**.
</objective>

<threat_model>
**Assets:** Same-origin SPA only; no auth.

| Threat | Mitigation |
|--------|------------|
| **T-open-redirect** | Navigation uses **whitelist** **`sectionToPath(SectionKey)`** only — **no** `navigate(userInput)`. Unknown HTTP paths → **`Navigate to="/"`**. |
| **T-XSS-via-URL** | **`pathToSection`** maps known segments only — arbitrary path segments do not render custom HTML. |

**Block severity:** High — covered by whitelist + catch-all route.

</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/28-section-routing-home-header/28-UI-SPEC.md
@.planning/phases/28-section-routing-home-header/28-RESEARCH.md
@.planning/phases/28-section-routing-home-header/28-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: react-router-dom + sectionRoutes + Vitest</name>
  <files>package.json, package-lock.json, src/lib/sectionRoutes.ts, src/lib/__tests__/sectionRoutes.test.ts</files>
  <read_first>
    - package.json
    - src/components/AppSidebar.tsx
    - vite.config.ts
  </read_first>
  <action>
    1. Install **`react-router-dom`** (**`^6.28.0`** or latest compatible with React 18): run **`npm install react-router-dom`** — commits **`package.json`** + **`package-lock.json`**.

    2. Add **`src/lib/sectionRoutes.ts`** exporting:
       - **`SECTION_PATHS`**: readonly mapping **`SectionKey` → path string** per **`28-UI-SPEC.md`** (`dashboard` → **`"/"`** represented as **`""`** internal suffix OR document that dashboard uses empty segment — prefer **`sectionToPath(key)`** returns **`"."`** / **`""`** for index — **concrete:** **`sectionToPath('dashboard') === '/'`** as string for **`navigate`**, **`pathToSection`** accepts **`pathname`** already stripped of **`basename`** by router — use **`location.pathname`** normalize trailing slash).
       - **`pathToSection(pathname: string): SectionKey | null`** — normalize leading/trailing slashes; **`'/'`** or **`''`** → **`dashboard`**; **`'/mutual-funds'`** → **`mutualFunds`**; unknown → **`null`**.
       - **`sectionToPath(key: SectionKey): string`** — returns paths like **`/gold`**, **`/`** for dashboard.

    3. **`src/lib/__tests__/sectionRoutes.test.ts`** — minimum cases:
       - Round-trip each **`SectionKey`** through **`sectionToPath`** + **`pathToSection`** (parse **`sectionToPath`** output: strip leading **`/`** for parser input if API expects relative segment — document in module).
       - **`mutual-funds`** ↔ **`mutualFunds`**.
       - **`null`** for **`/unknown`**.

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run src/lib/__tests__/sectionRoutes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "react-router-dom" package.json` exits 0
    - `test -f src/lib/sectionRoutes.ts` exits 0
    - `npm test -- --run src/lib/__tests__/sectionRoutes.test.ts` exits 0
  </acceptance_criteria>
  <done>Router dependency + pure route helpers tested</done>
</task>

<task type="auto">
  <name>Task 2: BrowserRouter basename + App layout Routes + sidebar/mobile/dashboard wiring</name>
  <files>src/main.tsx, src/App.tsx, src/components/AppSidebar.tsx, src/components/MobileTopBar.tsx, src/pages/DashboardPage.tsx</files>
  <read_first>
    - src/main.tsx
    - src/App.tsx
    - src/components/AppSidebar.tsx
    - src/components/MobileTopBar.tsx
    - src/pages/DashboardPage.tsx
    - src/lib/sectionRoutes.ts
    - .planning/phases/28-section-routing-home-header/28-UI-SPEC.md
  </read_first>
  <action>
    1. **`src/main.tsx`**: Wrap **`App`** with **`BrowserRouter`** **`basename={import.meta.env.BASE_URL}`** (must appear inside **`StrictMode`** tree such that **`App`** can use hooks — place **`BrowserRouter`** outside **`App`** but inside **`LivePricesProvider`** **or** wrap **`App`** only — **concrete:** **`BrowserRouter`** wraps **`App`** inside **`LivePricesProvider`** per existing provider order).

    2. **`src/App.tsx`**:
       - Remove **`useState`** for **`activeSection`**.
       - Keep **`SidebarProvider`**, sync components, **`loadError`** handling.
       - Introduce nested **`Routes`**/**`Route`**/**`Outlet`**:
         - Parent **`Route`** **`element={&lt;AppLayout /&gt;}`** where **`AppLayout`** renders **`GoldSpotPricesSync`**, **`SilverSpotPricesSync`**, **`AppSidebar`**, **`SidebarInset`**, **`MobileTopBar`**, **`main`** with **`&lt;Outlet /&gt;`** (and load error banner above outlet).
         - Child **`Route`** **`index`** → **`DashboardPage`** with **`onNavigate={(key) => navigate(sectionToPath(key))}`** using **`useNavigate()`** from **`react-router-dom`**.
         - Child routes **`path="gold"`**, **`commodities`**, … matching **`sectionToPath`** segments (**omit leading slash** in **`path`** prop — **concrete:** **`path="gold"`** not **`/gold`**).
       - Add **`Route path="*"`** sibling **`element={&lt;Navigate to="/" replace /&gt;}`** under **`Routes`** (catch-all).

    3. **`src/components/AppSidebar.tsx`**:
       - Import **`NavLink`** (or **`Link`**) from **`react-router-dom`**.
       - Replace **`onClick`**-only navigation with **`to={sectionToPath(item.key)}`**; preserve **`onClick`** **`setOpenMobile(false)`** on mobile after navigation.
       - **`NavLink`** **`end`** prop **`true`** when **`item.key === 'dashboard'`**.
       - **`isActive`** styling: keep **`SidebarMenuButton`** **`isActive`** using **`NavLink`** **`className`** callback **`({ isActive })`** or derive from **`useLocation`** — **concrete:** use **`NavLink`** wrapping **`SidebarMenuButton`** **or** pass **`isActive`** from **`useMatch`** — match existing visual (**`isActive`** prop on **`SidebarMenuButton`**).

    4. **`src/components/MobileTopBar.tsx`**:
       - **`useLocation()`** — if **`pathToSection(location.pathname)`** is **`dashboard`** (**or** pathname is index), **do not** render Home button; else render **`Link`** **`to="/"`** (**or** **`to={sectionToPath('dashboard')}`**) with **`House`** icon **`aria-label="Go to dashboard"`**, **`Button`** **`variant="ghost"`**, **`min-h-[44px] min-w-[44px]`**, placed **between** menu and theme per **28-UI-SPEC**.

    5. **`src/pages/DashboardPage.tsx`**: No signature change — **`onNavigate`** still **`SectionKey`**; ensure **`App`** passes **`navigate`** wrapper so internal **`onClick`** navigates by URL.

    6. Run **`npm run build`** — TypeScript must pass.

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'basename={import.meta.env.BASE_URL}' src/main.tsx` OR `grep -q 'basename={import.meta.env.BASE_URL}' src/App.tsx` exits 0 (exact file where Router lives)
    - `grep -q "Outlet" src/App.tsx` exits 0
    - `grep -q "Navigate" src/App.tsx` exits 0
    - `grep -q "House" src/components/MobileTopBar.tsx` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>SPA routes wired; mobile Home; production build green</done>
</task>

<task type="auto">
  <name>Task 3: 28-UAT.md manual script</name>
  <files>.planning/phases/28-section-routing-home-header/28-UAT.md</files>
  <read_first>
    - .planning/phases/28-section-routing-home-header/28-UI-SPEC.md
    - .planning/phases/27-settings-commodity-pricing-ux/27-UAT.md
  </read_first>
  <action>
    Create **`.planning/phases/28-section-routing-home-header/28-UAT.md`** with numbered checks:
    - Deep-link or navigate to **`/gold`** (with dev **`base`**), refresh browser, confirm Gold page.
    - Repeat for **`/settings`** (or at least two non-dashboard routes).
    - Mobile viewport: from Settings, Home icon returns to dashboard URL **`/`**.
    - Sidebar active state matches URL after manual **`Link`** navigation.

  </action>
  <verify>
    <automated>test -f .planning/phases/28-section-routing-home-header/28-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "refresh" .planning/phases/28-section-routing-home-header/28-UAT.md` exits 0
    - `grep -q "Home" .planning/phases/28-section-routing-home-header/28-UAT.md` exits 0
  </acceptance_criteria>
  <done>UAT checklist recorded</done>
</task>

</tasks>

<verification_criteria>
- Full **`npm test -- --run`** passes
- **`npm run build`** passes
- **`28-UAT.md`** lists refresh + mobile Home scenarios
</verification_criteria>

---

## PLANNING COMPLETE
