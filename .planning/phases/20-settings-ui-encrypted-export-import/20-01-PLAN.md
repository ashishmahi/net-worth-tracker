---
phase: 20-settings-ui-encrypted-export-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/SettingsPage.tsx
autonomous: true
requirements:
  - ENC-01
  - ENC-04
  - ENC-05
  - ENC-06

must_haves:
  truths:
    - "Blank export passphrase still downloads plain JSON via existing handleExport behavior"
    - "Non-blank export passphrase downloads JSON envelope with encrypted: true (AES path)"
    - "Plain JSON import unchanged — no decrypt UI; encrypted JSON shows inline passphrase + Decrypt before confirmation dialog"
    - "Correct decrypt passphrase leads to existing Replace-with-imported-data dialog and successful load path"
    - "Wrong-passphrase decrypt shows exact inline error copy from CONTEXT D-10; live AppData unchanged until user confirms import"
    - "npm test passes; npm run build passes"
  artifacts:
    - path: "src/pages/SettingsPage.tsx"
      provides: "Export passphrase UI, encrypted export, encrypted import branch, decrypt UI, inline errors, busy states"
      min_lines: 650
  key_links:
    - from: "src/pages/SettingsPage.tsx"
      to: "src/lib/cryptoUtils.ts"
      via: "import { encryptData, decryptData, isCryptoError, type EncryptedEnvelope }"
      pattern: "from '@/lib/cryptoUtils'"
---

<objective>
Wire `encryptData` / `decryptData` / `isCryptoError` into `SettingsPage.tsx` per `20-CONTEXT.md` and `20-UI-SPEC.md`: optional passphrase export, auto-detect encrypted import with inline decrypt flow, loading spinners, and inline destructive errors — satisfying ENC-01, ENC-04, ENC-05, ENC-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-settings-ui-encrypted-export-import/20-CONTEXT.md
@.planning/phases/20-settings-ui-encrypted-export-import/20-RESEARCH.md
@.planning/phases/20-settings-ui-encrypted-export-import/20-UI-SPEC.md
@.planning/phases/19-crypto-utilities/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export — passphrase field, show/hide, encrypted vs plain download</name>
  <files>src/pages/SettingsPage.tsx</files>
  <read_first>
    - src/pages/SettingsPage.tsx (full file — Data block ~556–655, handleExport ~31–42, state ~112–120)
    - src/lib/cryptoUtils.ts (encryptData signature, EncryptedEnvelope)
    - .planning/phases/20-settings-ui-encrypted-export-import/20-CONTEXT.md (D-01–D-04, D-07–D-09)
    - .planning/phases/20-settings-ui-encrypted-export-import/20-UI-SPEC.md (passphrase Input pr-10, Eye toggle, Export stays outline)
  </read_first>
  <action>
    1. Add imports: `Eye`, `EyeOff` from `lucide-react`; `encryptData` and type `EncryptedEnvelope` from `@/lib/cryptoUtils`.

    2. Add state (names may vary but behaviors required):
       - `exportPassphrase: string` (controlled input)
       - `exportBusy: boolean`
       - `exportEncryptError: string | null` (cleared when user edits passphrase or starts new export — mirror import error clearing pattern)
       - `showExportPassphrase: boolean` (for Eye toggle — default false so input is `type="password"`)

    3. In the Data section, **above** the row that contains "Export Data", add a labeled block:
       - `Label` with text exactly **Passphrase (optional)** (`htmlFor` tied to new input id e.g. `export-passphrase`)
       - Relative wrapper `div` containing `Input`: default `type={showExportPassphrase ? 'text' : 'password'}`, `className` includes **`pr-10`** when toggle present (per UI-SPEC)
       - Icon button (ghost/smallest padding) absolutely positioned `right-2 top-1/2 -translate-y-1/2` toggling `showExportPassphrase`, `aria-label` **Show passphrase** / **Hide passphrase**, icons `Eye` / `EyeOff` size `h-4 w-4`
       - Optional: helper `p` with `text-sm text-muted-foreground` e.g. placeholder **Leave blank to export unencrypted JSON** (Claude's discretion — if added, must remain helper not error)

    4. Replace the Export button `onClick`: 
       - If `exportPassphrase.trim() === ''`: call existing **`handleExport(data)`** unchanged (plain JSON).
       - Else: `setExportBusy(true)`, clear `exportEncryptError`, then `try { const envelope = await encryptData(JSON.stringify(data, null, 2), exportPassphrase); const json = JSON.stringify(envelope, null, 2); ... }` using the **same Blob / createObjectURL / download filename pattern** as `handleExport`, but stringifying **`envelope`** instead of `data`. Filename pattern must remain `wealth-tracker-YYYY-MM-DD.json` (same date slice as `handleExport`).
       - On rejection: set `exportEncryptError` to a short user-facing message (derive from `e instanceof Error ? e.message : 'Encryption failed.'` or static text)
       - `finally { setExportBusy(false) }`

    5. Export button: **`disabled={importBusy || exportBusy}`** (import still blocks export during file read/decrypt). While `exportBusy`, show **`Loader2`** `h-4 w-4 mr-2 animate-spin` inside button (match Import spinner pattern ~584–586).

    6. Below the Export button (or button row), when `exportEncryptError` set: **`<p role="alert" className="text-sm text-destructive">{exportEncryptError}</p>`**

    7. Run **`npm test`** and **`npm run build`**; commit: `feat(20-01): settings encrypted export UI`
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Passphrase (optional)" src/pages/SettingsPage.tsx` returns at least 1 line
    - `grep -n "encryptData" src/pages/SettingsPage.tsx` returns at least 1 line
    - `grep -n "exportBusy" src/pages/SettingsPage.tsx` returns at least 2 lines
    - `grep -n 'role="alert"' src/pages/SettingsPage.tsx` matches export error block when exportEncryptError used (file contains `text-destructive` near export path)
    - `grep -n "handleExport(data)" src/pages/SettingsPage.tsx` still appears for blank-passphrase branch
    - `npm test` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Export supports plain and encrypted download with busy/disable and inline encrypt failure alert per CONTEXT</done>
</task>

<task type="auto">
  <name>Task 2: Import — encrypted detection, decrypt UI, passphrase errors, success path to dialog</name>
  <files>src/pages/SettingsPage.tsx</files>
  <read_first>
    - src/pages/SettingsPage.tsx (onImportFileChange ~220–254, AlertDialog ~606–654, pendingImport flow)
    - src/context/AppDataContext.tsx (parseAppDataFromImport export — signature)
    - src/lib/cryptoUtils.ts (decryptData, isCryptoError, EncryptedEnvelope)
    - .planning/phases/20-settings-ui-encrypted-export-import/20-CONTEXT.md (D-05, D-06, D-08–D-11)
  </read_first>
  <action>
    1. Add imports: `decryptData`, `isCryptoError`, type `EncryptedEnvelope` from `@/lib/cryptoUtils` (merge with Task 1 imports if duplicate).

    2. Add state:
       - `pendingEncryptedEnvelope: EncryptedEnvelope | null` — set when file JSON parses and encrypted branch taken; cleared on new file pick, successful decrypt path setup, or cancel per D-06
       - `importDecryptPassphrase: string`
       - `importDecryptError: string | null`
       - `showImportDecryptPassphrase: boolean` (Eye toggle for decrypt field)
       - Reuse **`importBusy`** for async decrypt (D-09); ensure decrypt button and Import button respect busy rules below

    3. **Helpers:** Implement `function isEncryptedEnvelope(raw: unknown): raw is EncryptedEnvelope` in file (or inline guarded checks) requiring at minimum: `raw !== null`, `typeof raw === 'object'`, `(raw as EncryptedEnvelope).encrypted === true`, `(raw as EncryptedEnvelope).version === 1`, non-empty strings for `salt`, `iv`, `data` (match Phase 19 envelope). If parse succeeds but shape invalid, set **`importValidationError`** with a clear message (same inline pattern as existing validation errors) and do **not** enter decrypt UI.

    4. **Rewrite `onImportFileChange` logic after successful `JSON.parse`:**
       - Clear at start (in addition to existing clears): `pendingEncryptedEnvelope`, `importDecryptPassphrase`, `importDecryptError`, `showImportDecryptPassphrase` (reset to false)
       - If **`isEncryptedEnvelope(raw)`**: set `pendingEncryptedEnvelope` to `raw`, **do not** call `parseAppDataFromImport` yet, **do not** open `importDialogOpen`, `return` from try after clearing busy
       - Else: existing flow — `parseAppDataFromImport(raw)` → errors OR `setPendingImport` + `setImportDialogOpen(true)`

    5. **New handler `onDecryptImport` (async):** If `!pendingEncryptedEnvelope` return. Clear `importDecryptError`. `setImportBusy(true)`. Try:
       - If `importDecryptPassphrase.trim() === ''`: set `importDecryptError` to **Wrong passphrase — the file could not be decrypted.** (or equivalent short empty-passphrase message per ENC-06 — must be inline error, no dialog); `setImportBusy(false)`; return
       - `const plaintext = await decryptData(pendingEncryptedEnvelope, importDecryptPassphrase.trim())`
       - `const parsed = JSON.parse(plaintext) as unknown`
       - `const result = parseAppDataFromImport(parsed)` — if fail, set validation errors as today; if success: `setPendingImport(result.data)`, `setImportDialogOpen(true)`, clear `pendingEncryptedEnvelope` and `importDecryptPassphrase` and `importDecryptError`
       - `catch (e)`: if **`isCryptoError(e)`**: `importDecryptError = 'Wrong passphrase — the file could not be decrypted.'` **exact string** (CONTEXT D-10). Else: `importDecryptError = 'Decryption failed. Check that the app is running and try again.'` **exact string**
       - `finally`: `setImportBusy(false)`

    6. **Render (Data section, below Import button, only when `pendingEncryptedEnvelope !== null`):**
       - `Label` **Passphrase** (`htmlFor` e.g. `import-decrypt-passphrase`)
       - Same password + Eye toggle pattern as export (`pr-10`, absolute eye button, aria-labels)
       - **Decrypt** button: `variant="default"` (primary — UI-SPEC), `disabled={importBusy}`, show Loader2 + "Decrypt…" or similar when busy
       - `importDecryptError` shown as `<p role="alert" className="text-sm text-destructive">` below controls

    7. **Import from JSON** button: disable when `importBusy` **or** `exportBusy` (consistent with export task).

    8. **AlertDialog `onOpenChange`:** When closing dialog, also clear decrypt-related state if specified in CONTEXT (pending import cleared already — extend to clear `pendingEncryptedEnvelope` / decrypt passphrase when canceling import flow from dialog close if still relevant).

    9. When user picks a **new** file, decrypt UI must reset (handled by clears in step 3).

    10. Run **`npm test`** and **`npm run build`**; commit: `feat(20-01): settings encrypted import decrypt flow`
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pendingEncryptedEnvelope" src/pages/SettingsPage.tsx` returns at least 3 lines
    - `grep -n "decryptData" src/pages/SettingsPage.tsx` returns at least 1 line
    - `grep -n "Wrong passphrase — the file could not be decrypted." src/pages/SettingsPage.tsx` returns at least 1 line
    - `grep -n "Decryption failed. Check that the app is running and try again." src/pages/SettingsPage.tsx` returns at least 1 line
    - `grep -n "isCryptoError" src/pages/SettingsPage.tsx` returns at least 1 line
    - `grep -n 'Decrypt' src/pages/SettingsPage.tsx` returns at least 1 line (Decrypt button label)
    - `npm test` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Encrypted import shows inline decrypt flow; success reaches existing confirmation dialog; wrong/empty passphrase shows inline errors without mutating stored app data until confirmed save</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- **Client-only:** Passphrases and plaintext exist only in browser memory during export/import; no new network endpoints.
- **Files:** User-supplied JSON is parsed in-process; encrypted envelope ciphertext is decrypted only after explicit Decrypt action.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information disclosure | Passphrase in UI | mitigate | No `console.log` of passphrase or decrypted plaintext; type="password" default + optional Eye toggle only |
| T-20-02 | Spoofing | Fake JSON import | mitigate | `parseAppDataFromImport` remains gate before replacing data; decrypt path must use `isEncryptedEnvelope` shape check before decrypt UI |
| T-20-03 | Tampering | Malformed envelope | mitigate | `decryptData` + AES-GCM; failures surfaced as wrong-passphrase or generic decrypt error per CONTEXT |
| T-20-04 | Denial of service | Large file JSON.parse | accept | Same as pre-phase behavior; no new server |

</threat_model>

<verification>
1. `npm test` — full Vitest suite green (includes `cryptoUtils` regression)
2. `npm run build` — TypeScript + Vite build green
3. `eslint .` — no new errors in `SettingsPage.tsx` (run if project uses lint in CI)
4. Manual smoke (per `20-VALIDATION.md`): blank export → plain JSON; passphrase export → `encrypted: true` in file; plain import → no decrypt UI; encrypted import → decrypt UI; wrong passphrase → inline error; correct passphrase → confirmation dialog and load
</verification>

<success_criteria>
- ENC-01 / ENC-04 / ENC-05 / ENC-06 satisfied per ROADMAP success criteria and CONTEXT decisions
- Single-file change surface: `src/pages/SettingsPage.tsx` only
- No new npm dependencies
</success_criteria>

<output>
After execution, add `.planning/phases/20-settings-ui-encrypted-export-import/20-01-SUMMARY.md` and run `/gsd-verify-work` for UAT
</output>

---

## PLANNING COMPLETE
