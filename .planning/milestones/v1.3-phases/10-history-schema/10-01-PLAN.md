---
wave: 1
depends_on: []
files_modified:
  - src/types/data.ts
  - src/context/AppDataContext.tsx
  - src/pages/DashboardPage.tsx
  - data.example.json
autonomous: true
requirements_addressed: [NWH-01, NWH-02, NWH-03, NWH-05]
---

# Plan 10-01 — History & schema (netWorthHistory + Record snapshot)

<objective>
Ship **NWH-01** / **NWH-02** / **NWH-05** / **NWH-03** (record + persist only): add **`netWorthHistory`** to `DataSchema` with `{ recordedAt, totalInr }[]`, **migrate** legacy JSON missing the key to **`[]`**, extend **`createInitialData()`** and **`data.example.json`**, wire **Record snapshot** on the Dashboard per **`10-CONTEXT.md`** and **`10-UI-SPEC.md`**, and persist with **`saveData`**. **Out of scope:** chart (Phase 11).
</objective>

<threat_model>
| ID | STRIDE | Severity | Mitigation in plan |
|----|--------|----------|---------------------|
| T-10-01-01 | Tampering (malicious `data.json` / import) | low | **Unchanged** — `DataSchema.safeParse` on load/import; `POST` still JSON-only in plugin; no `eval` |
| T-10-01-02 | DoS (huge `netWorthHistory` array) | low | Local app; optional length cap is **out of scope** (same as 10.1) |
| T-10-01-03 | **Inconsistent snapshot value** (wrong `totalInr`) | med | **Disable** record when **skeleton** or **exclusion/partial** total; store value from same **`sumForNetWorth(totals)`** as displayed row (after `roundCurrency` in chain) |
| T-10-01-04 | **Partial write** | high | Reuse existing **`saveData`**: revert in-memory on failed **POST** |
</threat_model>

<must_haves>
- `AppData` includes **`netWorthHistory: { recordedAt: string; totalInr: number }[]`**; **Zod** validates; **`recordedAt`** is ISO-8601 (`z.string().datetime()`).
- Boot and **`parseAppDataFromImport`** (import) use the same pipeline: **legacy bank migrate** (existing) + **ensure missing `netWorthHistory` → `[]`** + **`DataSchema.safeParse`**.
- **`createInitialData()`** returns `netWorthHistory: []`; full reset still clears history (**NWH-05**).
- **Dashboard** shows **Record snapshot** (outline) **under** the net worth total **Card** when user has holdings; **disabled** when `showNetWorthSkeleton` **or** the exclusion / partial-total case (`excludedNames.length > 0` per current `DashboardPage` logic) **or** `saveData` in flight.
- `npm run build` and `npm run lint` pass.
- **Regression (manual or scripted grep):** `parseAppDataFromImport` and initial load use **`ensureNetWorthHistory`** (or exact name) before **safeParse**.
</must_haves>

---

## Task 10-01-01 — Schema, types, example file

<read_first>
- `src/types/data.ts` (entire)
- `data.example.json`
- `CLAUDE.md` (roundCurrency / no fake totals)
- `.planning/phases/10-history-schema/10-CONTEXT.md` (D-01, D-02, D-11)
</read_first>

<action>
1. In **`src/types/data.ts`**, define **`NetWorthPointSchema`**: `z.object({ recordedAt: z.string().datetime(), totalInr: z.number().nonnegative() })`. Export type **`export type NetWorthPoint = z.infer<typeof NetWorthPointSchema>`** (or the name you choose, but use one exported type for reuse).

2. Add **`netWorthHistory: z.array(NetWorthPointSchema)`** to **`DataSchema`** (root object) **as a required key** (same level as `version`, `settings`, `assets` — do **not** nest under `assets`).

3. In **`data.example.json`**, add **`"netWorthHistory": []`** at the **root** next to `version` / `settings` / `assets` (valid JSON, trailing comma disallowed in JSON).
</action>

<acceptance_criteria>
- `rg "NetWorthPointSchema" src/types/data.ts` — **exit 0**, at least one match.
- `rg "netWorthHistory" src/types/data.ts` — **exit 0**.
- `rg '"netWorthHistory"' data.example.json` — **exit 0**, value is an empty array `[]`.
- `DataSchema` parse of `readFileSync('data.example.json')` shape **would** pass after Task 10-01-02 wiring (or run **Node/TS** one-liner in verification if you add a temporary test script — *optional*; minimum: **grep** the example file has the key).
- `npm run build` **exit 0** after **Task 10-01-02** completes (schema alone will **fail** to compile until `createInitialData` and migration exist — this task’s **acceptance** is fully checked **with Task 10-01-02**; after full phase: **`npm run build` exit 0**).
</acceptance_criteria>

<verification>
```bash
rg "NetWorthPointSchema|netWorthHistory" src/types/data.ts
rg "netWorthHistory" data.example.json
```
</verification>

---

## Task 10-01-02 — Load migration, `createInitialData`, and shared parse path

<read_first>
- `src/context/AppDataContext.tsx` (entire)
- `src/types/data.ts` (from Task 10-01-01)
- `10-CONTEXT.md` (D-03, D-11, D-12) — migration + reset + import alignment
</read_first>

<action>
1. Add **`function ensureNetWorthHistory(raw: unknown): unknown`**: if `raw` is a non-null object and **`'netWorthHistory' in raw` is false** or the value is **`undefined`**, return a shallow copy with **`netWorthHistory: []`**. If the key is **present** but the value is **not** a plain **array** (e.g. `null`, `string` — *hand-edited bad file*), return **`raw` unchanged** and let **`DataSchema.safeParse`** **reject**; **only** the **missing-key** v1.2 case is pre-filled per **D-03**.

2. In **`parseAppDataFromImport`**, apply migrations in this **exact order**:
   - `migrateLegacyBankAccounts(raw)`
   - `ensureNetWorthHistory(…result)`
   - `DataSchema.safeParse(…)`
   - Refactor the duplicate chain so **initial** `useEffect` load uses **`parseAppDataFromImport(responseJson)`** (already expected pattern) — the net effect must be **one** function for “unknown → `AppData` or ZodError” for **import + load**.

3. In **`createInitialData()`**, add **`netWorthHistory: []`** to the returned object so it matches **`DataSchema`**.

4. **Confirm** **`export const INITIAL_DATA: AppData = createInitialData()`** still compiles; **no** other call sites of `createInitialData` need a signature change.
</action>

<acceptance_criteria>
- `AppDataContext.tsx` contains the substring **`ensureNetWorthHistory`** and **`parseAppDataFromImport`**.  
- `createInitialData`’s return literal or spread includes **`netWorthHistory: []`**.  
- `npm run build` **exit 0**; `npm run lint` **exit 0**.
</acceptance_criteria>

<verification>
```bash
npm run build
npm run lint
rg "ensureNetWorthHistory" src/context/AppDataContext.tsx
rg "netWorthHistory: \[\]" src/context/AppDataContext.tsx
```
</verification>

---

## Task 10-01-03 — Dashboard: `Record snapshot` + persist

<read_first>
- `src/pages/DashboardPage.tsx` (entire)
- `src/lib/dashboardCalcs.ts` — `sumForNetWorth`, `calcCategoryTotals`
- `src/lib/financials.ts` — `roundCurrency` (if you snapshot with explicit final round)
- `.planning/phases/10-history-schema/10-UI-SPEC.md`
- `10-CONTEXT.md` (D-05, D-07, D-08, D-10)
- `AppDataContext` — `saveData` contract
</read_first>

<action>
1. Import **`useAppData`**, **shadcn `Button`**, **`Loader2`**, and **`roundCurrency`** as needed. Keep **`useLivePrices`**; reuse existing **`totals`**, **`grandTotal`**, **`showNetWorthSkeleton`**, and **`excludedNames`** (or the **exclusion** boolean) from **`DashboardPage`**.  

2. Compute **`canRecordSnapshot`**: `true` only if **`!empty`** and **`!showNetWorthSkeleton`** and **`excludedNames.length === 0`** (or **equivalent** to “no category excluded from the net worth total” in this file’s logic). *Claude’s discretion:* a single `useMemo` for the boolean.  

3. **UI** (per **10-UI-SPEC**): when **`!empty`**, render a wrapper **between** the first **`Card` (net worth total)** and the second **`Card` (categories)** (same React fragment that currently has both cards). Place **`Button` `variant="outline"`** with children **`Record snapshot`**, **`disabled={!canRecordSnapshot || isRecording}`** (or equivalent; **`isRecording`** = while **`saveData`** promise in flight for this action). If **`!canRecordSnapshot`**, *optional* muted helper one line: when **`showNetWorthSkeleton`**, e.g. “Waiting for live prices…”; when **exclusion**, point to **Settings** / **rates** per existing copy tone — *Claude’s discretion* exact strings.  

4. **Handler:** `onClick` → build **`const next: AppData = { ...data, netWorthHistory: [...data.netWorthHistory, { recordedAt: new Date().toISOString(), totalInr: roundCurrency(sumForNetWorth(totals)) } ] }`**. (Use **`roundCurrency` + `sumForNetWorth(totals)`** to stay aligned with the displayed aggregate.) **`await saveData(next)`** inside **try**; on **throw**, set local error state and show **inline** error with **`role="alert"`** (e.g. same tone as Settings: *“Could not save. Check that the app is running and try again.”* — *Claude’s discretion* exact string). On success, *optional* muted **`role="status"`** success line.  

5. **Do not** add chart components in this task.
</action>

<acceptance_criteria>
- `DashboardPage.tsx` includes **`Record snapshot`**, **`variant="outline"`**, and **`useAppData`**.  
- `DashboardPage.tsx` includes **`netWorthHistory`** in an object spread for **`saveData`**, and **`new Date().toISOString()`** in the new row.  
- `npm run build` **exit 0**; `npm run lint` **exit 0**.  
- Grep: **`excludedNames.length` or `!canRecordSnapshot`** and **`showNetWorthSkeleton`** are both referenced in the new logic (or **equivalent** variables) so **loading** and **exclusion** both **disable** the button.
</acceptance_criteria>

<verification>
```bash
npm run build
npm run lint
rg "Record snapshot|useAppData|netWorthHistory" src/pages/DashboardPage.tsx
```
</verification>

---

## Post-implementation (executor / UAT, not a separate code task)

- **Manual — ROADMAP success criteria:** Open an **old v1.2** `data.json` without **`netWorthHistory`**; app **loads** without error; new key appears after first save. **Record snapshot**; **`data.json`** on disk has **`netWorthHistory`** with one entry. **Clear all data** → **`netWorthHistory`** empty again. **Settings → Import** with **export** round-trip still works.  
- **10.1 regression:** `parseAppDataFromImport` on export file **includes** or **migrates** `netWorthHistory` as needed.

---

## PLANNING COMPLETE

*Single plan; three tasks; wave 1. Research: **skipped** (`--skip-research`). Nyquist **VALIDATION.md** not generated (no `RESEARCH.md` this run).*

## VERIFICATION PASSED

*Orchestrator (local): `10-01-PLAN` covers NWH-01, NWH-02, NWH-03 (record), NWH-05; maps to 10-CONTEXT, 10-UI-SPEC, 10-PATTERNS; threat model and must_haves present.*
