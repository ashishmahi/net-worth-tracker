---
phase: 27-settings-commodity-pricing-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/data.ts
  - src/lib/silverLiveHints.ts
  - src/lib/__tests__/silverLiveHints.test.ts
  - src/lib/dashboardCalcs.ts
  - src/lib/__tests__/dashboardCalcs.test.ts
  - src/context/SilverSpotPricesSync.tsx
  - src/App.tsx
  - src/pages/SettingsPage.tsx
  - src/pages/DashboardPage.tsx
  - .planning/phases/27-settings-commodity-pricing-ux/27-UAT.md
autonomous: true
requirements:
  - UX-04
  - UX-05
  - UX-06
  - UX-07
must_haves:
  truths:
    - "SettingsSchema includes optional silverInrPerGram and silverPricesLocked"
    - "calcCategoryTotals uses effective silver INR/g that respects locked manual override"
    - "Gold and silver pricing cards default read-only when live feeds healthy; Edit reveals inputs"
    - "When silver or gold live feed unhealthy after load, pricing inputs are editable without pressing Edit first"
  artifacts:
    - path: "src/lib/silverLiveHints.ts"
      provides: "liveSilverInrPerGram, effectiveSilverInrPerGramForNetWorth, shouldAutoSyncSilverFromSpot"
    - path: "src/context/SilverSpotPricesSync.tsx"
      provides: "persist live silver INR/g when auto-sync allowed"
  key_links:
    - from: "src/pages/SettingsPage.tsx"
      to: "27-UI-SPEC.md"
      via: "read-only + Edit layout"
      pattern: "commodity pricing cards"
---

<objective>
Execute Phase 27: **compact read-only + Edit** commodity pricing on **Settings** for **gold** and **silver**, silver **persisted override** + **`SilverSpotPricesSync`**, **`effectiveSilver`** wiring in **`dashboardCalcs`**, optional dashboard loading polish, **Vitest** coverage, and **27-UAT.md** — satisfying **UX-04–UX-07**, **`27-CONTEXT.md`**, **`27-UI-SPEC.md`**, **`27-RESEARCH.md`**.
</objective>

<threat_model>
**Assets:** User-entered metal rates; third-party spot quotes already trusted from Phase 26.

| Threat | Mitigation |
|--------|------------|
| **T1** Wrong net worth after silver override | **Unit tests** for **`effectiveSilverInrPerGramForNetWorth`** locked vs live; **`sumCommoditiesInr`** integration cases. |
| **T2** User locked out when API down | **UX-06:** editable-by-default when feed unhealthy; persisted unlocked silver/gold still readable from storage. |
| **T3** XSS | No raw HTML from APIs; numeric fields only. |

**Block severity:** High — none beyond test + UX gates above.

</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-settings-commodity-pricing-ux/27-CONTEXT.md
@.planning/phases/27-settings-commodity-pricing-ux/27-RESEARCH.md
@.planning/phases/27-settings-commodity-pricing-ux/27-UI-SPEC.md
@.planning/phases/27-settings-commodity-pricing-ux/27-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema + silverLiveHints + dashboard silver effective + tests</name>
  <files>src/types/data.ts, src/lib/silverLiveHints.ts, src/lib/dashboardCalcs.ts, src/lib/__tests__/silverLiveHints.test.ts, src/lib/__tests__/dashboardCalcs.test.ts</files>
  <read_first>
    - src/types/data.ts
    - src/lib/dashboardCalcs.ts
    - src/lib/priceApi.ts
    - src/lib/financials.ts
    - src/lib/goldLiveHints.ts
  </read_first>
  <action>
    1. **`SettingsSchema`** (`src/types/data.ts`): add **`silverInrPerGram: z.number().nonnegative().optional()`** and **`silverPricesLocked: z.boolean().optional()`** with short comments mirroring gold.

    2. **New `src/lib/silverLiveHints.ts`**
       - Import **`roundCurrency`**, **`TROY_OZ_TO_GRAMS`** from **`priceApi`** (or **`financials`** + **`priceApi`** per existing gold file style).
       - **`liveSilverInrPerGram(silverUsdPerOz: number, usdInr: number): number`**  
         `roundCurrency((silverUsdPerOz / TROY_OZ_TO_GRAMS) * usdInr)`.
       - **`shouldAutoSyncSilverFromSpot(s: { silverInrPerGram?: number; silverPricesLocked?: boolean }): boolean`**  
         return **`false`** if **`s.silverPricesLocked === true`**; else **`true`** (no legacy carve-out).
       - **`effectiveSilverInrPerGramForNetWorth(settings, live)`** where **`settings`** exposes **`silverInrPerGram`** and **`silverPricesLocked`**, **`live`** has **`silverUsdPerOz`** and **`usdInr`**. Return type **`number | null`** per CONTEXT rules.  
         - If **`silverPricesLocked === true`** and **`silverInrPerGram`** is finite → return **`silverInrPerGram`**.  
         - Else if **`live.silverUsdPerOz != null`**, **`live.usdInr != null`** → return **`liveSilverInrPerGram(...)`**.  
         - Else if **`silverInrPerGram`** is defined (unlocked cached sync) → return **`silverInrPerGram`**.  
         - Else **`null`**.

    3. **`calcCategoryTotals`** (`src/lib/dashboardCalcs.ts`): replace inline silver ₹/g computation with **`effectiveSilverInrPerGramForNetWorth(data.settings, { silverUsdPerOz: live.silverUsdPerOz, usdInr: live.usdInr })`**; pass result into **`sumCommoditiesInr`** unchanged.

    4. **Tests**
       - **`silverLiveHints.test.ts`**: at minimum — pure live formula; **locked** uses settings; **unlocked** prefers live over stale cache when both exist; **missing live** falls back to cached **`silverInrPerGram`** when present.
       - **`dashboardCalcs.test.ts`**: extend commodities cases so **`standard` silver** uses locked **`silverInrPerGram`** when live would differ (construct **`AppData`** with **`settings.silverInrPerGram`**, **`silverPricesLocked: true`**).

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run src/lib/__tests__/silverLiveHints.test.ts src/lib/__tests__/dashboardCalcs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "silverInrPerGram" src/types/data.ts` exits 0
    - `grep -q "effectiveSilverInrPerGramForNetWorth" src/lib/dashboardCalcs.ts` exits 0
    - `test -f src/lib/silverLiveHints.ts` exits 0
    - `npm test -- --run src/lib/__tests__/silverLiveHints.test.ts src/lib/__tests__/dashboardCalcs.test.ts` exits 0
  </acceptance_criteria>
  <done>Silver schema + effective ₹/g + tests green</done>
</task>

<task type="auto">
  <name>Task 2: SilverSpotPricesSync + mount</name>
  <files>src/context/SilverSpotPricesSync.tsx, src/App.tsx</files>
  <read_first>
    - src/context/GoldSpotPricesSync.tsx
    - src/context/LivePricesContext.tsx
    - src/context/AppDataContext.tsx
    - src/App.tsx
  </read_first>
  <action>
    1. Add **`SilverSpotPricesSync`** mirroring **`GoldSpotPricesSync`**: **`useAppData`**, **`useLivePrices`**, compute **`liveSilverInrPerGram`** when **`silverUsdPerOz`** and **`usdInr`** available; if **`silverError`** or still loading with no numbers, return early; if **`!shouldAutoSyncSilverFromSpot(data.settings)`** return; if value unchanged vs **`data.settings.silverInrPerGram`**, return; else **`saveData`** with **`silverInrPerGram`**, **`silverPricesLocked: false`**, **`updatedAt: nowIso()`**.

    2. **`App.tsx`**: render **`<SilverSpotPricesSync />`** immediately adjacent to **`<GoldSpotPricesSync />`** (inside same provider scope).

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "SilverSpotPricesSync" src/App.tsx` exits 0
    - `grep -q "shouldAutoSyncSilverFromSpot" src/context/SilverSpotPricesSync.tsx` exits 0
    - `npm test -- --run` exits 0
  </acceptance_criteria>
  <done>Silver auto-persist when unlocked</done>
</task>

<task type="auto">
  <name>Task 3: Settings gold + silver UX (read-only, Edit, failure path)</name>
  <files>src/pages/SettingsPage.tsx</files>
  <read_first>
    - src/pages/SettingsPage.tsx
    - .planning/phases/27-settings-commodity-pricing-ux/27-UI-SPEC.md
    - src/components/ui/button.tsx
  </read_first>
  <action>
    1. **Gold block:** Introduce local state **`goldPricingEditing`** (boolean).  
       - **`pricingHealthyGold`** per **27-UI-SPEC** / **27-CONTEXT D-01**.  
       - If **`pricingHealthyGold`** and **`!goldPricingEditing`**: show **compact read-only** summary (24K/22K/18K ₹/g on one or two lines) using **effective** values from **`data.settings.goldPrices`** when present else **`k24Hint`**/**`k22Hint`**/**`k18Hint`**. Include **`Edit`** (`type="button"`).  
       - **`Edit`:** set **`goldPricingEditing=true`**; **`goldForm.reset(...)`** with **`formatInrPerGramInput`** for numeric defaults from effective snapshot.  
       - **`Cancel`:** **`goldPricingEditing=false`**; **`reset`** to last saved effective snapshot.  
       - **`Save`:** keep existing **`onGoldSubmit`** (sets **`goldPricesLocked: true`**). After successful save, set **`goldPricingEditing=false`**.  
       - If **`!pricingHealthyGold`** (after stable error/missing): treat as edit-required (equivalent to **`goldPricingEditing`** true): show inputs + errors without requiring **Edit** first.

    2. **Silver block (new Card below Gold or above Retirement—match visual rhythm):** local **`silverPricingEditing`**; single-field zod form **`silverPriceSchema`** `{ silver: z.string().min(1) }` or reuse pattern from gold string fields.  
       - **`pricingHealthySilver`**; **`silverHint`** = **`liveSilverInrPerGram(silverUsdPerOz, usdInr)`** when spot+forex OK.  
       - Read-only: display effective ₹/g per **`effectiveSilverInrPerGramForNetWorth`**.  
       - **Edit / Save / Cancel** mirror gold; **Save** writes **`silverInrPerGram: parseFinancialInput(...)`**, **`silverPricesLocked: true`**, **`updatedAt`**.  
       - Add **`onUseLiveSilverSpot`** when locked: **`silverPricesLocked: false`**, seed **`silverInrPerGram`** from current live ₹/g (mirror **`onUseLiveGoldSpot`**).

    3. Preserve existing gold behaviors: **Use live spot** row when **`!goldSpotAutoSync`**, error alerts, **`goldSaveError`**.

    4. If **`SettingsPage.tsx`** exceeds ~900 lines after edits, extract **`SettingsGoldPricingCard`** and **`SettingsSilverPricingCard`** under **`src/components/settings/`** with typed props.

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `grep -q "goldPricingEditing" src/pages/SettingsPage.tsx` exits 0 OR `grep -q "goldPricingEditing" src/components/settings/*.tsx` exits 0
    - `grep -qE "Silver price|Silver pricing|silverPricingEditing" src/pages/SettingsPage.tsx` OR `grep -q "silverInrPerGram" src/pages/SettingsPage.tsx` exits 0
  </acceptance_criteria>
  <done>Settings matches 27-UI-SPEC interaction model</done>
</task>

<task type="auto">
  <name>Task 4: Dashboard loading polish (optional but in-scope)</name>
  <files>src/pages/DashboardPage.tsx</files>
  <read_first>
    - src/pages/DashboardPage.tsx
    - src/lib/silverLiveHints.ts
  </read_first>
  <action>
    1. **`showNetWorthSkeleton`**: when user holds **only** silver commodities and **`data.settings.silverPricesLocked === true`** with **`silverInrPerGram`** defined, **do not** block headline on **`silverLoading`** (net worth can use locked rate). Implement minimal boolean helper or inline condition.

    2. Do **not** change **`calcCategoryTotals`** here — Task 1 owns math.

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run src/lib/__tests__/dashboardCalcs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "silverLoading" src/pages/DashboardPage.tsx` exits 0 (still referenced; logic may be conditional)
    - `npm test -- --run` exits 0
  </acceptance_criteria>
  <done>Dashboard skeleton respects locked silver when feasible</done>
</task>

<task type="manual">
  <name>Task 5: UAT checklist</name>
  <files>.planning/phases/27-settings-commodity-pricing-ux/27-UAT.md</files>
  <read_first>
    - .planning/phases/27-settings-commodity-pricing-ux/27-UI-SPEC.md
  </read_first>
  <action>
    Create **`27-UAT.md`** with **Given / When / Then** rows covering: (1) healthy feeds → read-only gold+silver, Edit opens form; (2) gold/silver API error → inputs editable without Edit; (3) Save locks prices; (4) Use live spot unlocks; (5) dashboard commodities row uses locked silver.

  </action>
  <verify>
    <automated>test -f .planning/phases/27-settings-commodity-pricing-ux/27-UAT.md &amp;&amp; grep -q Edit .planning/phases/27-settings-commodity-pricing-ux/27-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/27-settings-commodity-pricing-ux/27-UAT.md` exits 0
    - File contains the substring `Edit` and `read-only` or `read only`
  </acceptance_criteria>
  <done>UAT doc ready for human verification</done>
</task>

</tasks>

## PLANNING COMPLETE

**Plans:** 1 · **Waves:** 1 sequential batch (Tasks 1–2 parallelizable by different agents; default executor runs in order).  
**Requirement coverage:** **UX-04** Tasks 3–5 · **UX-05** Task 3 · **UX-06** Task 3 · **UX-07** Tasks 1–3.
