---
phase: 18-dashboard-net-worth-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/DashboardPage.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04

must_haves:
  truths:
    - "Headline net worth equals calcNetWorth(grossAssets, sumLiabilitiesInr(data)); snapshots record that value"
    - "Total Debt row shows sumAllDebtInr when > 0 and navigates to Liabilities"
    - "Debt-to-asset ratio line appears when total debt > 0; 0% when gross assets === 0"
    - "Asset % column uses gross assets as denominator, not net worth"
    - "npm test and npx tsc -b --noEmit succeed"
  artifacts:
    - path: "src/pages/DashboardPage.tsx"
      provides: "Debt-aware dashboard headline, row, ratio, snapshot, empty-state gate"
      contains: "calcNetWorth"
      contains: "sumAllDebtInr"
      contains: "onNavigate('liabilities')"
  key_links:
    - from: "DashboardPage"
      to: "calcNetWorth / sumLiabilitiesInr / sumAllDebtInr"
      via: "@/lib/liabilityCalcs"
      pattern: "liabilityCalcs"
---

<objective>
Deliver **Phase 18 — Dashboard & Net Worth Integration**: wire `liabilityCalcs` into `DashboardPage.tsx` so the headline and new snapshots use **`calcNetWorth(grossAssets, sumLiabilitiesInr(data))`**, add a conditional **Total Debt** row (`sumAllDebtInr`, destructive-colored INR, navigate to liabilities), add a conditional **Debt-to-asset ratio** under the headline when debt exists, fix **`noHoldingsYet`** for liabilities-only users, and use **`grossAssets`** for **`percentOfTotal`** and the `%` column zero guard — matching **`18-CONTEXT.md`** and **`18-UI-SPEC.md`**.
</objective>

<threat_model>
## Trust boundaries

Local-only app; same JSON persistence as existing dashboard. No new HTTP routes or env vars.

## STRIDE (ASVS L1)

| Threat ID | Category | Component | Severity | Mitigation |
|-----------|----------|-----------|----------|------------|
| T-18-01 | Tampering | POST `/api/data` snapshot | Low | Existing `saveData` + `DataSchema`; snapshot value is computed client-side only |
| T-18-02 | XSS | N/A new user HTML | — | No new raw HTML; labels static |

**Block policy:** No HIGH threats identified for this phase.
</threat_model>

<execution_context>
@.planning/phases/18-dashboard-net-worth-integration/18-CONTEXT.md
@.planning/phases/18-dashboard-net-worth-integration/18-UI-SPEC.md
@.planning/phases/18-dashboard-net-worth-integration/18-RESEARCH.md
@.planning/phases/18-dashboard-net-worth-integration/18-PATTERNS.md
@.planning/phases/18-dashboard-net-worth-integration/18-VALIDATION.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Imports, memos, empty state, headline, snapshot, % column</name>
  <files>src/pages/DashboardPage.tsx</files>
  <read_first>
    - src/pages/DashboardPage.tsx (full file)
    - src/lib/liabilityCalcs.ts
    - src/lib/dashboardCalcs.ts (sumForNetWorth, percentOfTotal)
    - .planning/phases/18-dashboard-net-worth-integration/18-CONTEXT.md
    - .planning/phases/18-dashboard-net-worth-integration/18-UI-SPEC.md
  </read_first>
  <action>
    **Imports:** Add `calcNetWorth`, `sumLiabilitiesInr`, `sumAllDebtInr`, `debtToAssetRatio` from `@/lib/liabilityCalcs`.

    **Memos:** Replace the single `grandTotal` memo with:
    - `grossAssets = sumForNetWorth(totals)` (keep dependency `[totals]`).
    - `netWorth = calcNetWorth(grossAssets, sumLiabilitiesInr(data))` with dependency `[grossAssets, data]` (or inline `sumForNetWorth(totals)` inside a `[totals, data]` memo — prefer separate `grossAssets` + `netWorth` for readability).

    Add **`totalDebtAll = sumAllDebtInr(data)`** in a `useMemo` with dependency **`[data]`** — required for Task 2 ratio/row conditions (keep **sumAllDebtInr** import even if only used for this memo after Task 2 JSX lands).

    **`noHoldingsYet`:** Append `&& data.liabilities.length === 0` to the boolean chain so users with only liabilities get the full dashboard (not the empty state).

    **Headline card:** Where the net worth figure is rendered (`CardTitle` with `inrNoDecimals(...)`), use **`netWorth`** instead of **`grandTotal`**. Keep **`showNetWorthSkeleton`** behavior unchanged.

    **`handleRecordSnapshot`:** Replace `roundCurrency(sumForNetWorth(totals))` with **`roundCurrency(netWorth)`** using the same rounding as today — compute `netWorth` inside the callback from current `totals`/`data` if needed so the closure is correct:  
    `const gross = sumForNetWorth(totals); const nw = calcNetWorth(gross, sumLiabilitiesInr(data)); const totalInr = roundCurrency(nw);`  
    Update `useCallback` dependency array to include **`data`** and **`totals`** (or the derived values).

    **Category breakdown `%`:** In the `DASHBOARD_CATEGORY_ORDER.map` loop, change **`percentOfTotal(v, grandTotal)`** to **`percentOfTotal(v, grossAssets)`**. Change the `%` column guard from **`grandTotal <= 0`** to **`grossAssets <= 0`** (CONTEXT D-11).

    **Rename references:** Replace remaining display uses of `grandTotal` that meant “net worth” with `netWorth`. Ensure no accidental use of `grandTotal` identifier unless removed entirely.
  </action>
  <acceptance_criteria>
    - `grep -n "from '@/lib/liabilityCalcs'" src/pages/DashboardPage.tsx` returns at least one match
    - `grep -n "calcNetWorth" src/pages/DashboardPage.tsx` returns at least one match
    - `grep -n "sumLiabilitiesInr" src/pages/DashboardPage.tsx` returns at least one match
    - `grep -n "data.liabilities.length === 0" src/pages/DashboardPage.tsx` appears inside `noHoldingsYet` (or equivalent logic)
    - `grep -n "percentOfTotal" src/pages/DashboardPage.tsx` shows second argument `grossAssets` not `grandTotal`
    - `grep -n "grandTotal" src/pages/DashboardPage.tsx` returns no matches **or** only comments (prefer zero matches)
    - `npm test` exits 0
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Debt-to-asset ratio line + Total Debt row</name>
  <files>src/pages/DashboardPage.tsx</files>
  <read_first>
    - src/pages/DashboardPage.tsx (after Task 1 edits)
    - .planning/phases/18-dashboard-net-worth-integration/18-UI-SPEC.md
  </read_first>
  <action>
    **Ratio line (DASH-03):** Inside the headline **`CardHeader`** (below `CardTitle`, still when not skeleton), conditionally render when **`totalDebtAll > 0`** a single line: **`text-sm text-muted-foreground`** with copy **`Debt-to-asset ratio:`** followed by **`Math.round(debtToAssetRatio(totalDebtAll, grossAssets))`** (or equivalent integer display) and **`%`**. When **`grossAssets === 0`**, `debtToAssetRatio` returns **0** — display **`0%`** per function contract.

    **Total Debt row (DASH-02):** After the **`DASHBOARD_CATEGORY_ORDER.map`** block closes — inside the same **`CardContent`** — if **`totalDebtAll > 0`**, render **`<Separator />`** then a **`<button type="button">`** matching asset row classes (`hover:bg-muted/50 flex w-full items-center justify-between gap-2 px-4 py-3 text-left transition-colors`). Label:** **`Total Debt`**. Value:** `inrNoDecimals(totalDebtAll)` **wrapped in `text-destructive` or `text-destructive/80`**. Right-hand **`%` slot:** show **`—`** (em dash), same width alignment as other rows (`w-10 text-right`). **`onClick`:** `onNavigate('liabilities')`. **`aria-label`:** e.g. **`Open Liabilities section`**.

    Ensure **`Separator`** between the last category row and Total Debt does not duplicate an extra separator before the first category row (only one separator before Total Debt).
  </action>
  <acceptance_criteria>
    - `grep -n "Debt-to-asset ratio" src/pages/DashboardPage.tsx` returns at least one match
    - `grep -n "debtToAssetRatio" src/pages/DashboardPage.tsx` returns at least one match
    - `grep -n "sumAllDebtInr" src/pages/DashboardPage.tsx` returns at least one match
    - `grep -n "onNavigate('liabilities')" src/pages/DashboardPage.tsx` returns at least one match
    - `grep -n "Total Debt" src/pages/DashboardPage.tsx` returns at least one match
    - `grep -n "text-destructive" src/pages/DashboardPage.tsx` appears on the Total Debt value (same line or wrapping element)
    - `npm test` exits 0
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
## Automated

- `npm test`
- `npx tsc -b --noEmit`

## Goal-backward (ROADMAP success criteria)

1. Headline equals gross minus standalone liabilities — matches **`calcNetWorth`** with **`sumLiabilitiesInr`**.
2. Total Debt row shows **`sumAllDebtInr`** and links to Liabilities.
3. Ratio from **`debtToAssetRatio`**; 0% when no assets.
4. New snapshots store **`calcNetWorth`** result; prior history entries untouched by code path.
</verification>

---

## PLANNING COMPLETE
