---
phase: 25-github-actions-ci-cd-beta-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci-pages.yml
  - README.md
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-03
  - DEPLOY-01
  - DEPLOY-02
  - BETA-01
must_haves:
  truths:
    - "`.github/workflows/ci-pages.yml` runs on `pull_request` and on `push` to `main`, and runs `npm ci`, `npm test`, and `npm run build` with `BASE_URL=/net-worth-tracker/`"
    - "Deploy to GitHub Pages runs only on `push` to `main` after CI succeeds, using `actions/upload-pages-artifact` and `actions/deploy-pages`"
    - "`README.md` includes the Project Pages URL pattern, the word `beta`, and that wealth data stays in browser localStorage only"
  artifacts:
    - path: ".github/workflows/ci-pages.yml"
      provides: "PR + main CI; Pages deploy on main with shared dist artifact"
    - path: "README.md"
      provides: "Public URL pattern + beta + client-only data disclaimer"
  key_links:
    - from: ".github/workflows/ci-pages.yml"
      to: "vite.config.ts"
      via: "BASE_URL must match Phase 24 normalizeBaseUrl for dist/"
---

<objective>
Implement Phase 25: add GitHub Actions CI for PRs and **`main`**, automated GitHub Pages deploy on **`main`** using the same **`BASE_URL`** as Phase 24, and README beta + URL + local-only data messaging for **CI-01–CI-03**, **DEPLOY-01–DEPLOY-02**, and **BETA-01**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-github-actions-ci-cd-beta-access/25-RESEARCH.md
@.planning/phases/25-github-actions-ci-cd-beta-access/25-PATTERNS.md
</context>

<threat_model>
| ID | Threat | Severity | Mitigation in this phase |
|----|--------|----------|---------------------------|
| T-25-01 | Malicious PR workflow trying to exfiltrate secrets or deploy | High | Deploy job **`if: github.ref == 'refs/heads/main' && github.event_name == 'push'`** only; **`pages: write` / `id-token: write`** scoped to deploy job; CI on PR uses **`contents: read`** only |
| T-25-02 | Supply-chain via unofficial Actions | Medium | Use **`actions/*`** official actions only; pin major versions (**`@v4`**, **`@v3`**) per CI-03 |
| T-25-03 | Asset 404 on Pages due to **`base`** drift | Medium | Set **`env.BASE_URL: /net-worth-tracker/`** for every **`npm run build`** in CI; grep **`dist/index.html`** for **`net-worth-tracker`** after build |

Blocker threshold: **high** — T-25-01 mitigated by branch/event guards; no remaining high-severity unmitigated threats.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add CI + GitHub Pages workflow</name>
  <files>.github/workflows/ci-pages.yml</files>
  <read_first>
    - package.json
    - vite.config.ts
    - README.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/25-github-actions-ci-cd-beta-access/25-RESEARCH.md
  </read_first>
  <action>
    1. Create **`.github/workflows/ci-pages.yml`** (filename exact).

    2. Set workflow **`name:`** to **`CI and GitHub Pages`**.

    3. Set **`on:`** to:
       - **`pull_request:`** (no branch filter — all PRs)
       - **`push:`** with **`branches: [main]`**

    4. At workflow root set **`concurrency:`**:
       - **`group: pages-${{ github.workflow }}-${{ github.ref }}`**
       - **`cancel-in-progress: true`**

    5. Define job **`ci`** (runner **`ubuntu-latest`**):
       - **`permissions:`** **`contents: read`**
       - **`env:`** **`BASE_URL: /net-worth-tracker/`**
       - Steps in order:
         - **`actions/checkout@v4`**
         - **`actions/setup-node@v4`** with **`node-version: '20'`** and **`cache: 'npm'`**
         - Run **`npm ci`**
         - Run **`npm test`**
         - Run **`npm run build`**
         - Step name **`Verify dist uses Pages base`** — run shell: **`grep -q "net-worth-tracker" dist/index.html`** (fails if missing)
         - **`actions/upload-artifact@v4`** with **`name: dist`**, **`path: dist`**, **`retention-days: 1`**

    6. Define job **`deploy`**:
       - **`needs: ci`**
       - **`if: github.ref == 'refs/heads/main' && github.event_name == 'push'`**
       - **`permissions:`** **`contents: read`**, **`pages: write`**, **`id-token: write`**
       - **`environment:`** **`name: github-pages`**, **`url: ${{ steps.deployment.outputs.page_url }}`**
       - Steps:
         - **`actions/download-artifact@v4`** with **`name: dist`**, **`path: dist`**
         - **`actions/upload-pages-artifact@v3`** with **`path: dist`**
         - **`actions/deploy-pages@v4`** with step **`id: deployment`**

    7. Do **not** add unrelated jobs (e.g. **`npm run lint`**) — CI-02 lists only **`npm ci`**, **`npm test`**, **`npm run build`**.

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Path exists: **`.github/workflows/ci-pages.yml`**
    - **`grep -q "pull_request:" .github/workflows/ci-pages.yml`** exits 0
    - **`grep -q "push:" .github/workflows/ci-pages.yml`** exits 0 and **`grep -q "main" .github/workflows/ci-pages.yml`** exits 0 (default-branch **`push`** trigger)
    - **`grep -q "npm ci" .github/workflows/ci-pages.yml`** exits 0
    - **`grep -q "npm test" .github/workflows/ci-pages.yml`** exits 0
    - **`grep -q "npm run build" .github/workflows/ci-pages.yml`** exits 0
    - **`grep -q "BASE_URL: /net-worth-tracker/" .github/workflows/ci-pages.yml`** exits 0
    - **`grep -q "actions/upload-artifact@v4" .github/workflows/ci-pages.yml`** exits 0
    - **`grep -q "actions/deploy-pages@v4" .github/workflows/ci-pages.yml`** exits 0
    - **`grep -q "if: github.ref == 'refs/heads/main'" .github/workflows/ci-pages.yml`** exits 0
    - **`npm test`** exits 0 (repo unchanged by YAML alone; confirms no accidental src edits required for this task)
  </acceptance_criteria>
  <done>CI and conditional Pages deploy workflow file added</done>
</task>

<task type="auto">
  <name>Task 2: README — beta URL and data disclaimer</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    1. Add a dedicated subsection (e.g. **`## Beta (GitHub Pages)`**) **after** the existing **Production build (GitHub Pages)** section (keep Phase 24 content intact).

    2. In that subsection, include all of the following **verbatim concepts** (exact wording may vary but these strings must appear somewhere in **`README.md`** as grep-verifiable hooks):
       - The substring **`beta`**
       - The substring **`https://`** and **`github.io`** (document the Project Pages pattern, e.g. **`https://<your-github-username>.github.io/net-worth-tracker/`**)
       - The substring **`localStorage`** (or **`local storage`** if grammar requires — prefer **`localStorage`** for grep)
       - A clear statement that wealth / portfolio data is **not** uploaded to GitHub or synced server-side (client-only persistence).

    3. Add a short bullet under the same section (or **Production build**) stating **one-time** repository setup: **Settings → Pages → Build and deployment → Source: GitHub Actions** (required for **`deploy`** job to succeed).

    4. Do not remove the Docker or Production build sections added in earlier phases.

  </action>
  <verify>
    <automated>grep -E -q "beta|Beta" README.md && grep -q "github.io" README.md && grep -q "localStorage" README.md</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -qi "beta" README.md`** exits 0
    - **`grep -q "github.io" README.md`** exits 0
    - **`grep -q "localStorage" README.md`** exits 0
    - **`grep -qi "GitHub Actions" README.md`** exits 0 (Pages source note)
    - **`grep -q "net-worth-tracker" README.md`** exits 0 (URL path consistency with **`BASE_URL`**)
  </acceptance_criteria>
  <done>README satisfies BETA-01 and documents Pages setup</done>
</task>

</tasks>

<verification>
1. **`npm test`** exits 0.
2. **`npm run build`** with **`BASE_URL=/net-worth-tracker/`** exits 0 locally (same as CI).
3. Confirm **`.github/workflows/ci-pages.yml`** parses (optional: run **`actionlint`** if installed — not required by REQUIREMENTS).
4. After merge to **`main`** on GitHub: **`deploy`** job succeeds and Pages URL loads (manual UAT per **`25-UAT.md`** when written).
</verification>

<success_criteria>
- CI-01 satisfied: PR + **`main`** **`push`** triggers; CI-02 satisfied: **`npm ci`**, **`npm test`**, **`npm run build`** with correct **`BASE_URL`**; CI-03 satisfied: named jobs + pinned major action versions.
- DEPLOY-01 satisfied: **`main`** deploy job publishes **`dist/`**; DEPLOY-02 satisfied: same **`BASE_URL`** as Phase 24 / README.
- BETA-01 satisfied: README contains public URL pattern, beta labeling, and localStorage-only data statement.
</success_criteria>
