---
phase: 05-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dashboardCalcs.ts
autonomous: true
requirements:
  - DASH-01
must_haves:
  truths:
    - "All seven category INR amounts follow 05-CONTEXT D-01–D-12; nothing is persisted; pure functions only"
    - "gold and bitcoin are number | null; other categories are number (bankSavings may exclude AED when aedInr null, still a finite sum)"
  artifacts:
    - path: "src/lib/dashboardCalcs.ts"
      provides: "calcCategoryTotals, grand total that excludes null categories, category key order for UI"
      contains: "calcCategoryTotals"
  key_links:
    - from: "src/lib/dashboardCalcs.ts"
      to: "src/types/data.ts"
      via: "import type AppData"
      pattern: "Zod-inferred data shapes"

---

<objective>
Add `src/lib/dashboardCalcs.ts` with pure functions to compute per-category INR totals and an aggregate total that **excludes** categories whose value is `null` (missing live rates or gold prices), matching **05-CONTEXT** D-01 through D-12. No React imports; no `data.json` writes. Consumed by Dashboard UI in **05-02-PLAN.md**.
</objective>

<threat_model>
**Scope:** New local read-only module; no network, no new persistence.

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-05-10 | `NaN` in totals (bad FP or undefined math) | Medium | Use `roundCurrency` from `financials.ts` per multiplication/sum; `outstandingLoanInr ?? 0` for property; guard gold when `goldPrices` absent |
| T-05-11 | Leaking mock/future test data in prod bundle | Low | No hardcoded user figures; only pure functions of `AppData` + price args |

**Block on:** high — N/A for this plan (math-only).
</threat_model>

<context>
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-PATTERNS.md
@src/types/data.ts
@src/lib/financials.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types and `calcCategoryTotals`</name>
  <files>src/lib/dashboardCalcs.ts</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/types/data.ts
    - /Users/a.mahindrakar/personal/fin/src/lib/financials.ts
    - /Users/a.mahindrakar/personal/fin/.planning/phases/05-dashboard/05-CONTEXT.md
  </read_first>
  <action>
1. Create **new** file `src/lib/dashboardCalcs.ts`.
2. `import type { AppData } from '@/types/data'` (or existing project alias) and `import { roundCurrency } from '@/lib/financials'`.
3. Export a const tuple **in fixed display order** (must match `05-UI-SPEC` and `AppSidebar` labels), e.g.:
   - `DASHBOARD_CATEGORY_ORDER = ['gold','mutualFunds','stocks','bitcoin','property','bankSavings','retirement'] as const` and `export type DashboardCategoryKey = typeof DASHBOARD_CATEGORY_ORDER[number]`.
4. Export a result type, e.g. `CategoryTotals` with fields: `gold: number | null` (null if `data.settings.goldPrices` is absent **and** gold has items, OR if prices missing for computation — per **D-08**: if `goldPrices` is undefined, treat gold total as `null`); `mutualFunds`, `stocks`, `property`, `retirement` as `number` using **D-11**/**D-03**/**D-01–D-02**; `bitcoin: number | null` when `btcUsd` or `usdInr` is `null` (**D-09**); `bankSavings: number` summing INR accounts and AED via `roundCurrency(balance * aedInr)` when `aedInr != null`, skipping AED when `aedInr` is `null` (**D-10**).
5. Implement `calcCategoryTotals(data: AppData, live: { btcUsd: number | null; usdInr: number | null; aedInr: number | null }): CategoryTotals`:
   - **Gold:** If `data.assets.gold.items.length === 0` → `gold: 0`. If length > 0 and `data.settings.goldPrices` is `undefined` → `gold: null`. Otherwise for each item map `item.karat` 24/22/18 to `k24`/`k22`/`k18` on `goldPrices` and `roundCurrency` after each `grams × price`. Match **05-RESEARCH** Pitfall 1.
   - **Mutual funds / stocks:** Sum `currentValue` with `roundCurrency` on accumulation.
   - **Bitcoin:** If `live.btcUsd == null` OR `live.usdInr == null` → `null`; else `roundCurrency(quantity * btcUsd * usdInr)`.
   - **Property:** For each item: if `item.hasLiability` then `roundCurrency(item.agreementInr - (item.outstandingLoanInr ?? 0))` else `item.agreementInr` (all **nonnegative**; use **D-01**). Sum with `roundCurrency` between items.
   - **Retirement:** `roundCurrency(nps + epf)` only (**D-03**).
6. Export `function sumForNetWorth(totals: CategoryTotals): number` that adds only fields that are not `null` (bitcoin and gold can be null). All additions use `roundCurrency`.
7. Export `function percentOfTotal(categoryValue: number, grandTotal: number): number` that returns `0` when `grandTotal <= 0`, else a 0–100 value (one decimal max optional) — document that UI shows `—` or `0%` for zero total per **05-RESEARCH** Pitfall 5.
8. Add `export function hasAedAccountsWithMissingRate(data: AppData, aedInr: number | null): boolean` — `true` when at least one `bankSavings.accounts` has `currency==='AED'` and `aedInr==null` (for disclaimer sub-line in plan 02).
9. **Do not** import `calcProjectedCorpus`.
  </action>
  <acceptance_criteria>
    - File `src/lib/dashboardCalcs.ts` exists and exports `calcCategoryTotals`, `sumForNetWorth`, `DASHBOARD_CATEGORY_ORDER`, and `hasAedAccountsWithMissingRate`
    - `cd /Users/a.mahindrakar/personal/fin && npx tsc --noEmit` exits 0
    - `cd /Users/a.mahindrakar/personal/fin && npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Run: `cd /Users/a.mahindrakar/personal/fin && npx tsc --noEmit` → exit 0
- Run: `cd /Users/a.mahindrakar/personal/fin && npm run build` → exit 0
- Grep: `rg "calcProjectedCorpus" src/lib/dashboardCalcs.ts` — **no** matches
</verification>

## PLANNING COMPLETE
