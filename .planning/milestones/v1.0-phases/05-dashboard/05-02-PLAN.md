---
phase: 05-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["05-01-PLAN.md"]
files_modified:
  - src/pages/DashboardPage.tsx
  - src/App.tsx
autonomous: true
requirements:
  - DASH-01
  - 05-UI-SPEC
must_haves:
  truths:
    - "Net worth total + seven clickable rows; rows call `onNavigate` with a key from `DASHBOARD_CATEGORY_ORDER` (asset sections only)"
    - "Skeleton in total and affected rows while `btcLoading`/`forexLoading` per D-06; D-07 disclaimers when categories excluded"
  artifacts:
    - path: "src/pages/DashboardPage.tsx"
      provides: "Full dashboard UI per 05-UI-SPEC; no writes to `data.json`"
      contains: "useAppData|useLivePrices|calcCategoryTotals"
    - path: "src/App.tsx"
      provides: "DashboardPage receives `onNavigate={setActiveSection}`"
      contains: "DashboardPage onNavigate|setActiveSection"
  key_links:
    - from: "src/pages/DashboardPage.tsx"
      to: "src/lib/dashboardCalcs.ts"
      via: "import"
      pattern: "calcCategoryTotals"

---

<objective>
Replace the `DashboardPage` stub with a read-only net worth **Card** and seven **clickable** category rows (per **05-UI-SPEC** and **05-CONTEXT** D-04–D-07). Update **`App.tsx`** to render `DashboardPage` with **`onNavigate={(key) => setActiveSection(key)}`** on the `dashboard` branch; keep other pages as zero-prop components from `SECTION_COMPONENTS`. No new `npm` dependencies.
</objective>

<threat_model>
**Scope:** Read-only local UI; navigation only; reuses `useLivePrices` (no new fetches in page).

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-05-20 | XSS from reflected user data in labels | Low | React text nodes only; no `dangerouslySetInnerHTML` |
| T-05-21 | Misleading net worth (wrong disclaimer) | Medium | Show D-07 disclaimer when any category is `null` or AED excluded; do not show a full total as complete when categories omitted |

**Block on:** high — D-07 disclaimer when totals exclude any category.
</threat_model>

<context>
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-UI-SPEC.md
@.planning/phases/05-dashboard/05-PATTERNS.md
@src/lib/dashboardCalcs.ts
@src/context/LivePricesContext.tsx
@src/components/AppSidebar.tsx
@src/pages/BankSavingsPage.tsx
@src/components/ui/card.tsx
@src/components/ui/skeleton.tsx
@src/components/ui/separator.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx special-case `DashboardPage` with `onNavigate`</name>
  <files>src/App.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/App.tsx
    - /Users/a.mahindrakar/personal/fin/src/components/AppSidebar.tsx
  </read_first>
  <action>
1. Import `type { SectionKey }` is already from `AppSidebar` via `AppSidebar` import line — keep `SectionKey` available.
2. Replace the single `<ActivePage />` pattern so that when `activeSection === 'dashboard'`, render `<DashboardPage onNavigate={setActiveSection} />`.
3. When `activeSection !== 'dashboard'`, set `const Page = SECTION_COMPONENTS[activeSection]` and render `<Page />` (or equivalent) with **no** props — same as today for non-dashboard sections.
4. **Do not** change `SECTION_COMPONENTS` to `ComponentType` with props; avoid typing casts that would allow passing props to all pages.
5. `DashboardPage` import remains from `@/pages/DashboardPage` — update its **props** in `DashboardPage.tsx` (Task 2).
  </action>
  <acceptance_criteria>
    - `src/App.tsx` contains the substring `onNavigate={setActiveSection}` adjacent to `DashboardPage`
    - `src/App.tsx` contains a conditional that renders `DashboardPage` only when `activeSection === 'dashboard'`
    - `cd /Users/a.mahindrakar/personal/fin && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: DashboardPage — data, live prices, and memoized totals</name>
  <files>src/pages/DashboardPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/lib/dashboardCalcs.ts
    - /Users/a.mahindrakar/personal/fin/src/context/AppDataContext.tsx
    - /Users/a.mahindrakar/personal/fin/src/context/LivePricesContext.tsx
    - /Users/a.mahindrakar/personal/fin/.planning/phases/05-dashboard/05-UI-SPEC.md
  </read_first>
  <action>
1. `export function DashboardPage({ onNavigate }: { onNavigate: (key: SectionKey) => void })` — import `SectionKey` from `@/components/AppSidebar`.
2. Call `const { data } = useAppData()` (read-only; do not call `saveData` in this file).
3. Call `const { btcUsd, usdInr, aedInr, btcLoading, forexLoading, btcError, forexError } = useLivePrices()` (use errors only if needed for subtle D-07 hints per **02** inline-error **spirit**).
4. `useMemo` to compute `totals = calcCategoryTotals(data, { btcUsd, usdInr, aedInr })` and `grandTotal = sumForNetWorth(totals)`.
5. Derive: `anyPriceLoading` = `btcLoading || forexLoading` (for **D-06** total/row skeletons as in **05-RESEARCH**).
6. For **D-07**: build `excludedLabels: string[]` for categories with `null` value (gold/bitcoin) using human-readable names matching sidebar: e.g. Gold, Bitcoin. If `grandTotal` used excluded categories, add a **one-line** `CardDescription` or muted paragraph below the list: plain language + points to **Settings** / rate refresh, per **05-UI-SPEC** Copywriting contract.
7. If `hasAedAccountsWithMissingRate(data, aedInr)` is `true`, optional muted sub-line on Bank savings row: **`AED balances excluded — rate unavailable`** (per **05-RESEARCH** open question / **05-UI-SPEC**).
8. **Do not** add fetches; **do not** write `data.json`.
  </action>
  <acceptance_criteria>
    - `src/pages/DashboardPage.tsx` imports `calcCategoryTotals` and `sumForNetWorth` from `@/lib/dashboardCalcs` (or correct alias)
    - `src/pages/DashboardPage.tsx` contains `onNavigate` in the props type and `useLivePrices` hook usage
    - `rg "saveData" src/pages/DashboardPage.tsx` has no matches (or only in comment — prefer none)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Net worth `Card` + row list (05-UI-SPEC)</name>
  <files>src/pages/DashboardPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/pages/BankSavingsPage.tsx
    - /Users/a.mahindrakar/personal/fin/src/components/ui/card.tsx
    - /Users/a.mahindrakar/personal/fin/src/components/ui/skeleton.tsx
    - /Users/a.mahindrakar/personal/fin/src/components/ui/separator.tsx
  </read_first>
  <action>
1. **Page title:** `h1` with `className="text-xl font-semibold"`, text `Dashboard` (match other pages).
2. **Top net worth `Card`:** `Card` with label text **`Net worth`** (or `Total net worth (INR)`), primary figure `text-2xl font-semibold` using `toLocaleString('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 })` **or** a `Skeleton` with `className="h-8 w-40"` (or per **05-UI-SPEC**) when `anyPriceLoading` applies to the running total (show Skeleton until BTC/forex that gate included amounts have settled — at minimum when `anyPriceLoading` and the total still depends on those rates).
3. **Category `Card`:** second `Card` with `CardContent className="p-0"`, list in order **`DASHBOARD_CATEGORY_ORDER`**. For each key, map to display label: Gold; Mutual Funds; Stocks; Bitcoin; Property; Bank savings (sentence case per **05-UI-SPEC**), Retirement — align strings with `AppSidebar` / **05-UI-SPEC** where they differ only by case.
4. **Rows:** `button type="button"` per row, full width, `className` includes `hover:bg-muted/50`, `w-full`, `text-left`, `px-4 py-3` per spec. `onClick` → `onNavigate('<sectionKey>')` with keys `gold` | `mutualFunds` | `stocks` | `bitcoin` | `property` | `bankSavings` | `retirement` only.
5. **Value column:** Show `Skeleton` in value column for **Bitcoin** when `btcLoading` (or when forex blocks USD/INR); for **Bank savings** row, show value Skeleton when `forexLoading` and user has **AED** accounts (AED rate pending). For unavailable `null` category totals, show `—` and optional `AlertCircle` icon (lucide, `h-3.5 w-3.5`) per spec.
6. **Percent column:** `text-sm text-muted-foreground` with `percentOfTotal(...)`; when `grandTotal === 0`, show `—` or `0%` per **05-RESEARCH** Pitfall 5 and **05-UI-SPEC**.
7. **Separator** between rows (not inside the button) — same structure as `BankSavingsPage` list.
8. **`aria-label`:** each row `Open {label} section`.
9. **Empty state (optional):** when all holdings are effectively zero / empty, show the **05-UI-SPEC** `No holdings yet` block with muted body text.
10. **`aria-live="polite"`** on a single region wrapping the total or list per **05-UI-SPEC** § Accessibility.
  </action>
  <acceptance_criteria>
    - `src/pages/DashboardPage.tsx` imports `Card` (and as needed `CardContent`, `CardHeader`, `CardTitle`, or `CardDescription` from the same module)
    - `src/pages/DashboardPage.tsx` imports `Skeleton` from `@/components/ui/skeleton` and `Separator` from `@/components/ui/separator`
    - `rg "aria-label=\"Open " src/pages/DashboardPage.tsx` has at least one match OR `aria-label={\`Open` pattern exists
    - `cd /Users/a.mahindrakar/personal/fin && npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Run: `cd /Users/a.mahindrakar/personal/fin && npx tsc --noEmit` → exit 0
- Run: `cd /Users/a.mahindrakar/personal/fin && npm run build` → exit 0
- Manual (dev server): open Dashboard; confirm total matches sum of visible non-null rows; click each of 7 rows → correct section; throttle network to see Skeleton on BTC/forex-dependent rows
</verification>

## PLANNING COMPLETE
