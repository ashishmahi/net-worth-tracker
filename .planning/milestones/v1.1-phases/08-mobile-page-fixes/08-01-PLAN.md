---
phase: 08-mobile-page-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PageHeader.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/GoldPage.tsx
  - src/pages/MutualFundsPage.tsx
  - src/pages/StocksPage.tsx
  - src/pages/BankSavingsPage.tsx
  - src/pages/PropertyPage.tsx
  - src/pages/BitcoinPage.tsx
  - src/pages/RetirementPage.tsx
autonomous: true
requirements:
  - MB-02
must_haves:
  truths:
    - "Below 768px, page headers stack (title block then full-width primary action) with no horizontal overflow; at 768px+ row layout preserved"
  artifacts:
    - path: "src/components/PageHeader.tsx"
      provides: "PageHeader, title+meta+action"
      contains: "min-[768px]|text-xl font-semibold"
  key_links:
    - from: "src/components/PageHeader.tsx"
      to: "src/hooks/use-mobile.tsx"
      via: "MOBILE_BREAKPOINT alignment in Tailwind (768px)"

---

<objective>
Deliver **MB-02** and ROADMAP Phase 8 header criteria: add a shared **`PageHeader`** and replace ad hoc `flex items-start justify-between` header rows on **Dashboard** and all **7 asset** pages. **Narrow:** column stack with **primary CTA `w-full`**; **wide (`min-[768px]:`):** row with title/meta left and action right — per **08-UI-SPEC** and `08-CONTEXT` D-01–D-03. **No** changes to Sheet internals (Plan 02).
</objective>

<threat_model>
**Scope:** Presentational layout only; no new data, links, or external fetches.

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-08-01a | CTA not reachable / clipped on small screens | Medium | Stacked full-width CTA; `min-w-0` on title column |
| T-08-01b | `h1` or live region not exposed | Low | Pass through same `h1` / `<output>` semantics as current pages |
| T-08-01c | Accessibility regression on stock buttons | Low | Preserve `aria-label` on Add / primary actions |

**Block on:** high — N/A
</threat_model>

<context>
@.planning/phases/08-mobile-page-fixes/08-CONTEXT.md
@.planning/phases/08-mobile-page-fixes/08-UI-SPEC.md
@.planning/phases/08-mobile-page-fixes/08-RESEARCH.md
@.planning/phases/08-mobile-page-fixes/08-PATTERNS.md
@.planning/ROADMAP.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: New PageHeader component</name>
  <files>src/components/PageHeader.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/.planning/phases/08-mobile-page-fixes/08-UI-SPEC.md
    - /Users/a.mahindrakar/personal/fin/src/hooks/use-mobile.tsx
  </read_first>
  <action>
1. Create `src/components/PageHeader.tsx` exporting a named or default `PageHeader` with props:
   - `title: string` (render as `h1` with `className` exactly `text-xl font-semibold`)
   - `meta?: ReactNode` (optional: totals, helper text, live `output` — used by Gold)
   - `action?: ReactNode` (primary button cluster — optional)
2. Root element `className` must use Tailwind: base **column** with `flex flex-col gap-3` (or `gap-4`); at **`min-[768px]:`**: `flex-row items-start justify-between` so title block and action sit in one row.
3. Title block: outer `div` with `className` including `min-w-0` wrapping the `h1` and `meta`.
4. When `action` is set: render it inside a `div` with `className` including **`w-full min-[768px]:w-auto shrink-0`**; when `action` is a single shadcn `Button`, the wrapper may also use **`min-[768px]:flex min-[768px]:justify-end`**.
5. Use `cn` from `@/lib/utils` to merge classes.
6. If `action` is absent: only render the title block (for Dashboard or pages without a top CTA).
  </action>
  <acceptance_criteria>
    - `test -f /Users/a.mahindrakar/personal/fin/src/components/PageHeader.tsx` exits 0
    - `grep "min-\[768px\]" /Users/a.mahindrakar/personal/fin/src/components/PageHeader.tsx` returns at least one match
    - `grep "text-xl font-semibold" /Users/a.mahindrakar/personal/fin/src/components/PageHeader.tsx` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GoldPage — use PageHeader</name>
  <files>src/pages/GoldPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/components/PageHeader.tsx
    - /Users/a.mahindrakar/personal/fin/src/pages/GoldPage.tsx
  </read_first>
  <action>
1. Import `PageHeader` from `@/components/PageHeader`.
2. Remove the `div` with `className="flex items-start justify-between"` (around the old `h1` + `Button` + total).
3. Replace with `PageHeader` where:
   - `title="Gold"`
   - `meta` = the existing fragment containing `<output …>` and the “Set gold prices in Settings” conditional paragraph (unchanged text)
   - `action` = the existing `Button` with `onClick={openAdd}`, `aria-label="Add gold item"`, and children `Plus` + "Add Item"
4. On the `Button` wrapper for narrow layout: ensure the button is **full width** when parent is full width (use `className="w-full min-[768px]:w-auto"` on the `Button` or its wrapper from `action` prop as needed to satisfy 08-UI-SPEC).
5. Do **not** change the Card list or Sheet below in this plan.
  </action>
  <acceptance_criteria>
    - `grep "PageHeader" /Users/a.mahindrakar/personal/fin/src/pages/GoldPage.tsx` returns a match
    - `grep "flex items-start justify-between" /Users/a.mahindrakar/personal/fin/src/pages/GoldPage.tsx` — **no** match (old pattern removed)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: MutualFundsPage, StocksPage, BankSavingsPage</name>
  <files>src/pages/MutualFundsPage.tsx, src/pages/StocksPage.tsx, src/pages/BankSavingsPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/components/PageHeader.tsx
    - /Users/a.mahindrakar/personal/fin/src/pages/MutualFundsPage.tsx
  </read_first>
  <action>
For each file, replace the top `div` with `flex items-start justify-between` that contains the page `h1` and Add `Button` with `PageHeader`:
- **Mutual Funds:** `title="Mutual Funds"`, `action` = existing Add control (preserve `aria-label` and label text "Add…" as today).
- **Stocks:** `title="Stocks"`, `action` = same pattern.
- **Bank Savings:** `title` per existing `h1` string, `action` = Add control.
Use full-width CTA on narrow: `className` on `Button` **`w-full min-[768px]:w-auto`** where needed.
  </action>
  <acceptance_criteria>
    - `grep "PageHeader" /Users/a.mahindrakar/personal/fin/src/pages/MutualFundsPage.tsx` and same for `StocksPage` and `BankSavingsPage` each return a match
    - `grep "flex items-start justify-between" /Users/a.mahindrakar/personal/fin/src/pages/MutualFundsPage.tsx` — no match (repeat mental check for other two)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: PropertyPage — header only</name>
  <files>src/pages/PropertyPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/components/PageHeader.tsx
    - /Users/a.mahindrakar/personal/fin/src/pages/PropertyPage.tsx
  </read_first>
  <action>
1. Replace the `div` with `flex items-start justify-between` containing "Property" `h1` and "Add property" `Button` with `PageHeader` with `title="Property"` and `action` = the existing Add `Button` (with `w-full min-[768px]:w-auto` on the button).
2. **Do not** change `Sheet` / `Table` markup in this plan.
  </action>
  <acceptance_criteria>
    - `grep "PageHeader" /Users/a.mahindrakar/personal/fin/src/pages/PropertyPage.tsx` returns a match
    - `grep "flex items-start justify-between" /Users/a.mahindrakar/personal/fin/src/pages/PropertyPage.tsx` — no match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Dashboard, Bitcoin, Retirement (title / layout consistency)</name>
  <files>src/pages/DashboardPage.tsx, src/pages/BitcoinPage.tsx, src/pages/RetirementPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/components/PageHeader.tsx
    - /Users/a.mahindrakar/personal/fin/src/pages/DashboardPage.tsx
  </read_first>
  <action>
1. **Dashboard:** wrap the top “Dashboard” `h1` in `PageHeader` with `title="Dashboard"` and no `action` (or no second column).
2. **Bitcoin:** replace the standalone `h1` with `PageHeader title="Bitcoin"`.
3. **Retirement:** replace the page title `h1` with `PageHeader` and the same title string as today.
4. Preserve all content below the header (cards, forms) unchanged.
  </action>
  <acceptance_criteria>
    - `grep "PageHeader" /Users/a.mahindrakar/personal/fin/src/pages/DashboardPage.tsx` returns a match
    - `grep "PageHeader" /Users/a.mahindrakar/personal/fin/src/pages/BitcoinPage.tsx` returns a match
    - `grep "PageHeader" /Users/a.mahindrakar/personal/fin/src/pages/RetirementPage.tsx` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Build</name>
  <files>package.json</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/package.json
  </read_first>
  <action>
Run from repo root: `npm run build` — must exit 0.
  </action>
  <acceptance_criteria>
    - `cd /Users/a.mahindrakar/personal/fin && npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- Visual smoke at 375px (DevTools): at least **Gold** and **Property** show **stacked** title + full-width add button with **no** horizontal page overflow for the **header** row
</verification>
