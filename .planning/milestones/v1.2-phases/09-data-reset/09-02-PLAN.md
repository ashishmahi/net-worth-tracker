---
phase: 09-data-reset
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/pages/SettingsPage.tsx
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
must_haves:
  truths:
    - "Settings has a non-Save danger block with Clear all + AlertDialog; success clears UI state; failed save shows inline error and no silent half-clear"
  artifacts:
    - path: "src/pages/SettingsPage.tsx"
      provides: "Danger zone UI, clear flow, form re-sync on empty settings"
  key_links:
    - from: "src/pages/SettingsPage.tsx"
      to: "src/context/AppDataContext.tsx"
      via: "saveData(createInitialData())"
---

<objective>
**Wave 2 (Settings UX):** Add the **danger zone** block, **AlertDialog** with copy per **09-UI-SPEC**, wire **`saveData(createInitialData())`**, **inline** success/error, and **fix** Gold/Retirement **RHF** hydration so when `goldPrices` / `retirement` are **removed** (after reset) the forms show **empty** fields. Fulfills **DATA-01, DATA-02, DATA-03** and D-01–D-13 in `09-CONTEXT.md`. **Preserves** `handleExport` and **Export Data** (D-18) unchanged in behavior.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-09-02a | User confirms without reading | Medium | Long description + Cancel default; destructive only on second button in dialog |
| T-09-02b | Optimistic state shows cleared UI while file save failed | — | `saveData` reverts; **must** catch error and set **inline** error; user must see failure message |
| T-09-02c | Stale form fields after server-shaped settings cleared | High | `useEffect` `else` branch reset when optional settings absent |

**Block on:** high — T-09-02c
</threat_model>

<context>
@.planning/milestones/v1.2-phases/09-data-reset/09-CONTEXT.md
@.planning/milestones/v1.2-phases/09-data-reset/09-UI-SPEC.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hydration when goldPrices / retirement absent</name>
  <files>src/pages/SettingsPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/pages/SettingsPage.tsx
  </read_first>
  <action>
1. In the `useEffect` for `data.settings.goldPrices`, add an **`else`** branch: `goldForm.reset({ k24: '', k22: '', k18: '' })` when `gp` is **undefined** / missing.
2. In the `useEffect` for `data.settings.retirement`, add an **`else`** branch: `retirementForm.reset({ currentAge: '', targetAge: '', npsReturnPct: '', epfRatePct: '' })` when `ra` is **undefined** / missing.
3. Keep `eslint-disable` for exhaustive-deps as today.
  </action>
  <acceptance_criteria>
    - `grep -A3 "data.settings.goldPrices" /Users/a.mahindrakar/personal/fin/src/pages/SettingsPage.tsx` shows an `else` with `reset` to empty strings
    - `grep -A3 "data.settings.retirement" /Users/a.mahindrakar/personal/fin/src/pages/SettingsPage.tsx` shows an `else` with `reset` to empty strings
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Danger zone + AlertDialog + reset</name>
  <files>src/pages/SettingsPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/pages/SettingsPage.tsx
    - /Users/a.mahindrakar/personal/fin/src/components/ui/alert-dialog.tsx
    - /Users/a.mahindrakar/personal/fin/.planning/milestones/v1.2-phases/09-data-reset/09-UI-SPEC.md
  </read_first>
  <action>
1. `import` `createInitialData` from `@/context/AppDataContext` and AlertDialog parts from `@/components/ui/alert-dialog`, `cn` if needed, `Button` with **`buttonVariants`** from `@/components/ui/button` for destructive **confirm** in the dialog.
2. After the **Data** / **Export Data** `div` and **before** the closing `</div>` of the page, add a **new** `Card` (or `div` with border) for **Danger zone** with:
   - `h2` or `p` with `text-sm font-semibold` and title **Danger zone** (or per CONTEXT).
   - Muted/short line that this **permanently removes** all net-worth / asset data from the **local** file.
   - **Controlled** `AlertDialog` with state `open`, `onOpenChange`.
   - `AlertDialogTrigger` asChild wrapping **only** a **destructive** `Button` **"Clear all data"** (or per CONTEXT D-02).
3. `AlertDialogContent` must include `AlertDialogHeader` + `AlertDialogTitle` + `AlertDialogDescription` with: irreversible; all wealth rows in `data.json` removed; use **Export Data** above for a **backup** first.
4. `AlertDialogFooter`: `AlertDialogCancel` **"Cancel"**; then a **`Button`** (not unlabeled `AlertDialogAction` for default styling only) with **`className={cn(buttonVariants({ variant: 'destructive' }))}`** or equivalent, label **"Yes, clear all data"**, `disabled` when a local **`resetClearing`** is true, **`onClick`** `async` that calls `setResetError(null)` / clears success, then `try { await saveData(createInitialData()); setDialogOpen(false); set resetSuccess } catch { setResetError('...') }` with message matching CONTEXT D-11 tone.
5. **Inline** `role="alert"` in the danger `Card` for `resetError` and optional **success** line for D-12.
6. **Do not** modify `handleExport` or the **Export Data** `Button` implementation.
7. **Do not** `clear localStorage` for theme.
  </action>
  <acceptance_criteria>
    - `grep "createInitialData" /Users/a.mahindrakar/personal/fin/src/pages/SettingsPage.tsx` returns a match
    - `grep "AlertDialog" /Users/a.mahindrakar/personal/fin/src/pages/SettingsPage.tsx` returns a match
    - `grep "Clear all data" /Users/a.mahindrakar/personal/fin/src/pages/SettingsPage.tsx` returns a match
    - `grep "Yes, clear all data" /Users/a.mahindrakar/personal/fin/src/pages/SettingsPage.tsx` returns a match
  </acceptance_criteria>
</task>

</tasks>
