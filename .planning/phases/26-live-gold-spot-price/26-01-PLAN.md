---
phase: 26-live-gold-spot-price
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/priceApi.ts
  - src/lib/goldLiveHints.ts
  - src/lib/__tests__/goldLiveHints.test.ts
  - src/lib/__tests__/priceApi.gold.test.ts
  - src/context/LivePricesContext.tsx
  - src/pages/SettingsPage.tsx
  - src/pages/GoldPage.tsx
autonomous: true
requirements:
  - SPOT-01
  - SPOT-02
  - SPOT-03
  - UX-01
  - UX-02
  - UX-03
  - CALC-01
  - TEST-01
must_haves:
  truths:
    - "fetchGoldUsdPerOz reads https://api.gold-api.com/price/XAU and validates json.price"
    - "LivePricesContext exposes goldUsdPerOz goldLoading goldError and refetch runs gold"
    - "Settings Gold Prices shows read-only ₹/g hints per karat when spot and usdInr exist"
    - "sumGoldInr / persisted goldPrices unchanged unless user saves manually"
  artifacts:
    - path: "src/lib/goldLiveHints.ts"
      provides: "liveInrPerGramPure + per-karat helpers using roundCurrency"
    - path: "src/context/LivePricesContext.tsx"
      provides: "gold channel parity with silver"
  key_links:
    - from: "src/pages/SettingsPage.tsx"
      to: "src/context/LivePricesContext.tsx"
      via: "useLivePrices()"
      pattern: "goldUsdPerOz usdInr goldLiveHints"
---

<objective>
Implement Phase 26: live **gold** spot (**XAU**) via **gold-api.com**, **`LivePricesContext`** parity with **silver**, **Settings** Gold Prices **₹/g** hints + **Live market rates** rows for **XAU** and **XAG**, compact **Gold** page hint line, and **Vitest** coverage for API + math — satisfying **SPOT-01–03**, **UX-01–03**, **CALC-01**, **TEST-01** and **`26-CONTEXT.md`** / **`26-UI-SPEC.md`**.
</objective>

<threat_model>
**Assets:** User sees third-party **spot prices**; no secrets in client for metals APIs.

| Threat | Mitigation |
|--------|------------|
| **T1** Tampered or malicious API response | **HTTPS** only; **strict JSON parse** — reject non-finite / non-positive **`price`** (same as silver). |
| **T2** XSS via API strings | Use **numeric** `price` only; do not **`dangerouslySetInnerHTML`** API payload. |
| **T3** Misleading net worth | Hints are **read-only**; **`settings.goldPrices`** remains save path for valuations (**UX-03**). |

**Block severity:** High — none that bypass mitigations above without changing scope.

</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-live-gold-spot-price/26-CONTEXT.md
@.planning/phases/26-live-gold-spot-price/26-RESEARCH.md
@.planning/phases/26-live-gold-spot-price/26-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: priceApi fetchGoldUsdPerOz + GOLD_TTL_MS + goldLiveHints + tests</name>
  <files>src/lib/priceApi.ts, src/lib/goldLiveHints.ts, src/lib/__tests__/priceApi.gold.test.ts, src/lib/__tests__/goldLiveHints.test.ts</files>
  <read_first>
    - src/lib/priceApi.ts
    - src/lib/financials.ts
    - src/pages/CommoditiesPage.tsx
    - .planning/phases/26-live-gold-spot-price/26-CONTEXT.md
  </read_first>
  <action>
    1. **`src/lib/priceApi.ts`**
       - Add constant: **`const GOLD_API_GOLD_URL = 'https://api.gold-api.com/price/XAU'`**
       - Export **`export const GOLD_TTL_MS = SILVER_TTL_MS`** (or **`FOREX_TTL_MS`** — must equal **`SILVER_TTL_MS`** per CONTEXT).
       - Add **`export async function fetchGoldUsdPerOz()`** returning **`Promise<number>`** — mirror **`fetchSilverUsdPerOz`**: `fetch(GOLD_API_GOLD_URL)`, check **`res.ok`**, parse **`json.price`** as finite positive number, else throw **`Error`** with message **`gold-api.com gold failed: HTTP ${status}`** or **`gold price: unexpected response shape`** / **`non-positive or non-numeric value`** matching silver tone.

    2. **New `src/lib/goldLiveHints.ts`**
       - Import **`roundCurrency`**, **`TROY_OZ_TO_GRAMS`** from **`priceApi`** (or **`financials`** + **`priceApi`** as appropriate).
       - Export **`liveInrPerGramPure(goldUsdPerOz: number, usdInr: number): number`**  
         **`roundCurrency((goldUsdPerOz / TROY_OZ_TO_GRAMS) * usdInr)`**
       - Export **`liveInrPerGramK22`** / **`liveInrPerGramK18`** **or** single **`liveInrPerGramForKarat(goldUsdPerOz, usdInr, karat: 24 | 22 | 18)`** returning **`roundCurrency(pure * (karat/24))`** for 22 and 18.

    3. **Tests**
       - **`src/lib/__tests__/priceApi.gold.test.ts`** — **`vi.stubGlobal('fetch', …)`** mock **200** JSON **`{ price: 4600 }`** → resolves **4600**; **404** / bad shape → throws (copy style from silver if no existing test — use **Vitest** **`vi`**).
       - **`src/lib/__tests__/goldLiveHints.test.ts`** — fixed inputs: e.g. **`goldUsdPerOz=3103.5`**, **`usdInr=83`** → assert pure gram INR and **22K/18K** ratios match **`roundCurrency`** expectations.

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run src/lib/__tests__/goldLiveHints.test.ts src/lib/__tests__/priceApi.gold.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "fetchGoldUsdPerOz" src/lib/priceApi.ts` exits 0
    - `grep -q "GOLD_TTL_MS" src/lib/priceApi.ts` exits 0
    - `grep -q "GOLD_API_GOLD_URL\\|api.gold-api.com/price/XAU" src/lib/priceApi.ts` exits 0
    - `test -f src/lib/goldLiveHints.ts` exits 0
    - `npm test -- --run src/lib/__tests__/goldLiveHints.test.ts src/lib/__tests__/priceApi.gold.test.ts` exits 0
  </acceptance_criteria>
  <done>Gold fetch + karat math helpers tested</done>
</task>

<task type="auto">
  <name>Task 2: LivePricesContext — gold channel</name>
  <files>src/context/LivePricesContext.tsx</files>
  <read_first>
    - src/context/LivePricesContext.tsx
    - src/lib/priceApi.ts
  </read_first>
  <action>
    1. Import **`fetchGoldUsdPerOz`**, **`GOLD_TTL_MS`** from **`../lib/priceApi`**.

    2. Extend **`LivePricesContextValue`** with:
       - **`goldUsdPerOz: number | null`**
       - **`goldLoading: boolean`**
       - **`goldError: string | null`**

    3. Add state/refs mirroring silver: **`liveGold`**, **`lastGoldAt`**, **`liveGoldRef`**, **`goldLoading`**, **`goldError`**.

    4. Implement **`runGoldFetch(force: boolean)`** — same stale logic as **`runSilverFetch`** but **`GOLD_TTL_MS`** and **`fetchGoldUsdPerOz`**; error message **`Gold price fetch failed`** or **`e.message`** like silver.

    5. Add **`runGoldFetch`** to **`refetch`**, **`refetchStale`**, and the **mount** **`useEffect`** alongside BTC/forex/silver.

    6. Include **`goldUsdPerOz`**, **`goldLoading`**, **`goldError`** in **`useMemo`** **`value`** deps.

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx tsc -b --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "goldUsdPerOz" src/context/LivePricesContext.tsx` exits 0
    - `grep -q "runGoldFetch" src/context/LivePricesContext.tsx` exits 0
    - `grep -q "fetchGoldUsdPerOz" src/context/LivePricesContext.tsx` exits 0
    - `grep -q "void runGoldFetch" src/context/LivePricesContext.tsx` exits 0
  </acceptance_criteria>
  <done>Context exposes gold spot with silver-parity refresh</done>
</task>

<task type="auto">
  <name>Task 3: SettingsPage — Gold hints + Live rates XAU/XAG</name>
  <files>src/pages/SettingsPage.tsx</files>
  <read_first>
    - src/pages/SettingsPage.tsx
    - src/lib/goldLiveHints.ts
    - .planning/phases/26-live-gold-spot-price/26-UI-SPEC.md
  </read_first>
  <action>
    1. **`useLivePrices()`** destructure **`goldUsdPerOz`**, **`goldLoading`**, **`goldError`**, **`silverUsdPerOz`**, **`silverLoading`**, **`silverError`** (silver may already be partially used — ensure all needed).

    2. **`useMemo`** derived **`k24Hint`**, **`k22Hint`**, **`k18Hint`** (`number | null`) from **`goldUsdPerOz`**, **`usdInr`** via **`goldLiveHints`** — null if either input missing.

    3. **Gold Prices card:** Under each **`Input`** (k24, k22, k18), add muted hint **`&lt;p&gt;`** or **`&lt;div&gt;`** per **26-UI-SPEC** — show **`Loader2`** when loading blocks display; show **`goldError`** with **`role="alert"`** if set; else formatted **`en-IN`** ₹/g.

    4. **Live market rates `&lt;dl&gt;`:** Insert two **`&lt;div className="flex justify-between..."&gt;`** rows: **Gold (XAU)**, **Silver (XAG)** — copy **`BTC/USD`** row structure ( **`Loader2`**, **`tabular-nums`**, **`—`** ).

    5. **Error alert:** Extend **`{(btcError || forexError) &&`** to include **`goldError || silverError`** (one **`&lt;p role="alert"&gt;`** or append messages).

    6. Update intro **`text-muted-foreground`** sentence to mention **gold/silver** spot + existing Bitcoin/AED usage.

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "goldUsdPerOz" src/pages/SettingsPage.tsx` exits 0
    - `grep -q "Silver (XAG)\\|XAG" src/pages/SettingsPage.tsx` exits 0
    - `grep -q "Gold (XAU)\\|XAU" src/pages/SettingsPage.tsx` exits 0
    - `grep -q "goldLiveHints\\|liveInrPerGram" src/pages/SettingsPage.tsx` exits 0
  </acceptance_criteria>
  <done>Settings shows live metal USD/oz and per-karat ₹ hints</done>
</task>

<task type="auto">
  <name>Task 4: GoldPage hint line + full verification</name>
  <files>src/pages/GoldPage.tsx</files>
  <read_first>
    - src/pages/GoldPage.tsx
    - src/lib/goldLiveHints.ts
    - .planning/phases/26-live-gold-spot-price/26-UI-SPEC.md
  </read_first>
  <action>
    1. Import **`useLivePrices`** from **`@/context/LivePricesContext`**.

    2. When **`goldTotal === null`** (same condition as existing **Set gold prices in Settings**) **and** **`goldUsdPerOz`** + **`usdInr`** available, render **one** compact **`text-sm text-muted-foreground`** line with **Live spot hints (₹/g)** for **24K / 22K / 18K** using **`goldLiveHints`** (abbreviated numbers per **26-UI-SPEC**).

    3. If live data incomplete, either omit the line or show neutral **`—`** — do not remove existing CTA.

    4. Run **`npm test`** and **`npm run build`** from repo root; fix any type or lint errors introduced.

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "useLivePrices" src/pages/GoldPage.tsx` exits 0
    - `npm test -- --run` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Gold page shows optional live hint row; suite and build pass</done>
</task>

</tasks>
