---
phase: 03-live-prices-bitcoin
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/types/data.ts
  - src/context/AppDataContext.tsx
  - src/pages/BankSavingsPage.tsx
autonomous: true
requirements:
  - D-11
  - D-12

must_haves:
  truths:
    - "Each bank account has currency INR|AED and native balance; legacy balanceInr migrates to INR+balance on load"
    - "AED accounts contribute to section total in INR using effective aedInr from useLivePrices; rounding per CLAUDE.md"
    - "Sheet form matches Phase 2 pattern with added currency control (default INR for new rows)"
    - "npx tsc --noEmit exits 0"
  artifacts:
    - path: "src/types/data.ts"
      provides: "BankAccountSchema with currency + native balance"
      contains: "currency"
    - path: "src/context/AppDataContext.tsx"
      provides: "Pre-parse migration of legacy bank accounts"
      contains: "balanceInr|migrate|bankSavings"
    - path: "src/pages/BankSavingsPage.tsx"
      provides: "AED/INR list + sheet"
      contains: "AED|useLivePrices"
  key_links:
    - from: "src/pages/BankSavingsPage.tsx"
      to: "src/types/data.ts"
      via: "BankAccount"
      pattern: "currency"
---

<objective>
Evolve **bank savings** to **D-11 / D-12**: Zod + TS types for `currency: 'INR' | 'AED'` and **native** balance; migrate existing persisted `balanceInr` records to **INR** + same numeric balance; update **BankSavingsPage** Sheet + list to select currency, show native + optional INR equivalent for AED, and sum section **total in INR** using live (or session) `aedInr` from `useLivePrices()`.
</objective>

<threat_model>
| ID | Threat | Mitigation |
|----|--------|------------|
| T-03-06 | Data loss on migration | `safeParse` after transform; on failure show existing load error; migration is additive (preserve ids, label) |

</threat_model>

<context>
@/Users/a.mahindrakar/personal/fin/.planning/phases/03-live-prices-bitcoin/03-CONTEXT.md
@/Users/a.mahindrakar/personal/fin/.planning/phases/03-live-prices-bitcoin/03-UI-SPEC.md — Bank
@/Users/a.mahindrakar/personal/fin/src/types/data.ts
@/Users/a.mahindrakar/personal/fin/src/pages/BankSavingsPage.tsx
@/Users/a.mahindrakar/personal/fin/src/lib/financials.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema — BankAccount with currency + balance</name>
  <files>src/types/data.ts</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/types/data.ts
  </read_first>
  <action>
Replace `BankAccountSchema` so each account has `label`, `currency: z.enum(['INR','AED'])`, and `balance: z.number().nonnegative()`. Remove `balanceInr` from the *schema* (migration maps old data before parse). Update inline comments D-23. Export types continue to be inferred from Zod. Ensure `INITIAL_DATA` in `AppDataContext` uses the new shape for **new** defaults (empty accounts array unchanged; template account structure not required).
  </action>
  <acceptance_criteria>
    - `src/types/data.ts` includes the substring `currency` inside `BankAccount` definition area
    - `src/types/data.ts` does not list `balanceInr` as the sole balance field for new schema (old key may appear only in comments about migration)
    - `npx tsc --noEmit` may fail until Task 2/3 — complete dependent tasks; final state: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Load-time migration in AppDataContext</name>
  <files>src/context/AppDataContext.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/context/AppDataContext.tsx
    - /Users/a.mahindrakar/personal/fin/src/types/data.ts
  </read_first>
  <action>
Before `DataSchema.safeParse(raw)`, transform `raw` (as `unknown`) so each `assets.bankSavings.accounts[]` item that still has `balanceInr` becomes `{ ...acc, currency: 'INR', balance: <same number as balanceInr> }` and **omit** `balanceInr`. Do not change unrelated sections. If migration helper grows large, move to `src/lib/migrateBankAccounts.ts` and unit-test optional.
  </action>
  <acceptance_criteria>
    - `AppDataContext.tsx` (or new helper file) contains the substring `balanceInr` in code that maps legacy field
    - `npx tsc --noEmit` exits 0 after all phase files compile
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: BankSavingsPage — currency select + INR total</name>
  <files>src/pages/BankSavingsPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/pages/BankSavingsPage.tsx
    - /Users/a.mahindrakar/personal/fin/src/context/LivePricesContext.tsx
  </read_first>
  <action>
Update forms and display: (1) **Add** `currency` to zod form schema with default `INR` for new accounts; (2) balance field string maps to `balance` in native units; (3) for **INR** rows use `roundCurrency` on parse; (4) for **AED** rows same; (5) `sectionTotal` in INR = sum of `roundCurrency(balance)` for INR plus `roundCurrency(balance * aedInr)` for AED using **effective** `aedInr` from `useLivePrices` (handle null rate with 0 or explicit UI warning — must match 03-CONTEXT: clear error when feeds fail; reuse error display pattern); (6) list row shows native currency; (7) update empty state copy to mention INR or AED. Use shadcn **Select** if available in project, else radio group — if adding Select requires `npx shadcn add select`, document in task commit and add component per Phase 2 rules.
  </action>
  <acceptance_criteria>
    - `src/pages/BankSavingsPage.tsx` imports `useLivePrices`
    - `src/pages/BankSavingsPage.tsx` contains both `AED` and `INR` in file (currency handling)
    - `grep "fetch(" src/pages/BankSavingsPage.tsx` returns no matches
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` → 0
- Manual: Create AED account, verify total updates when aedInr is available; legacy `data.json` with `balanceInr` loads
</verification>

## PLANNING COMPLETE
