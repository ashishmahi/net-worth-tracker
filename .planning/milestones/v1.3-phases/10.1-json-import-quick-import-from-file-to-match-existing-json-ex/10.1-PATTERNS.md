# Phase 10.1 — PATTERN MAPPING COMPLETE

## PATTERN MAPPING COMPLETE

| Planned touch | Role | Closest analog | Notes |
|---------------|------|----------------|--------|
| `AppDataContext.tsx` | Central parse + load | Current `useEffect` fetch + migrate + `safeParse` | Extract shared `parseAppDataFromImport` used by effect + import |
| `SettingsPage.tsx` | Import UI + dialogs | `handleExport` + `Clear all data` `AlertDialog` + per-block `Loader2` + `role="alert"` | Second `AlertDialog` for import; Data row `flex flex-wrap gap-2` |
| (none) | New routes / plugins | N/A | IMP-01: reuse `POST` `/api/data` only |

### Code excerpt (analog: initial load)

```78:90:src/context/AppDataContext.tsx
  useEffect(() => {
    fetch('/api/data')
      .then(r => r.json())
      .then(raw => {
        const migrated = migrateLegacyBankAccounts(raw)
        const result = DataSchema.safeParse(migrated)
        if (result.success) {
          setData(result.data)
        } else {
          console.warn('data.json schema mismatch:', result.error.issues)
          setLoadError('Saved data format is unrecognized. Starting with defaults to avoid data loss.')
        }
      })
```

Import must call the **same** `migrate` + `safeParse` path via the new helper.

### Code excerpt (analog: clear-data save error)

```557:560:src/pages/SettingsPage.tsx
                    } catch {
                      setClearDataError('Could not clear data. Check that the app is running and try again.')
                    } finally {
```

Import save error uses parallel copy per `10.1-UI-SPEC` / CONTEXT D-08.

---

*End of pattern map.*
