---
phase: 17-liabilities-page-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/liabilityCalcs.ts
  - src/lib/__tests__/liabilityCalcs.test.ts
  - src/pages/LiabilitiesPage.tsx
  - src/components/AppSidebar.tsx
  - src/App.tsx
autonomous: true
requirements:
  - LIAB-01
  - LIAB-02
  - LIAB-03
  - LIAB-04
  - LIAB-05
  - LIAB-06
  - INFRA-03

must_haves:
  truths:
    - "Sidebar includes Liabilities after Property; clicking it renders LiabilitiesPage"
    - "Users can add, edit, and delete standalone loans; changes persist via saveData"
    - "Each loan shows a secondary Badge with loan type label Home/Car/Personal/Other"
    - "Empty list shows empty state; persistent banner uses CONTEXT/UI-SPEC verbatim copy"
    - "npm test and npx tsc -b --noEmit succeed"
  artifacts:
    - path: "src/pages/LiabilitiesPage.tsx"
      provides: "Full CRUD + aggregates + banner"
      contains: "For loans tied to a specific property"
    - path: "src/components/AppSidebar.tsx"
      provides: "SectionKey liabilities + nav item after property"
      contains: "liabilities"
  key_links:
    - from: "LiabilitiesPage"
      to: "data.liabilities"
      via: "saveData({ ...data, liabilities })"
      pattern: "liabilities"
---

<objective>
Deliver **Phase 17 — Liabilities Page CRUD**: standalone loan list with Sheet-based add/edit, inline delete confirmation, loan-type badges, page aggregates (outstanding + EMI), persistent disambiguation banner, and sidebar navigation — matching `17-CONTEXT.md`, `17-UI-SPEC.md`, and Gold/Bank UX patterns. Add a **pure EMI aggregate helper** in `liabilityCalcs.ts` with unit tests. Wire **`liabilities`** into `SectionKey`, `NAV_ITEMS` (after `property`), and `SECTION_COMPONENTS`.
</objective>

<threat_model>
## Trust boundaries

Local-only app; JSON persistence via existing API. No new network endpoints.

## STRIDE (ASVS L1)

| Threat ID | Category | Component | Severity | Mitigation |
|-----------|----------|-----------|----------|------------|
| T-17-01 | Tampering | POST `/api/data` | Low | Existing save path; Zod validates `DataSchema` on load |
| T-17-02 | XSS | User-entered `label`, `lender` | Low | React text nodes only; no `dangerouslySetInnerHTML` |

**Block policy:** No HIGH threats identified for this phase.
</threat_model>

<execution_context>
@.planning/phases/17-liabilities-page-crud/17-CONTEXT.md
@.planning/phases/17-liabilities-page-crud/17-UI-SPEC.md
@.planning/phases/17-liabilities-page-crud/17-RESEARCH.md
@.planning/phases/17-liabilities-page-crud/17-PATTERNS.md
@.planning/phases/17-liabilities-page-crud/17-VALIDATION.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: sumStandaloneLiabilitiesEmiInr + tests</name>
  <files>src/lib/liabilityCalcs.ts, src/lib/__tests__/liabilityCalcs.test.ts</files>
  <read_first>
    - src/lib/liabilityCalcs.ts (full file)
    - src/lib/__tests__/liabilityCalcs.test.ts (full file)
    - src/types/data.ts (LiabilityItemSchema / BaseItemSchema)
  </read_first>
  <action>
    In `liabilityCalcs.ts`, add and export a pure function **`sumStandaloneLiabilitiesEmiInr(data: AppData): number`** that sums **`emiInr`** across `data.liabilities` for items where `emiInr` is a finite non-negative number (skip undefined/missing). Use **`roundCurrency`** the same way as `sumLiabilitiesInr` (accumulator rounding pattern).

    In `liabilityCalcs.test.ts`, add at least two cases: (1) all items missing `emiInr` → `0`; (2) two items with EMI → correct rounded sum.

    Do not change existing exported function signatures’ behavior.
  </action>
  <acceptance_criteria>
    - `grep -n "sumStandaloneLiabilitiesEmiInr" src/lib/liabilityCalcs.ts` returns at least one match
    - `grep -n "sumStandaloneLiabilitiesEmiInr" src/lib/__tests__/liabilityCalcs.test.ts` returns at least one match
    - `npm test` exits 0
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: LiabilitiesPage — CRUD, aggregates, banner, empty state</name>
  <files>src/pages/LiabilitiesPage.tsx</files>
  <read_first>
    - src/pages/GoldPage.tsx
    - src/pages/BankSavingsPage.tsx (delete/save patterns)
    - .planning/phases/17-liabilities-page-crud/17-UI-SPEC.md
    - src/context/AppDataContext.tsx (useAppData)
    - src/components/PageHeader.tsx
  </read_first>
  <action>
    Create **`src/pages/LiabilitiesPage.tsx`** exporting **`LiabilitiesPage`**.

    **State:** `sheetOpen`, `editingId`, `saveError`, `saving`, `deletingId` (string | null for inline delete confirm scope).

    **Header:** `PageHeader` title **`Liabilities`**; meta shows **Total outstanding** via **`sumLiabilitiesInr(data)`** and **Total EMI** via **`sumStandaloneLiabilitiesEmiInr(data)`**, formatted with **`toLocaleString('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 })`** (match Gold aggregate style). Primary action opens add Sheet (**Plus** + label per UI-SPEC).

    **Banner (always on page, above list/empty):** Exact copy from **`17-UI-SPEC.md`**:  
    `For loans tied to a specific property (home loan, builder payment), use the Property section.`  
    Use **`text-sm text-muted-foreground`**.

    **List:** One **`Card`** per liability; **`Badge variant="secondary"`** showing mapped type: `home→Home`, `car→Car`, `personal→Personal`, `other→Other`; show **`label`** prominently; secondary line with lender / EMI when present; outstanding **`outstandingInr`** prominent (formatted INR). **Edit** opens Sheet with **`openEdit(item)`**. **Delete:** first click sets **`deletingId`** to item id and replaces actions with confirm row (**Yes** / **Cancel**); Yes filters item out, **`await saveData({ ...data, liabilities: filtered, … })`**, clears **`deletingId`**; Cancel clears **`deletingId`**.

    **Empty state:** When `data.liabilities.length === 0`, show centered prompt to add first loan (tone like Gold empty state).

    **Sheet:** Zod + RHF; fields — **`label`** (required), **`loanType`** (`z.enum(['home','car','personal','other'])` — use `<select>` or consistent control), **`outstandingInr`** string → **`parseFinancialInput` + `roundCurrency`**, optional **`lender`** string, optional **`emiInr`** string → number optional. On create: **`createId()`**, **`nowIso()`** for timestamps; on edit: preserve **`id`**, **`createdAt`**, update **`updatedAt`**. Save: **`await saveData({ ...data, liabilities: updatedArray })`** with **`assets`** and other root keys unchanged.

    **Errors:** Same save error string pattern as Gold/Bank on failure.
  </action>
  <acceptance_criteria>
    - `test -f src/pages/LiabilitiesPage.tsx` exits 0
    - `grep -F "For loans tied to a specific property (home loan, builder payment), use the Property section." src/pages/LiabilitiesPage.tsx` exits 0
    - `grep -E "sumLiabilitiesInr|sumStandaloneLiabilitiesEmiInr" src/pages/LiabilitiesPage.tsx` shows both imports from `@/lib/liabilityCalcs`
    - `grep -E "Badge variant=\"secondary\"" src/pages/LiabilitiesPage.tsx` exits 0
    - `grep -E "deletingId" src/pages/LiabilitiesPage.tsx` exits 0
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Sidebar + App section wiring (INFRA-03)</name>
  <files>src/components/AppSidebar.tsx, src/App.tsx</files>
  <read_first>
    - src/components/AppSidebar.tsx (full file)
    - src/App.tsx (full file)
    - src/pages/LiabilitiesPage.tsx (export name)
  </read_first>
  <action>
    **`AppSidebar.tsx`:** Extend **`SectionKey`** union with **`'liabilities'`**. Insert **`{ key: 'liabilities', label: 'Liabilities' }`** into **`NAV_ITEMS`** immediately **after** the **`property`** entry (before **`bankSavings`**).

    **`App.tsx`:** Import **`LiabilitiesPage`** from `@/pages/LiabilitiesPage`. Add **`liabilities: LiabilitiesPage`** to **`SECTION_COMPONENTS`** (same record that maps section keys to page components).
  </action>
  <acceptance_criteria>
    - `grep -A20 "export type SectionKey" src/components/AppSidebar.tsx | grep liabilities` exits 0
    - `grep -B2 -A2 "bankSavings" src/components/AppSidebar.tsx | grep liabilities` exits 0 (liabilities appears adjacent to property/bank order — confirm snippet shows property → liabilities → bankSavings)
    - `grep "LiabilitiesPage" src/App.tsx` exits 0
    - `grep "liabilities: LiabilitiesPage" src/App.tsx` exits 0
    - `npx tsc -b --noEmit` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
## Automated

- `npm test` — all green
- `npx tsc -b --noEmit` — no errors
- `npm run lint` — no new errors in touched files (if lint fails on pre-existing issues, only fix files this phase modified)

## Goal-backward

- [ ] LIAB-01 — Add loan with all fields; appears in list after save
- [ ] LIAB-02 — Edit each field; persists
- [ ] LIAB-03 — Delete with confirm; removed from list and data
- [ ] LIAB-04 — Badge shows correct type label per row
- [ ] LIAB-05 — Empty state when no loans
- [ ] LIAB-06 — Banner copy matches UI-SPEC and stays visible with 0 or N loans
- [ ] INFRA-03 — Liabilities in sidebar after Property; section renders `LiabilitiesPage`
</verification>

---

## PLANNING COMPLETE
