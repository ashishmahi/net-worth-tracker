---
phase: 13-commodities-product-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AppSidebar.tsx
  - src/App.tsx
  - src/pages/CommoditiesPage.tsx
autonomous: true
requirements: [COM-03, COM-06]

must_haves:
  truths:
    - "Sidebar exposes a Commodities destination that renders CommoditiesPage"
    - "User can create, edit, and delete standard silver and manual commodity lines via RHF+Zod"
    - "GoldPage.tsx is unchanged from pre–Phase 13 behavior"
    - "No computed commodity INR totals are persisted for standard items"
  artifacts:
    - path: "src/components/AppSidebar.tsx"
      provides: "SectionKey commodities + NAV_ITEMS entry after Gold"
      contains: "commodities"
    - path: "src/App.tsx"
      provides: "SECTION_COMPONENTS commodities → CommoditiesPage"
      contains: "CommoditiesPage"
    - path: "src/pages/CommoditiesPage.tsx"
      provides: "CRUD UI + empty state with two primary actions"
      contains: "otherCommodities"
  key_links:
    - from: "CommoditiesPage"
      to: "useLivePrices"
      via: "read-only silver valuation display"
      pattern: "useLivePrices"
---

<objective>
Register a **Commodities** sidebar section and implement **`CommoditiesPage`** with list + sheet CRUD for **`standard` (silver, grams)** and **`manual` (label + ₹)** items, including a **rich empty state** (two primary actions + explainer). Does **not** modify **`GoldPage.tsx`** (COM-06).

Output: `AppSidebar` + `App` wiring; new `CommoditiesPage.tsx`; `npm test` and `npx tsc -b --noEmit` green.
</objective>

<threat_model>
**Surface:** Local-only React app; data in `data.json`; no auth.

| Threat | Severity | Mitigation |
|--------|----------|------------|
| XSS via stored labels | Low | React text escaping; no `dangerouslySetInnerHTML` |
| Client-side validation bypass | Low | Same trust model as other sections; Zod on submit paths |

No HIGH findings — blocking threshold N/A for this phase.
</threat_model>

<execution_context>
@.planning/milestones/v1.4-phases/13-commodities-product-ux/13-CONTEXT.md
@.planning/milestones/v1.4-phases/13-commodities-product-ux/13-RESEARCH.md
@.planning/milestones/v1.4-phases/13-commodities-product-ux/13-PATTERNS.md
@.planning/milestones/v1.4-phases/13-commodities-product-ux/13-UI-SPEC.md
@.planning/milestones/v1.4-phases/13-commodities-product-ux/13-VALIDATION.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add commodities SectionKey and register CommoditiesPage in App</name>
  <files>src/components/AppSidebar.tsx, src/App.tsx</files>
  <read_first>
    - src/components/AppSidebar.tsx (full file — SectionKey, NAV_ITEMS order)
    - src/App.tsx (full file — SECTION_COMPONENTS pattern)
    - .planning/milestones/v1.4-phases/13-commodities-product-ux/13-CONTEXT.md (D-01)
  </read_first>
  <action>
    1. In `AppSidebar.tsx`, extend `SectionKey` with literal `'commodities'` (place logically after `'gold'`).
    2. Insert `{ key: 'commodities', label: 'Commodities' }` in `NAV_ITEMS` immediately after the Gold entry.
    3. In `App.tsx`, import `CommoditiesPage` from `@/pages/CommoditiesPage`.
    4. Add `commodities: CommoditiesPage` to `SECTION_COMPONENTS` object.
  </action>
  <acceptance_criteria>
    - `src/components/AppSidebar.tsx` contains `| 'commodities'` inside the `SectionKey` union type definition
    - `src/components/AppSidebar.tsx` contains `key: 'commodities'` in `NAV_ITEMS`
    - `src/App.tsx` contains `commodities: CommoditiesPage` inside `SECTION_COMPONENTS`
    - `src/App.tsx` contains `import { CommoditiesPage } from '@/pages/CommoditiesPage'`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Implement CommoditiesPage (list, sheets, empty state, CRUD)</name>
  <files>src/pages/CommoditiesPage.tsx</files>
  <read_first>
    - src/pages/GoldPage.tsx (sheet layout, openAdd/openEdit, delete, saveData pattern)
    - src/types/data.ts (OtherCommodityItemSchema / exported types)
    - src/context/LivePricesContext.tsx (hook exports used by DashboardPage — mirror imports)
    - src/lib/financials.ts (`createId`, `nowIso`, `parseFinancialInput`, `roundCurrency`)
    - .planning/milestones/v1.4-phases/13-commodities-product-ux/13-UI-SPEC.md
  </read_first>
  <action>
    1. Create `src/pages/CommoditiesPage.tsx` exporting `CommoditiesPage`.
    2. Use `useAppData()` for `data` / `saveData`; mutate only `data.assets.otherCommodities` (`items` array + `updatedAt` via `nowIso()`), spreading sibling asset keys like `GoldPage`.
    3. **Empty state** (when `items.length === 0`): short explainer text for silver vs manual; **two** `Button`s (e.g. “Add silver” opens add flow with `standard`; “Add manual item” opens add flow with `manual`).
    4. **List:** Card + rows; each row opens edit sheet for that item. Display `standard` rows as Silver + grams; `manual` rows as label + formatted ₹ (`toLocaleString` INR).
    5. **Add/edit sheets:** Use separate Zod schemas or discriminated handling:
       - `standard`: single field `grams` (string → `parseFinancialInput`, nonnegative). Optionally show **read-only** derived INR when rates exist: compute INR/gram exactly like `calcCategoryTotals` in `src/lib/dashboardCalcs.ts` (see local `silverInrPerGram` computation using `silverUsdPerOz`, `usdInr`, and `TROY_OZ_TO_GRAMS` from `@/lib/priceApi`). Do **not** persist derived INR on standard items.
       - `manual`: `label` (min 1 char), `valueInr` string → parse nonnegative.
    6. **Delete:** Destructive button in sheet footer when editing, same ordering pattern as `GoldPage` (error text, `aria-label`).
    7. **Accessibility:** `PageHeader` title `Commodities`; sheet titles `Add Silver` / `Edit Silver` / `Add manual item` / `Edit manual item` as appropriate.
    8. **Constraint:** No `fetch` or price-fetch helpers in this file — consume rates via `useLivePrices()` only. Importing **`TROY_OZ_TO_GRAMS`** from `@/lib/priceApi` as a constant for the INR/gram formula is allowed (same as `dashboardCalcs.ts`).
  </action>
  <acceptance_criteria>
    - File `src/pages/CommoditiesPage.tsx` exists and exports `function CommoditiesPage` or `export function CommoditiesPage`
    - `src/pages/CommoditiesPage.tsx` contains `otherCommodities` substring (save path)
    - `src/pages/CommoditiesPage.tsx` contains `useLivePrices` substring
    - `src/pages/CommoditiesPage.tsx` does not contain `fetchSilver` or `fetch(` for HTTP (grep passes)
    - `src/pages/GoldPage.tsx` is unchanged (verify `git diff src/pages/GoldPage.tsx` empty after this task)
    - `npm test` exits 0
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

## PLANNING COMPLETE
