---
phase: 35-reporting-currency-selector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ReportingCurrencySelect.tsx
  - src/components/AppTopbar.tsx
  - src/components/MobileTopBar.tsx
  - src/lib/wealthFormat.ts
  - src/pages/DashboardPage.tsx
  - src/lib/__tests__/wealthFormat.reporting.test.ts
autonomous: true
requirements:
  - RC-01
  - RC-02
  - RC-03
must_haves:
  truths:
    - "Native HTML select only (no Radix Select) for reporting currency; six options INR USD AED EUR GBP SGD with symbol+code labels per 35-CONTEXT D-04"
    - "AppTopbar: select appears after Live prices pill and before USD/INR chip; MobileTopBar: min tap height ≥44px"
    - "saveData updates settings.reportingCurrency and settings.updatedAt (nowIso); persistence via existing localStorage path"
    - "Dashboard monetary displays derive from calcCategoryTotals INR sums converted with toReportingCurrency(..., 'INR', reportingCurrency, ForexRateSnapshot from useLivePrices)"
    - "When conversion returns rate_unavailable, show Rate unavailable hint per UI-SPEC; do not throw"
  artifacts:
    - path: "src/components/ReportingCurrencySelect.tsx"
      provides: "Controlled reporting currency select + optional wrapper className"
    - path: "src/lib/wealthFormat.ts"
      provides: "Reporting-currency-aware compact formatting (INR keeps Cr/L; others use Intl)"
  key_links:
    - from: "src/pages/DashboardPage.tsx"
      to: ".planning/phases/35-reporting-currency-selector/35-CONTEXT.md"
      via: "D-02 dashboard-only RC-02 scope"
      pattern: "Convert display amounts only; leave NetWorthOverTimeCard INR unless trivial"
---

<objective>
Deliver **Phase 35** (**RC-01–RC-03**): **native `<select>`** for **reporting currency** in **`AppTopbar`** and **`MobileTopBar`**, wired to **`settings.reportingCurrency`** with **immediate Dashboard recalculation** (net worth, summaries, breakdown rows, allocation ring inputs) using **`toReportingCurrency`** and **`LivePricesContext`** rates, plus **persistence** on reload. Follow **[35-CONTEXT.md](35-CONTEXT.md)**, **[35-UI-SPEC.md](35-UI-SPEC.md)**, **[35-RESEARCH.md](35-RESEARCH.md)**.
</objective>

<threat_model>
**Assets:** Wealth totals shown in browser; localStorage settings integrity.

| Threat | Mitigation |
|--------|------------|
| **T-35-01** | XSS via crafted currency string | **`CurrencyCode`** only from **`CURRENCY_CODES`** constant — **no** user-typed currency |
| **T-35-02** | Misleading totals when FX missing | **`toReportingCurrency`** **`ok: false`** → **Rate unavailable** UI; never show silent wrong numbers |

**Block severity:** Low (local-only app); **T-35-02** is **medium** for correctness — preserve **`rate_unavailable`** paths.

</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/35-reporting-currency-selector/35-CONTEXT.md
@.planning/phases/35-reporting-currency-selector/35-RESEARCH.md
@.planning/phases/35-reporting-currency-selector/35-PATTERNS.md
@.planning/phases/35-reporting-currency-selector/35-UI-SPEC.md
@docs/multi-currency.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>35-01-01 — ReportingCurrencySelect + top bars</name>
  <files>src/components/ReportingCurrencySelect.tsx, src/components/AppTopbar.tsx, src/components/MobileTopBar.tsx</files>
  <read_first>
    - src/components/AppTopbar.tsx
    - src/components/MobileTopBar.tsx
    - src/types/currency.ts
    - src/context/AppDataContext.tsx
    - src/lib/financials.ts
    - .planning/phases/35-reporting-currency-selector/35-CONTEXT.md
    - .planning/phases/35-reporting-currency-selector/35-UI-SPEC.md
  </read_first>
  <action>
    1. Add **`src/components/ReportingCurrencySelect.tsx`** exporting a component that takes **`value: CurrencyCode`**, **`onChange: (code: CurrencyCode) => void`**, optional **`className`**, **`disabled?: boolean`**. Render **one** native **`<select>`** with **`aria-label="Reporting currency"`** (or associate a visible **`<label className="sr-only">`**). Options: iterate **`CURRENCY_CODES`** with labels exactly:
       - **`₹ INR`**, **`$ USD`**, **`AED`**, **`€ EUR`**, **`£ GBP`**, **`S$ SGD`** (match **35-UI-SPEC** / **D-04** — adjust symbol for SGD if spec uses different text).
    2. **`AppTopbar.tsx`**: **`useAppData()`**; insert **`<ReportingCurrencySelect`** **after** the “Live prices” **`<span>`** block (lines 56–65) **before** the USD/INR chip; **`value={data.settings.reportingCurrency ?? 'INR'}`**; **`onChange`** → **`void saveData({ ...data, settings: { ...data.settings, reportingCurrency: code, updatedAt: nowIso() } })`** with **`nowIso`** from **`@/lib/financials`**.
    3. **`MobileTopBar.tsx`**: same **`useAppData`** wiring; place select in the header row (e.g. **between** the center flex and theme button, or **flex-1** row that wraps) so **`min-h-[44px]`** is preserved on the **`<select>`** via **`className`** (e.g. **`min-h-[44px] max-w-[…]`**).
    4. Style select with Tailwind consistent with topbar (**`text-xs`/`text-sm`**, **`border-border`**, **`bg-card`**, **`rounded-md`**) — **no** Radix Select.
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx tsc -b --pretty false && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/ReportingCurrencySelect.tsx` exits 0
    - `grep -q 'ReportingCurrencySelect' src/components/AppTopbar.tsx` exits 0
    - `grep -q 'ReportingCurrencySelect' src/components/MobileTopBar.tsx` exits 0
    - `grep -q 'Live prices' src/components/AppTopbar.tsx` exits 0 && `grep -q 'USD/INR' src/components/AppTopbar.tsx` exits 0 — verify in file that **ReportingCurrencySelect** JSX appears **between** these two clusters (order: Live prices → select → USD/INR): `awk '/Live prices/,/USD\\/INR/' src/components/AppTopbar.tsx | grep -q ReportingCurrencySelect`
    - `grep -q "aria-label=\"Reporting currency\"" src/components/ReportingCurrencySelect.tsx` exits 0 OR `grep -q 'Reporting currency' src/components/ReportingCurrencySelect.tsx` with sr-only label
    - `grep -q 'reportingCurrency' src/components/AppTopbar.tsx` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>Top bars expose native reporting currency select wired to saveData (RC-01 partial, RC-03 partial).</done>
</task>

<task type="auto">
  <name>35-01-02 — Reporting display formatters</name>
  <files>src/lib/wealthFormat.ts, src/lib/__tests__/wealthFormat.reporting.test.ts</files>
  <read_first>
    - src/lib/wealthFormat.ts
    - src/types/currency.ts
    - .planning/phases/35-reporting-currency-selector/35-UI-SPEC.md
  </read_first>
  <action>
    1. Extend **`src/lib/wealthFormat.ts`** with pure helpers, e.g.:
       - **`fmtCompactForReporting(amount: number, code: CurrencyCode): string`** — if **`code === 'INR'`**, delegate to **`fmtCompactInr(amount)`**; else use **`Intl.NumberFormat('en-IN', { style: 'currency', currency: code, notation: amount >= 1e6 ? 'compact' : 'standard', maximumFractionDigits: ... })`** or fixed decimals per UX — **document** chosen rules in a **one-line comment**.
       - **`splitReportingHero(amount: number, code: CurrencyCode): { symbol: string; amount: string }`** — for hero typography: INR keeps **`splitInrAmount`** behavior; other currencies return symbol + formatted body from **`Intl`** (grep-verifiable structure).
    2. Add **`src/lib/__tests__/wealthFormat.reporting.test.ts`** with **≥2** cases: **`fmtCompactForReporting(100000, 'INR')`** contains **`₹`** or **`Cr`**/**`L`** per existing INR rules; **`fmtCompactForReporting(1000, 'USD')`** produces string containing **`$`** or **`USD`** per Intl output.
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run src/lib/__tests__/wealthFormat.reporting.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'fmtCompactForReporting' src/lib/wealthFormat.ts` exits 0
    - `grep -q 'splitReportingHero' src/lib/wealthFormat.ts` exits 0
    - `test -f src/lib/__tests__/wealthFormat.reporting.test.ts` exits 0
    - `npm test -- --run src/lib/__tests__/wealthFormat.reporting.test.ts` exits 0
  </acceptance_criteria>
  <done>Pure formatters for dashboard reporting display with Vitest coverage.</done>
</task>

<task type="auto">
  <name>35-01-03 — DashboardPage reporting conversion</name>
  <files>src/pages/DashboardPage.tsx</files>
  <read_first>
    - src/pages/DashboardPage.tsx
    - src/lib/dashboardCalcs.ts
    - src/lib/currencyConversion.ts
    - src/context/LivePricesContext.tsx
    - .planning/phases/35-reporting-currency-selector/35-CONTEXT.md
  </read_first>
  <action>
    1. **`const reportingCurrency = data.settings.reportingCurrency ?? 'INR'`**.
    2. Build **`ForexRateSnapshot`** **`useMemo`** from **`useLivePrices()`**: **`{ usdInr, aedInr, eurInr, gbpInr, sgdInr }`** (already on context from Phase 34).
    3. Add **`useMemo`** helper **`convertInrDisplay(amountInr: number): { ok: true; amount: number } | { ok: false }`** using **`toReportingCurrency(amountInr, 'INR', reportingCurrency, snapshot)`** — import from **`@/lib/currencyConversion`**.
    4. Replace **`splitInrAmount(netWorth)`** with **`splitReportingHero`** when **`ok`**; when **`rate_unavailable`**, show primary net worth region with **“Rate unavailable”** text (and optional INR fallback line — match **35-UI-SPEC** “FX degradation”).
    5. Replace **`fmtCompactInr`** on **gross assets**, **total debt** summary, **delta vs snapshot** absolute value, breakdown row **`fmtInr0(v)`**, and **Total Debt** row **`fmtInr0(totalDebtAll)`** with reporting-aware formatting: map **`v`** / totals through **`convertInrDisplay`** then **`fmtCompactForReporting`** / compact variant for smaller rows (use same formatter family as hero for consistency).
    6. **`ringSlices`**: use **converted** positive amounts when **`reportingCurrency !== 'INR'`** and conversion **ok** for each category; if a category value is **`null`** from **`calcCategoryTotals`**, keep existing skeleton/em dash behavior. If conversion fails for reporting currency, treat ring inputs as **0** or hide slice — **prefer**: show ring only when global conversion valid; if **`reportingCurrency === 'INR'`**, unchanged.
    7. **`percentOfTotal` / `pct`**: still computed from **INR** **`totals`** and **`grossAssets`** (ratios invariant under linear scale); **no change** to **`calcCategoryTotals`**.
    8. **Leave** **`NetWorthOverTimeCard`** and snapshot **recording** logic using **INR** internals (**`totalInr`** field); optionally add **`text-xs`** muted note under chart: **“History shown in INR”** — only if copy fits **RC-02** scope (**35-CONTEXT**: dashboard headline/breakdown primary).
    9. **`deps`**: add **`reportingCurrency`**, forex snapshot fields, to all **`useMemo`** blocks that consume converted values.
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx tsc -b --pretty false && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'toReportingCurrency' src/pages/DashboardPage.tsx` exits 0
    - `grep -q 'fmtCompactForReporting' src/pages/DashboardPage.tsx` exits 0 OR `grep -q 'splitReportingHero' src/pages/DashboardPage.tsx` exits 0
    - `grep -q 'reportingCurrency' src/pages/DashboardPage.tsx` exits 0
    - `grep -q 'ForexRateSnapshot' src/pages/DashboardPage.tsx` exits 0 OR snapshot built inline with **`usdInr`** and **`eurInr`** fields typed from conversion import usage
    - `grep -q 'Rate unavailable' src/pages/DashboardPage.tsx` exits 0
    - `npm test -- --run` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>Dashboard reflects reporting currency in real time without navigation (RC-02).</done>
</task>

</tasks>

<verification>
<criteria>
- `npm test -- --run` exits 0
- `npx tsc -b --pretty false` exits 0
- Manual: select USD → Dashboard figures rescale; reload → USD still selected (**RC-03**)
</criteria>
</verification>
