---
phase: 06-dark-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/context/ThemeContext.tsx
  - src/main.tsx
autonomous: true
requirements:
  - DM-02
must_haves:
  truths:
    - "localStorage key theme with values light|dark; invalid/missing yields light; inline script and React use the same key and behavior"
    - "First paint respects dark without React; html.classList.dark matches stored dark after reload"
  artifacts:
    - path: "index.html"
      provides: "Inline try/catch script before module; sets class dark on html when theme===dark"
      contains: "localStorage.getItem('theme')"
    - path: "src/context/ThemeContext.tsx"
      provides: "ThemeProvider, useTheme, sync classList + setItem on change"
      contains: "createContext|ThemeProvider"
  key_links:
    - from: "src/main.tsx"
      to: "src/context/ThemeContext.tsx"
      via: "import { ThemeProvider } from '@/context/ThemeContext'"

---

<objective>
Add FOUC-preventing inline `index.html` script and a **`ThemeContext`** (provider + `useTheme`) that reads the **initial** `dark` class from `<html>`, syncs `localStorage` key **`theme`** with values **`light`** | **`dark`**, and never strips `dark` on first mount. Satisfies **DM-02** and **06-CONTEXT** D-01, D-04, D-05, D-06. Wrap the app in `main.tsx` with `ThemeProvider`.
</objective>

<threat_model>
**Scope:** Client-only; theme string in `localStorage`; no new network or `data.json`.

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-06-01 | `localStorage` throws (Safari private, blocked storage) | Low | try/catch in `index.html` script (silent no-op); provider reads storage with try/catch, default light |
| T-06-02 | Theme value XSS via `localStorage` (another origin script) | Low (local Vite app) | Only two string branches accepted; `setItem` only with literal `light` or `dark` from app code, never from URL |

**Block on:** high — N/A; no `eval` or `innerHTML` in theme code.
</threat_model>

<context>
@.planning/phases/06-dark-mode/06-CONTEXT.md
@.planning/phases/06-dark-mode/06-RESEARCH.md
@.planning/phases/06-dark-mode/06-UI-SPEC.md
@.planning/phases/06-dark-mode/06-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline script in index.html</name>
  <files>index.html</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/index.html
    - /Users/a.mahindrakar/personal/fin/.planning/phases/06-dark-mode/06-CONTEXT.md
  </read_first>
  <action>
1. In `index.html`, insert a **non-module** `&lt;script&gt;` **immediately before** the line `&lt;script type="module" src="/src/main.tsx"&gt;&lt;/script&gt;` (must run before module load).
2. Script body: wrap in `try { ... } catch (e) { /* ignore */ }`.
3. Inside try: if `localStorage.getItem('theme') === 'dark'`, then `document.documentElement.classList.add('dark')`.
4. Do **not** set `classList` for `light` explicitly (removing `dark` is the default; absence of `dark` means light for Tailwind).
5. No other side effects, no `document.write`.
  </action>
  <acceptance_criteria>
    - `grep -n "localStorage.getItem('theme')" /Users/a.mahindrakar/personal/fin/index.html` returns a match
    - `grep -n "classList.add('dark')" /Users/a.mahindrakar/personal/fin/index.html` returns a match
    - `grep -n "type=\"module\".*main.tsx" /Users/a.mahindrakar/personal/fin/index.html` - the inline script line number is **less than** the module script line number (script runs first)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create ThemeContext.tsx</name>
  <files>src/context/ThemeContext.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/context/AppDataContext.tsx
    - /Users/a.mahindrakar/personal/fin/tailwind.config.js
    - /Users/a.mahindrakar/personal/fin/.planning/phases/06-dark-mode/06-CONTEXT.md
  </read_first>
  <action>
1. **Create** new file `src/context/ThemeContext.tsx`.
2. Export type `Theme` = alias `'light' | 'dark'`.
3. Export const `STORAGE_KEY = 'theme' as const` (or literal `'theme'` in all reads/writes).
4. Implement `function readStored(): Theme | null` — return `localStorage.getItem('theme')` when value is **exactly** `light` or `dark` (lowercase), else `null` — in try/catch, return `null` on error.
5. Initial theme for `useState` **init function**: on client, if `document.documentElement.classList.contains('dark')` return `'dark'`, else if `readStored() === 'dark'` return `'dark'`, else return `'light'`. (Matches inline script after first paint.)
6. Export `ThemeProvider` component: `children` only; on mount, ensure `classList` and storage align — if `theme` state is `dark`, `classList.add('dark')`; if `light`, `classList.remove('dark')` — but **idempotent** (no flicker) — a single `useLayoutEffect` or `useEffect` that only fixes drift if any (optional); avoid clearing `dark` on first paint. Preferred: do **not** use effect to replace initial class if inline script did the work; only `setItem` on user-driven `setTheme` in plan 02.
7. For Phase 01 minimal scope, export `useTheme()` with `{ theme: Theme; setTheme: (t: Theme) =&gt; void }` where `setTheme` (a) `setItem(STORAGE_KEY, t)`, (b) `t === 'dark' ? add('dark') : remove('dark')` on `document.documentElement.classList`, (c) `setState`.
8. Use `createContext` + `useContext` with a throw if provider missing, matching `AppDataContext` error pattern.
9. **No** `prefers-color-scheme` in this file. **No** `data.json` imports.
  </action>
  <acceptance_criteria>
    - File `src/context/ThemeContext.tsx` exists and exports `ThemeProvider` and `useTheme`
    - `grep "prefers-color-scheme" /Users/a.mahindrakar/personal/fin/src/context/ThemeContext.tsx` exits **1** (no matches)
    - `grep "STORAGE_KEY\|'theme'" /Users/a.mahindrakar/personal/fin/src/context/ThemeContext.tsx` has at least one `theme` storage reference
    - `cd /Users/a.mahindrakar/personal/fin && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wrap app with ThemeProvider in main.tsx</name>
  <files>src/main.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/main.tsx
    - /Users/a.mahindrakar/personal/fin/src/context/ThemeContext.tsx
  </read_first>
  <action>
1. `import { ThemeProvider } from '@/context/ThemeContext'`.
2. Wrap existing tree: `&lt;StrictMode&gt;&lt;ThemeProvider&gt;&lt;AppDataProvider&gt;...&lt;/ThemeProvider&gt;&lt;/StrictMode&gt;` (ThemeProvider may be inside or outside AppData per team — use **outer** wrap: `StrictMode` &gt; `ThemeProvider` &gt; `AppDataProvider` &gt; `LivePricesProvider` &gt; `App` so theme does not depend on data load).
  </action>
  <acceptance_criteria>
    - `grep ThemeProvider /Users/a.mahindrakar/personal/fin/src/main.tsx` returns at least 2 lines (open + import or two tags)
    - `cd /Users/a.mahindrakar/personal/fin && npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Run: `cd /Users/a.mahindrakar/personal/fin && npm run build` → exit 0
- Run: `cd /Users/a.mahindrakar/personal/fin && npm run lint` → exit 0
- Manual: Set `localStorage.setItem('theme','dark')` in DevTools, reload, confirm `document.documentElement.className` includes `dark` after load without React interaction
</verification>

## PLANNING COMPLETE
