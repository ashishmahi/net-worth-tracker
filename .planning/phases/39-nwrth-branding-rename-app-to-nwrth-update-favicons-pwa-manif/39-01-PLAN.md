---
phase: 39-nwrth-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/favicon.svg
  - public/icon-32.png
  - public/apple-touch-icon.png
  - public/icon-192.png
  - public/icon-512.png
  - public/icon-512-dark.png
  - public/icon-512-light.png
  - public/brand-mark.svg
  - public/wordmark.svg
  - public/wordmark-white.svg
  - public/manifest.json
  - index.html
autonomous: true
requirements: []

must_haves:
  truths:
    - "Browser tab shows nwrth favicon (not Vite logo)"
    - "Browser tab title reads 'nwrth'"
    - "PWA manifest loads with correct name, icons, and theme color"
    - "All 10 icon files exist in public/ and are served at root /"
  artifacts:
    - path: "public/manifest.json"
      provides: "PWA manifest with nwrth branding"
      contains: '"name": "nwrth"'
    - path: "public/favicon.svg"
      provides: "SVG favicon"
    - path: "public/icon-192.png"
      provides: "PWA icon 192x192"
    - path: "public/icon-512.png"
      provides: "PWA icon 512x512"
    - path: "public/apple-touch-icon.png"
      provides: "iOS home screen icon"
    - path: "public/icon-32.png"
      provides: "PNG favicon fallback"
    - path: "index.html"
      provides: "Updated head tags with nwrth branding"
      contains: "favicon.svg"
  key_links:
    - from: "index.html"
      to: "public/manifest.json"
      via: '<link rel="manifest">'
      pattern: 'rel="manifest".*manifest.json'
    - from: "index.html"
      to: "public/favicon.svg"
      via: '<link rel="icon">'
      pattern: 'href="/favicon.svg"'
    - from: "public/manifest.json"
      to: "public/icon-192.png"
      via: "icons array"
      pattern: "icon-192.png"
---

<objective>
Copy nwrth brand assets to public/, create PWA manifest, and update index.html head tags.

Purpose: Establish the nwrth brand identity at the browser/OS level -- favicon, PWA install, iOS home screen, meta description, and page title.
Output: 10 icon files in public/, public/manifest.json, updated index.html head section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-nwrth-branding-rename-app-to-nwrth-update-favicons-pwa-manif/39-CONTEXT.md
@.planning/phases/39-nwrth-branding-rename-app-to-nwrth-update-favicons-pwa-manif/39-UI-SPEC.md
@NWRTH-BRANDING-PROMPT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy icon assets and create PWA manifest</name>
  <files>
    public/favicon.svg
    public/icon-32.png
    public/apple-touch-icon.png
    public/icon-192.png
    public/icon-512.png
    public/icon-512-dark.png
    public/icon-512-light.png
    public/brand-mark.svg
    public/wordmark.svg
    public/wordmark-white.svg
    public/manifest.json
  </files>
  <read_first>
    NWRTH-BRANDING-PROMPT.md
    .planning/phases/39-nwrth-branding-rename-app-to-nwrth-update-favicons-pwa-manif/39-UI-SPEC.md
  </read_first>
  <action>
**Step 1 -- Copy all 10 icon files from nwrth-icons/ to public/ (per D-10):**

```bash
cp nwrth-icons/favicon.svg public/favicon.svg
cp nwrth-icons/icon-32.png public/icon-32.png
cp nwrth-icons/apple-touch-icon.png public/apple-touch-icon.png
cp nwrth-icons/icon-192.png public/icon-192.png
cp nwrth-icons/icon-512.png public/icon-512.png
cp nwrth-icons/icon-512-dark.png public/icon-512-dark.png
cp nwrth-icons/icon-512-light.png public/icon-512-light.png
cp nwrth-icons/brand-mark.svg public/brand-mark.svg
cp nwrth-icons/wordmark.svg public/wordmark.svg
cp nwrth-icons/wordmark-white.svg public/wordmark-white.svg
```

**Step 2 -- Create public/manifest.json (per D-06, D-07):**

Write this exact JSON to `public/manifest.json`:

```json
{
  "name": "nwrth",
  "short_name": "nwrth",
  "description": "Privacy-first personal wealth tracker — all data stays on your device",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#f7f4ed",
  "theme_color": "#3d3480",
  "icons": [
    { "src": "/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png" },
    { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable" }
  ]
}
```

Do NOT deviate from this JSON. The `start_url: "/"` is intentional per D-06.
  </action>
  <verify>
    <automated>ls -la public/favicon.svg public/icon-32.png public/apple-touch-icon.png public/icon-192.png public/icon-512.png public/icon-512-dark.png public/icon-512-light.png public/brand-mark.svg public/wordmark.svg public/wordmark-white.svg public/manifest.json && cat public/manifest.json | grep -c '"nwrth"'</automated>
  </verify>
  <acceptance_criteria>
    - All 10 icon files exist in public/ (ls confirms each file)
    - public/manifest.json exists and contains `"name": "nwrth"`
    - public/manifest.json contains `"short_name": "nwrth"`
    - public/manifest.json contains `"theme_color": "#3d3480"`
    - public/manifest.json contains `"background_color": "#f7f4ed"`
    - public/manifest.json contains `"start_url": "/"`
    - public/manifest.json icons array references icon-192.png and icon-512.png
    - `grep -c '"nwrth"' public/manifest.json` returns 2 (name + short_name)
  </acceptance_criteria>
  <done>All 10 icon files copied to public/. manifest.json created with exact nwrth PWA metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Update index.html head tags and title</name>
  <files>index.html</files>
  <read_first>
    index.html
    .planning/phases/39-nwrth-branding-rename-app-to-nwrth-update-favicons-pwa-manif/39-UI-SPEC.md
  </read_first>
  <action>
Edit `index.html` to make these exact changes in the `<head>` section:

**Remove this line (per D-08):**
```html
<link rel="icon" type="image/svg+xml" href="/vite.svg" />
```

**Add these lines in its place (per D-08, D-09):**
```html
<link rel="icon" href="/favicon.svg" type="image/svg+xml" />
<link rel="icon" href="/icon-32.png" sizes="32x32" type="image/png" />
<link rel="apple-touch-icon" href="/apple-touch-icon.png" />
<link rel="manifest" href="/manifest.json" />
<meta name="theme-color" content="#3d3480" />
<meta name="description" content="Privacy-first personal wealth tracker — all data stays on your device" />
```

**Replace the title tag:**
Change `<title>Wealth Tracker</title>` to `<title>nwrth</title>` (per D-02 -- always lowercase).

Do NOT modify the GitHub Pages SPA redirect script, the Google Fonts preconnect links, or the body section. Only the favicon link, new head tags, and title change.
  </action>
  <verify>
    <automated>grep -c 'vite.svg' index.html | grep '^0$' && grep -c 'favicon.svg' index.html | grep '^1$' && grep -c 'manifest.json' index.html | grep '^1$' && grep -c '<title>nwrth</title>' index.html | grep '^1$' && grep -c 'theme-color' index.html | grep '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'vite.svg' index.html` returns 0 (old favicon removed)
    - `grep 'href="/favicon.svg"' index.html` matches exactly once
    - `grep 'href="/icon-32.png"' index.html` matches exactly once
    - `grep 'href="/apple-touch-icon.png"' index.html` matches exactly once
    - `grep 'href="/manifest.json"' index.html` matches exactly once
    - `grep 'content="#3d3480"' index.html` matches exactly once (theme-color)
    - `grep '<title>nwrth</title>' index.html` matches exactly once
    - `grep 'Privacy-first personal wealth tracker' index.html` matches exactly once (description meta)
    - No occurrences of "Wealth Tracker" remain in index.html
  </acceptance_criteria>
  <done>index.html has nwrth favicon links, manifest link, theme-color meta, description meta, and title "nwrth". Old vite.svg reference removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public/ static assets | Files served directly to browser -- no user input processing |
| index.html head tags | Browser-parsed HTML -- no dynamic content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering | public/manifest.json | accept | Local-only app served from localhost or static hosting. Manifest is static JSON with no secrets. Standard web delivery. |
| T-39-02 | Information Disclosure | meta description | accept | Description is generic marketing copy ("Privacy-first personal wealth tracker"), contains no sensitive data. |
| T-39-03 | Tampering | icon files in public/ | accept | Static binary assets (PNG/SVG) copied from trusted local source (nwrth-icons/). No user-supplied content. |
</threat_model>

<verification>
1. `npm run dev` starts without errors
2. Browser tab shows nwrth favicon (not Vite logo)
3. Browser tab title shows "nwrth"
4. All icon files accessible at localhost:5173/favicon.svg, /icon-32.png, etc.
5. manifest.json accessible at localhost:5173/manifest.json with correct content
</verification>

<success_criteria>
- 10 icon files in public/ matching nwrth-icons/ source
- manifest.json in public/ with exact PWA metadata from spec
- index.html references favicon.svg, icon-32.png, apple-touch-icon.png, manifest.json
- index.html has theme-color #3d3480 and description meta
- index.html title is "nwrth" (lowercase)
- Zero references to vite.svg in index.html
</success_criteria>

<output>
After completion, create `.planning/phases/39-nwrth-branding-rename-app-to-nwrth-update-favicons-pwa-manif/39-01-SUMMARY.md`
</output>
