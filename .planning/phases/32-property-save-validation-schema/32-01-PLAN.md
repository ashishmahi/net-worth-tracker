---
phase: 32-property-save-validation-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/propertyValidation.ts
  - src/lib/__tests__/propertyValidation.test.ts
  - src/types/data.ts
  - src/lib/__tests__/schema.test.ts
  - src/pages/PropertyPage.tsx
autonomous: true
requirements:
  - PRV-01
  - PRV-02
  - PRV-03
  - PRV-04
  - PRV-05
must_haves:
  truths:
    - "Save never calls saveData when PropertyItem would violate PRV-01–03 rules after the same path-collapse logic as today"
    - "PropertyItemSchema.superRefine rejects the same bad tuples as the sheet-level validation"
    - "Save button disabled iff validation fails (same predicate as submit)"
    - "saveError used only for name-required and persistence failures, not PRV inline rules"
  artifacts:
    - path: "src/lib/propertyValidation.ts"
      provides: "Pure validation helpers + stable issue codes/messages consumed by Zod + PropertyPage"
    - path: "src/types/data.ts"
      provides: "PropertyItemSchema cross-field refinements"
    - path: "src/pages/PropertyPage.tsx"
      provides: "Disabled Save + inline loan/EMI errors + safeParse gate"
  key_links:
    - from: "src/types/data.ts"
      to: "src/lib/propertyValidation.ts"
      via: "superRefine imports helpers"
      pattern: "single implementation"
    - from: "src/pages/PropertyPage.tsx"
      to: "src/lib/propertyValidation.ts"
      via: "derive sheet validity + messages"
      pattern: "same helpers as schema"
---

<objective>
Deliver **Phase 32**: save-blocking validation (**PRV-01**–**PRV-03**), **`PropertyItemSchema`** parity (**PRV-04**), **Vitest** coverage (**PRV-05**) per [**32-CONTEXT.md**](32-CONTEXT.md), [**32-RESEARCH.md**](32-RESEARCH.md), and [**32-UI-SPEC.md**](32-UI-SPEC.md).
</objective>

<threat_model>
**Assets:** Integrity of **`wealth-tracker-data`** property rows; user trust in equity rollups.

| Threat | Mitigation |
|--------|------------|
| **T-32-01** | Inconsistent **`PropertyItem`** persisted (milestones vs agreement; loan vs agreement; EMI sanity) | **PRV-04**: **`safeParse`** / **`superRefine`** on **`nextItem`** before **`saveData`** |
| **T-32-02** | UI shows “valid” while schema would reject (or reverse) | **D-12/D-13**: one helper predicate for disabled Save and submit |

**Block severity:** High for **T-32-01** (data corruption); Medium for **T-32-02** (misleading UX).

</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-property-save-validation-schema/32-CONTEXT.md
@.planning/phases/32-property-save-validation-schema/32-RESEARCH.md
@.planning/phases/32-property-save-validation-schema/32-UI-SPEC.md
@.planning/phases/32-property-save-validation-schema/32-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>32-01-01 — propertyValidation helpers + exhaustive Vitest</name>
  <files>src/lib/propertyValidation.ts, src/lib/__tests__/propertyValidation.test.ts</files>
  <read_first>
    - src/types/data.ts
    - src/lib/financials.ts
    - src/pages/PropertyPage.tsx
    - .planning/phases/32-property-save-validation-schema/32-CONTEXT.md
  </read_first>
  <action>
    1. **Add `src/lib/propertyValidation.ts`** exporting pure functions that operate on a **`PropertyItem`**-shaped object (numeric fields already parsed). Minimum exports:
       - **`PROPERTY_VALIDATION_CODES`** — const object or string union with **exact** keys used in tests:
         - **`MILESTONE_TOTAL_EXCEEDS_AGREEMENT`**
         - **`OUTSTANDING_REQUIRED`**
         - **`OUTSTANDING_EXCEEDS_AGREEMENT`**
         - **`EMI_NOT_LESS_THAN_OUTSTANDING`**
       - **`getPropertyValidationIssues(item: PropertyItem): ReadonlyArray&lt;{ code: typeof PROPERTY_VALIDATION_CODES[keyof typeof PROPERTY_VALIDATION_CODES]; message: string }&gt;`**
         Implement rules:
         - **PRV-01:** If **`item.milestones.length > 0`**, let **`total = roundCurrency(sum of amountInr)`**. If **`total > item.agreementInr`**, emit **`MILESTONE_TOTAL_EXCEEDS_AGREEMENT`** with message **exactly** **`Milestone total exceeds agreement. Check amounts.`**
         - **PRV-02 / D-01–D-03:** If **`item.hasLiability`** is true:
           - If **`item.outstandingLoanInr`** is **`undefined`** or **`<= 0`** → **`OUTSTANDING_REQUIRED`** with message **`Outstanding loan must be greater than zero.`**
           - Else if **`item.outstandingLoanInr > item.agreementInr`** → **`OUTSTANDING_EXCEEDS_AGREEMENT`** with message **`Outstanding loan cannot exceed agreement value.`**
         - **PRV-03 / D-09–D-11:** If **`hasLiability`** and **`item.emiInr`** is **`undefined`** → OK. If **`item.emiInr`** is **`>= 0`** (including `0`): treat **`0`** as permissive per **D-09**. If **`item.emiInr > 0`** AND **`item.outstandingLoanInr`** is defined and **`item.emiInr >= item.outstandingLoanInr`** → **`EMI_NOT_LESS_THAN_OUTSTANDING`** with message **`EMI must be less than outstanding loan.`** (adjust only if outstanding missing — then outstanding rule fires first).

    2. **Order of checks:** Evaluate milestone sum vs agreement **when** milestones non-empty **regardless** of liability (both can surface — CONTEXT shows milestone block may coexist). Loan checks only when **`hasLiability`**. EMI check only when **`hasLiability`** and outstanding passes **> 0**.

    3. **`src/lib/__tests__/propertyValidation.test.ts`**: Table-driven cases covering each code + at least one **clean** golden **`PropertyItem`** per rule boundary (equality: **`total === agreement`** passes; **`outstanding === agreement`** passes).

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run src/lib/__tests__/propertyValidation.test.ts && npx tsc -b --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/lib/propertyValidation.ts` exits 0
    - `grep -q "getPropertyValidationIssues" src/lib/propertyValidation.ts` exits 0
    - `grep -q "MILESTONE_TOTAL_EXCEEDS_AGREEMENT" src/lib/propertyValidation.ts` exits 0
    - `grep -q "Milestone total exceeds agreement. Check amounts." src/lib/propertyValidation.ts` exits 0
    - `npm test -- --run src/lib/__tests__/propertyValidation.test.ts` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>Pure validation rules + exhaustive unit tests</done>
</task>

<task type="auto">
  <name>32-01-02 — PropertyItemSchema superRefine + schema tests</name>
  <files>src/types/data.ts, src/lib/__tests__/schema.test.ts</files>
  <read_first>
    - src/types/data.ts
    - src/lib/propertyValidation.ts
    - src/lib/__tests__/schema.test.ts
  </read_first>
  <action>
    1. Change **`PropertyItemSchema`** from a plain **`extend`** chain to **`.superRefine((val, ctx) => { ... })`** calling **`getPropertyValidationIssues(val)`**. For each issue, **`ctx.addIssue`** with **`code`** set to the string **`code`**, **`message`**, and **`path`**: **`['milestones']`** for milestone total; **`['outstandingLoanInr']`** for outstanding/agreement issues; **`['emiInr']`** for EMI sanity.

    2. **`src/lib/__tests__/schema.test.ts`**: Add **`describe('PropertyItemSchema cross-field validation')`** with **`safeParse`**:
       - **Fails** when milestones sum above agreement (construct minimal valid UUID/datetime base fields).
       - **Fails** **`hasLiability` true**, **`outstandingLoanInr: 0`**.
       - **Passes** fully-paid-like row: **`milestones: []`**, **`hasLiability: false`**.

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run src/lib/__tests__/schema.test.ts && npx tsc -b --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "superRefine" src/types/data.ts` exits 0
    - `grep -q "getPropertyValidationIssues" src/types/data.ts` exits 0
    - `grep -q "PropertyItemSchema cross-field" src/lib/__tests__/schema.test.ts` exits 0
    - `npm test -- --run src/lib/__tests__/schema.test.ts` exits 0
  </acceptance_criteria>
  <done>Zod parity with helpers</done>
</task>

<task type="auto">
  <name>32-01-03 — PropertyPage: build candidate, gate save, disable Save, loan inline errors</name>
  <files>src/pages/PropertyPage.tsx</files>
  <read_first>
    - src/pages/PropertyPage.tsx
    - src/lib/propertyValidation.ts
    - src/types/data.ts
    - .planning/phases/32-property-save-validation-schema/32-UI-SPEC.md
    - .planning/phases/32-property-save-validation-schema/32-CONTEXT.md
  </read_first>
  <action>
    1. **Extract** the **`nextItem`** construction from **`onSubmit`** into a **pure helper function in the page file** or a tiny **`buildPropertyItemFromDraft(...)`** local function **above** the submit handler — same logic as today for **`entryPath === 'fullyPaid'`** collapsing milestones/liability.

    2. **Validity predicate:** **`const candidate = buildPropertyItemFromDraft(...)`** then **`PropertyItemSchema.safeParse(candidate).success`** **must** equal **no validation issues** from **`getPropertyValidationIssues(candidate)`** — prefer **`safeParse`** as the single gate (**PRV-04**) per RESEARCH; if **`safeParse`** fails, do not save.

    3. **`sheetIsValid`**: Recompute whenever draft fields used in **`buildPropertyItemFromDraft`** change (same deps as **`buildPropertyItemFromDraft`** inputs).

    4. **Save button:** `<Button type="submit" disabled={saving || !sheetIsValid}>` (or equivalent: **`disabled={saving || !sheetIsValid}`**).

    5. **`onSubmit`:** After name trim check, build **`candidate`**. If **`!PropertyItemSchema.safeParse(candidate).success`**, **`preventDefault`** behavior already implicit — **return early** without **`saveData`**; **do not** set **`saveError`** for validation (**D-08**).

    6. **Inline messages:**
       - Milestone: existing **`exceedAgreement`** paragraph remains the primary PRV-01 signal (**D-07**).
       - Loan: when **`hasLiability`** and issues include **`OUTSTANDING_EXCEEDS_AGREEMENT`** or **`OUTSTANDING_REQUIRED`**, render **one** **`text-destructive text-sm`** paragraph **inside** the outstanding loan field group (after **`#loan-helper`** or **`aria-describedby`** linkage **)** with **`issue.message`** from helper — **not** in **`saveError`**.
       - EMI: same pattern for **`EMI_NOT_LESS_THAN_OUTSTANDING`** near **`#property-emi`**.

    7. **Imports:** Add **`PropertyItemSchema`** if needed for **`safeParse`** in page.

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run && npx tsc -b --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "PropertyItemSchema.safeParse\\|PropertyItemSchema\\.safeParse" src/pages/PropertyPage.tsx` exits 0
    - `grep -q "disabled={saving || !sheetIsValid}\\|disabled={!sheetIsValid" src/pages/PropertyPage.tsx` exits 0 OR `grep -q "sheetIsValid" src/pages/PropertyPage.tsx` exits 0 combined with Save **`disabled=`** containing **`sheetIsValid`**
    - `grep -q "getPropertyValidationIssues\\|safeParse" src/pages/PropertyPage.tsx` exits 0
    - `npm test -- --run` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>Sheet behavior matches CONTEXT + UI-SPEC</done>
</task>

</tasks>
