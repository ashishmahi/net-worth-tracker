---
phase: 33-property-sheet-responsive-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/PropertyPage.tsx
autonomous: true
requirements:
  - PRA-01
must_haves:
  truths:
    - "Milestone block keeps horizontal scroll (overflow-x-auto + wide min-width table) and shows a muted horizontal-scroll hint per D-02"
    - "Path radiogroup stacks on narrow widths and uses row layout from sm breakpoint per D-05"
    - "Arrow keys move selection within the path radiogroup with orientation-aware keys per D-06; Tab visits each path control then Property name per D-07"
    - "Opening the sheet focuses the first path control per D-08/D-13; focus-visible matches global Button styling per D-16"
    - "No aria-describedby added to the Save button; no assertive live-region upgrade to Phase 32 validation copy per D-10/D-11; no extra visible disabled-Save helper line per D-12"
  artifacts:
    - path: "src/pages/PropertyPage.tsx"
      provides: "Responsive path layout, scroll hint, radiogroup keyboard + initial focus, D-09–D-12 constraints preserved"
  key_links:
    - from: "src/pages/PropertyPage.tsx"
      to: ".planning/phases/33-property-sheet-responsive-accessibility/33-CONTEXT.md"
      via: "implementation matches locked decisions D-01–D-16"
      pattern: "single page shell"
---

<objective>
Deliver **Phase 33** (**PRA-01**): Property sheet **narrow-viewport usability** and **accessible path controls + milestone region** per [**33-CONTEXT.md**](33-CONTEXT.md), [**33-RESEARCH.md**](33-RESEARCH.md), [**33-UI-SPEC.md**](33-UI-SPEC.md), and [**33-PATTERNS.md**](33-PATTERNS.md).
</objective>

<threat_model>
**Assets:** User comprehension of Property entry; predictable keyboard/focus in the Sheet.

| Threat | Mitigation |
|--------|------------|
| **T-33-01** | Focus lands somewhere unusable or steals from restore | **D-08/D-13**: imperative focus to first path only on open; **D-14**: no override on close |
| **T-33-02** | Arrow keys conflict with Sheet trap or browser chrome | **`preventDefault`** only for arrows when event target is inside **`radiogroup`** and key is Arrow |

**Block severity:** Low (UX); no persisted data shape changes.

</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-property-sheet-responsive-accessibility/33-CONTEXT.md
@.planning/phases/33-property-sheet-responsive-accessibility/33-RESEARCH.md
@.planning/phases/33-property-sheet-responsive-accessibility/33-UI-SPEC.md
@.planning/phases/33-property-sheet-responsive-accessibility/33-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>33-01-01 — Milestone scroll hint + preserve wide table</name>
  <files>src/pages/PropertyPage.tsx</files>
  <read_first>
    - src/pages/PropertyPage.tsx
    - .planning/phases/33-property-sheet-responsive-accessibility/33-CONTEXT.md
  </read_first>
  <action>
    1. Adjacent to the milestone **`overflow-x-auto`** wrapper (same **`milestonesBlock`** subtree), add **muted** helper copy per **D-02** — short line such as **“Scroll sideways to see all columns.”** using **`text-xs text-muted-foreground`** (or equivalent existing muted utility).
    2. Keep **`Table`** with **`min-w-[36rem]`** and outer **`overflow-x-auto`** + **`min-w-0`** intact (**D-01**). Do not replace the table with a card layout.
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx tsc -b --pretty false && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "overflow-x-auto" src/pages/PropertyPage.tsx` exits 0
    - `grep -q "min-w-\\[36rem\\]" src/pages/PropertyPage.tsx` exits 0
    - `grep -qE "Scroll sideways|scroll horizontally" src/pages/PropertyPage.tsx` exits 0
    - `grep -q "text-muted-foreground" src/pages/PropertyPage.tsx` exits 0
    - `npx tsc -b --pretty false` exits 0
    - `npm test -- --run` exits 0
  </acceptance_criteria>
  <done>Milestone region documents horizontal scroll; table pattern retained</done>
</task>

<task type="auto">
  <name>33-01-02 — Path radiogroup: responsive stack, arrows, initial focus</name>
  <files>src/pages/PropertyPage.tsx</files>
  <read_first>
    - src/pages/PropertyPage.tsx
    - src/components/ui/button.tsx
    - .planning/phases/33-property-sheet-responsive-accessibility/33-CONTEXT.md
  </read_first>
  <action>
    1. **Responsive layout (**D-05**):** Change the path container from fixed three columns to **`grid grid-cols-1 sm:grid-cols-3`** (keep **`gap-1`**, border, padding). **`sm`** breakpoint must match Tailwind default **640px** so orientation logic can align.
    2. **Refs:** Create **`useRef`** (or array of refs) for each path **`Button`** in **`PATH_KEYS` order**.
    3. **Initial focus (**D-08**, **D-13**):** When **`sheetOpen`** becomes **`true`**, after layout (**`requestAnimationFrame`** or **`setTimeout(0)`**), call **`focus()`** on the **`Button`** for the current **`entryPath`** (or first key in **`PATH_KEYS`** if you always reset focus target to first — **CONTEXT** says first path control; focus the **first** path button on open, not necessarily the selected one — re-read: **D-08** “move focus explicitly to the **first path control**”. Implement focus on **`PATH_KEYS[0]`**’s button when sheet opens.
    4. **Arrow keys (**D-06**):** On the **`radiogroup`** wrapper **`onKeyDown`**: when focus is on a path **`Button`**, **`preventDefault`** for arrows and move **`entryPath`** + **`focus()`** to adjacent index; use **Left/Right** when **`sm+`** (matchMedia **`(min-width: 640px)`**), **Up/Down** when below **`sm`**. Wrap at ends (last → first).
    5. **Tab (**D-07**):** Preserve DOM order **Fully paid → Builder milestones → Mortgaged** matching **`PATH_KEYS`** — stacked **`grid-cols-1`** preserves top-to-bottom visual order.
    6. **D-16:** Do **not** add a custom **`ring-`** / **`focus-visible:`** class only on path buttons beyond **`Button`** defaults.
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx tsc -b --pretty false && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "grid-cols-1 sm:grid-cols-3" src/pages/PropertyPage.tsx` exits 0
    - `grep -q "role=\"radiogroup\"" src/pages/PropertyPage.tsx` exits 0
    - `grep -q "onKeyDown" src/pages/PropertyPage.tsx` exits 0
    - `grep -q "640px\\|min-width: 640px\\|matchMedia" src/pages/PropertyPage.tsx` exits 0
    - `grep -q "\\.focus(" src/pages/PropertyPage.tsx` exits 0
    - `grep -q "useRef" src/pages/PropertyPage.tsx` exits 0
    - `npx tsc -b --pretty false` exits 0
    - `npm test -- --run` exits 0
  </acceptance_criteria>
  <done>Path controls reflow on narrow screens; keyboard navigation and open-focus behave per CONTEXT</done>
</task>

<task type="auto">
  <name>33-01-03 — SR/disabled Save constraints + suite green</name>
  <files>src/pages/PropertyPage.tsx</files>
  <read_first>
    - src/pages/PropertyPage.tsx
    - .planning/phases/33-property-sheet-responsive-accessibility/33-CONTEXT.md
  </read_first>
  <action>
    1. **D-10:** Confirm the submit **`Button`** (**`type="submit"`**, **`Save`**) has **no** **`aria-describedby`** attribute. If absent, **leave unchanged** — do not add.
    2. **D-12:** Confirm **no new** visible footer helper text **solely** explaining disabled Save (inline validation under sections remains OK per Phase **32**).
    3. **D-11:** Do **not** change **`role`** on Phase **32** milestone/loan inline messages (keep **`role="status"`** where present).
    4. Run full **`npm test -- --run`** and **`npx tsc -b --pretty false`** — record as verification gate for phase.
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx tsc -b --pretty false && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - Python one-liner confirms Save **`Button`** opening tag has no **`aria-describedby`**: `python3 -c "import re; t=open('src/pages/PropertyPage.tsx').read(); m=re.search(r'<Button[^>]*type=\"submit\"[^>]*>', t); assert m; assert 'aria-describedby' not in t[m.start():m.start()+220]"` exits 0
    - `grep -q 'role=\"status\"' src/pages/PropertyPage.tsx` exits 0 (Phase 32 inline validation pattern retained — do not remove)
    - `npm test -- --run` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>No new Save-specific ARIA wiring; suite and types green</done>
</task>

</tasks>

<verification_criteria>
- **PRA-01:** At ~375px viewport width, Property sheet shows stacked path controls; milestone table remains horizontally scrollable with visible hint; primary fields do not require horizontal page scroll beyond the intentional table scroll region.
- **Automated:** All task **`acceptance_criteria`** commands exit 0.
</verification_criteria>
