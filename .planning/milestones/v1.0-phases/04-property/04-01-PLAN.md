---
phase: 04-property
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/data.ts
autonomous: true
requirements:
  - D-03
  - D-04
  - D-11
  - D-12
must_haves:
  truths:
    - "Property `items` array is no longer `z.unknown()` — each item has typed fields per 04-CONTEXT (label, agreementInr, milestones[], hasLiability, optional outstandingLoanInr)"
    - "Milestone rows store id, label, amountInr, isPaid only (no computed totals in JSON per CLAUDE.md)"
    - "INITIAL_DATA shape in AppDataContext remains valid: property.items: []"
  artifacts:
    - path: "src/types/data.ts"
      provides: "PropertyMilestoneRowSchema, PropertyItemSchema, PropertySchema, exported PropertyItem and milestone types"
      contains: "PropertyItemSchema"
    - path: "src/types/data.ts"
      provides: "Re-exported AppData with property.items typed as PropertyItem[]"
      contains: "export type Property"
  key_links:
    - from: "src/types/data.ts"
      to: "BaseItemSchema"
      via: "extend"
      pattern: "PropertyItemSchema = BaseItemSchema.extend"

---

<objective>
Replace the `PropertySchema` placeholder with a full **Zod** model for `assets.property.items`: per-property `label`, **`agreementInr`**, a **variable-length** `milestones` array (uuid `id`, `label`, `amountInr`, `isPaid`), **`hasLiability`**, and optional **`outstandingLoanInr`**. Export TypeScript types from `data.ts` only. No UI changes in this plan.
</objective>

<threat_model>
**Scope:** Local `data.json`; no network. **Assets at risk:** User data integrity on parse.

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-04-10 | Overly strict schema rejects good saves | Medium | `nonnegative()` for amounts; `milestones` default `[]`; no unnecessary `.min(1)` on milestone label at Zod level if it blocks migration |
| T-04-11 | PII in logs | Low | No `console.log` of property payloads in this plan (types only) |

**Block on:** high-severity N/A for schema-only work.
</threat_model>

<context>
@.planning/phases/04-property/04-CONTEXT.md
@.planning/phases/04-property/04-RESEARCH.md
@.planning/phases/04-property/04-PATTERNS.md
@src/types/data.ts
@src/context/AppDataContext.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Property milestone and item Zod schemas</name>
  <files>src/types/data.ts</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/types/data.ts
    - /Users/a.mahindrakar/personal/fin/.planning/phases/04-property/04-CONTEXT.md
    - /Users/a.mahindrakar/personal/fin/CLAUDE.md
  </read_first>
  <action>
1. Add `PropertyMilestoneRowSchema = z.object({ id: z.string().uuid(), label: z.string(), amountInr: z.number().nonnegative(), isPaid: z.boolean() })`.
2. Add `PropertyItemSchema = BaseItemSchema.extend({ label: z.string().min(1), agreementInr: z.number().nonnegative(), milestones: z.array(PropertyMilestoneRowSchema), hasLiability: z.boolean(), outstandingLoanInr: z.number().nonnegative().optional() })`.
3. Set `PropertySchema = z.object({ updatedAt: z.string().datetime(), items: z.array(PropertyItemSchema) })`.
4. In `DataSchema` → `assets.property`, use the new `PropertySchema` instead of the old one.
5. Export: `export type PropertyItem = z.infer<typeof PropertyItemSchema>` and `export type PropertyMilestoneRow = z.infer<typeof PropertyMilestoneRowSchema>` (exact names `PropertyMilestoneRow` and `PropertyItem` — adjust if alias collision; keep exports discoverable for `PropertyPage` in plan 02).
  </action>
  <acceptance_criteria>
    - `src/types/data.ts` contains the substring `PropertyMilestoneRowSchema` and `PropertyItemSchema`
    - `DataSchema` references `PropertySchema` with `items: z.array(PropertyItemSchema)` (or equivalent) — **not** `z.array(z.unknown())` for property items
    - `cd /Users/a.mahindrakar/personal/fin && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Type exports and INITIAL_DATA compile check</name>
  <files>src/types/data.ts, src/context/AppDataContext.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/context/AppDataContext.tsx
  </read_first>
  <action>
1. After Task 1, run `npx tsc --noEmit`. If `INITIAL_DATA` in `AppDataContext.tsx` no longer satisfies `AppData` (e.g. property `items: []` needs explicit typing), fix with **only** the minimal type assertion or satisfies clause so empty `[]` is valid. Do **not** change runtime behavior of load/save. Prefer fixing `data.ts` exports if inference fails.
2. **Do not** add a migration function unless tsc or safeParse actually requires it for `items: []`.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `INITIAL_DATA` still has `property.items: []` and a valid `updatedAt` (same structure as before plan 01)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Run: `cd /Users/a.mahindrakar/personal/fin && npx tsc --noEmit` → exit 0
- Run: `cd /Users/a.mahindrakar/personal/fin && npm run build` → exit 0
- Grep: `rg "z\\.unknown\\(\\)" src/types/data.ts` — if Property line is the only one removed, confirm file no longer uses `z.unknown()` for `property.items` (may be none left)
</verification>

## PLANNING COMPLETE
