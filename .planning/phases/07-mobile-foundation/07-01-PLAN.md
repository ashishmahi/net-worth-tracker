---
phase: 07-mobile-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AppSidebar.tsx
  - src/components/MobileTopBar.tsx
  - src/App.tsx
  - src/components/ui/sidebar.tsx
autonomous: true
requirements:
  - MB-01
must_haves:
  truths:
    - "At <768px: Menu in top bar opens offcanvas; nav item closes drawer; theme in top bar without opening drawer; at 768px+ no top bar and inline sidebar"
    - "AppSidebar uses collapsible=\"offcanvas\" and setOpenMobile(false) on nav select when isMobile"
  artifacts:
    - path: "src/components/MobileTopBar.tsx"
      provides: "MobileTopBar, Menu, theme, useSidebar+useTheme"
      contains: "Menu|useSidebar|useTheme|Toggle main navigation|Switch to dark mode"
    - path: "src/components/AppSidebar.tsx"
      provides: "offcanvas, close on nav"
      contains: "offcanvas|setOpenMobile|useSidebar"
  key_links:
    - from: "src/components/MobileTopBar.tsx"
      to: "src/context/ThemeContext.tsx"
      via: "useTheme()"
    - from: "src/components/AppSidebar.tsx"
      to: "src/components/ui/sidebar.tsx"
      via: "useSidebar, Sidebar collapsible"

---

<objective>
Enable **MB-01** and ROADMAP Phase 7: switch `AppSidebar` to **offcanvas** mobile **Sheet**; **close the drawer** on **nav** selection; add **`MobileTopBar`** with **lucide `Menu`**, **`toggleSidebar`**, and **icon-only** theme (`useTheme`, same **aria-label** substrings as footer). Place the bar **above** `<main>` in **`SidebarInset`**. Update mobile **Sheet** `SheetTitle` / `SheetDescription` in `sidebar.tsx` to **07-UI-SPEC** copy. **Desktop 768px+** unchanged: **no** top bar.
</objective>

<threat_model>
**Scope:** Client-side shell, navigation, theme preference (localStorage via existing `ThemeContext`).

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-07-01 | User cannot open/close main nav (trapped in content) | Medium | `Menu` control calls `toggleSidebar()`; `setOpenMobile(false)` on nav; Sheet overlay/Esc from Radix defaults |
| T-07-02 | Assistive name missing on chrome controls | Medium | `aria-label` **"Toggle main navigation"** (or equivalent with **main** + **navigation** per 07-UI-SPEC) on hamburger; theme: **"Switch to dark mode"** / **"Switch to light mode"**; Sheet strings **"Main navigation"** + non-generic description |
| T-07-03 | Styling with raw grays/white in new chrome | Low | `bg-background`, `border-border`, `Button variant="ghost"` per 07-UI-SPEC |

**Block on:** high — N/A.
</threat_model>

<context>
@.planning/phases/07-mobile-foundation/07-UI-SPEC.md
@.planning/phases/07-mobile-foundation/07-CONTEXT.md
@.planning/phases/07-mobile-foundation/07-PATTERNS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppSidebar offcanvas + close on nav (D-04, D-06)</name>
  <files>src/components/AppSidebar.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/components/AppSidebar.tsx
    - /Users/a.mahindrakar/personal/fin/src/components/ui/sidebar.tsx
  </read_first>
  <action>
1. Import `useSidebar` from `@/components/ui/sidebar` (same module as `Sidebar`, `SidebarMenuButton`, etc.).
2. At top of `AppSidebar` function body, add: `const { isMobile, setOpenMobile } = useSidebar()`.
3. Change the root `&lt;Sidebar&gt;` prop from `collapsible="none"` to `collapsible="offcanvas"`.
4. In each `SidebarMenuButton` `onClick` handler, after calling `onSelect(item.key)` (or equivalent), add: if `isMobile` is true, call `setOpenMobile(false)`.
5. Do **not** remove the footer theme control (D-03: theme in two places on mobile).
  </action>
  <acceptance_criteria>
    - `grep "collapsible=\"offcanvas\"" /Users/a.mahindrakar/personal/fin/src/components/AppSidebar.tsx` returns a match
    - `grep "setOpenMobile" /Users/a.mahindrakar/personal/fin/src/components/AppSidebar.tsx` returns a match
    - `grep "useSidebar" /Users/a.mahindrakar/personal/fin/src/components/AppSidebar.tsx` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: New MobileTopBar (D-01, D-02, D-05, D-07)</name>
  <files>src/components/MobileTopBar.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/context/ThemeContext.tsx
    - /Users/a.mahindrakar/personal/fin/src/components/AppSidebar.tsx
    - /Users/a.mahindrakar/personal/fin/.planning/phases/07-mobile-foundation/07-UI-SPEC.md
  </read_first>
  <action>
1. Create **new** file `src/components/MobileTopBar.tsx` exporting a default or named `MobileTopBar` component.
2. Use `useSidebar()` to get `isMobile` and `toggleSidebar`. Use `useTheme()` from `@/context/ThemeContext` for `theme` and `setTheme` (or the same `theme` + toggle pattern as `AppSidebar`: `setTheme(theme === 'light' ? 'dark' : 'light')` if that matches `ThemeContext` API).
3. If `!isMobile`, return **`null`** (call hooks before this return so hook order is stable: `useSidebar` + `useTheme` first, then early return).
4. Render a full-width **row** with at minimum: `className` including `z-40`, `border-b border-border`, `bg-background`, horizontal padding `px-4` (or `px-3`), `min-h-[44px]` (or `min-h-11` if exactly 44px), `flex items-center justify-between` — leading: **hamburger**; trailing: **theme** icon-only.
5. **Hamburger:** shadcn `Button` with `type="button"`, `variant="ghost"`, import **`Menu`** from `lucide-react` (not `PanelLeft`). `onClick` calls `toggleSidebar` only. **`aria-label`:** exactly **`Toggle main navigation`**.
6. **Theme:** `Button` `type="button"` `variant="ghost"`, `min-h`/`min-w` at least 44px where applicable; icon: **Moon** when current theme is light, **Sun** when dark (match `AppSidebar` direction). **No** visible "Light"/"Dark" text in this bar. **`aria-label`:** when `theme === 'light'` use exactly **`Switch to dark mode`**; when `theme === 'dark'` use exactly **`Switch to light mode`**.
7. Do not add a page title or section title in this bar (D-01).
  </action>
  <acceptance_criteria>
    - `test -f /Users/a.mahindrakar/personal/fin/src/components/MobileTopBar.tsx` exits 0
    - `grep "Menu" /Users/a.mahindrakar/personal/fin/src/components/MobileTopBar.tsx` returns a match
    - `grep "Toggle main navigation" /Users/a.mahindrakar/personal/fin/src/components/MobileTopBar.tsx` returns a match
    - `grep "Switch to dark mode" /Users/a.mahindrakar/personal/fin/src/components/MobileTopBar.tsx` returns a match
    - `grep "Switch to light mode" /Users/a.mahindrakar/personal/fin/src/components/MobileTopBar.tsx` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: App.tsx — mount MobileTopBar in SidebarInset</name>
  <files>src/App.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/App.tsx
    - /Users/a.mahindrakar/personal/fin/src/components/MobileTopBar.tsx
  </read_first>
  <action>
1. Import `MobileTopBar` from `@/components/MobileTopBar` (or relative path per project alias).
2. Inside `&lt;SidebarInset&gt;`, place `&lt;MobileTopBar /&gt;` **immediately before** the existing `&lt;main className="p-6"&gt;` (or as first child of `SidebarInset`), so the top bar is above main content and only shows on mobile (handled inside `MobileTopBar`).
3. Do not wrap `SidebarProvider` differently; do not add a desktop top bar.
  </action>
  <acceptance_criteria>
    - `grep "MobileTopBar" /Users/a.mahindrakar/personal/fin/src/App.tsx` returns a match
    - `grep "SidebarInset" /Users/a.mahindrakar/personal/fin/src/App.tsx` — `MobileTopBar` appears in the same file as `SidebarInset` (manual read: `MobileTopBar` is child of `SidebarInset`)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Mobile Sheet screen-reader copy (07-UI-SPEC)</name>
  <files>src/components/ui/sidebar.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/components/ui/sidebar.tsx
    - /Users/a.mahindrakar/personal/fin/.planning/phases/07-mobile-foundation/07-UI-SPEC.md
  </read_first>
  <action>
In the **mobile** `Sheet` branch of `Sidebar` (where `SheetHeader` is `className="sr-only"`), replace:
- `SheetTitle` child text: from **Sidebar** to **Main navigation**
- `SheetDescription` child text: to exactly **Wealth and settings sections** (matches table in 07-UI-SPEC allowed examples)
Only edit the `SheetHeader` **inside the `isMobile` branch** (the block with `&lt;Sheet open={openMobile} ...&gt;`).
  </action>
  <acceptance_criteria>
    - `grep "Main navigation" /Users/a.mahindrakar/personal/fin/src/components/ui/sidebar.tsx` returns a match
    - `grep "Wealth and settings sections" /Users/a.mahindrakar/personal/fin/src/components/ui/sidebar.tsx` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Build and regression smoke</name>
  <files>package.json</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/package.json
  </read_first>
  <action>
1. From repo root run `npm run build` and fix any TypeScript or Vite errors until exit code 0.
2. Optional: `npm run lint` if the project has it and it is quick — fix new issues only in touched files.
  </action>
  <acceptance_criteria>
    - `cd /Users/a.mahindrakar/personal/fin && npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run build` → exit 0
- **Manual (MB-01):** 375px — **Menu** visible; opens drawer; tap nav item — section changes and drawer closes; theme in top bar toggles light/dark without opening drawer; **768px+** — no mobile top bar, sidebar not in Sheet overlay
- Grep: `07-RESEARCH.md` + this plan’s acceptance criteria pass for new/edited files
</verification>

## PLANNING COMPLETE
