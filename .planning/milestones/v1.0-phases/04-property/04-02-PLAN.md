---
phase: 04-property
plan: 02
type: execute
wave: 1
depends_on: ["04-01-PLAN.md"]
files_modified:
  - src/pages/PropertyPage.tsx
  - src/components/ui/checkbox.tsx
  - src/components/ui/switch.tsx
  - src/components/ui/table.tsx
  - components.json
autonomous: true
requirements:
  - D-01
  - D-02
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - 04-UI-SPEC
must_haves:
  truths:
    - "Property list uses Sheet for add/edit; row tap opens sheet; Delete + inline error pattern matches Bank"
    - "Milestone paid sum and balance due to builder are computed in UI; not stored in data.json"
    - "shadcn Checkbox + Switch + Table (or stacked rows per UI-SPEC) installed from official registry"
  artifacts:
    - path: "src/pages/PropertyPage.tsx"
      provides: "Full property CRUD, milestones, liability, derived readouts, empty state per 04-UI-SPEC"
      contains: "useAppData|saveData"
  key_links:
    - from: "src/pages/PropertyPage.tsx"
      to: "src/types/data.ts"
      via: "import"
      pattern: "PropertyItem"

---

<objective>
Build **`PropertyPage`** to match `04-CONTEXT` and `04-UI-SPEC`: install shadcn **checkbox**, **switch**, **table**; list properties with **summary** (agreement, derived balance, milestone count); **Sheet** for add/edit with **milestone** rows (add/remove, paid toggle), **liability** Switch + **outstanding loan (INR)**; soft **reconciliation** warning; **empty state** and **copy** per UI-SPEC. Use **`parseFinancialInput`**, **`roundCurrency`**, **`createId`**, **`nowIso`**; persist **only** raw fields per D-11.
</objective>

<threat_model>
**Scope:** Local-only SPA. **Risks:** user-supplied text in React tree.

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-04-01 | XSS via property labels | Low | No `dangerouslySetInnerHTML`; use normal JSX `{label}` text nodes |
| T-04-02 | Misleading “balance” | Low (UX) | Use UI-SPEC copy: balance due to builder = derived; loan = separate field |

**Block on:** high-severity N/A.
</threat_model>

<context>
@.planning/phases/04-property/04-CONTEXT.md
@.planning/phases/04-property/04-RESEARCH.md
@.planning/phases/04-property/04-UI-SPEC.md
@.planning/phases/04-property/04-PATTERNS.md
@src/pages/BankSavingsPage.tsx
@src/lib/financials.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Checkbox, Switch, Table</name>
  <files>src/components/ui/checkbox.tsx, src/components/ui/switch.tsx, src/components/ui/table.tsx, components.json</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/components.json
    - /Users/a.mahindrakar/personal/fin/.planning/phases/04-property/04-UI-SPEC.md
  </read_first>
  <action>
From project root, run the official shadcn CLI (same as Phase 2 style):
- `npx shadcn@latest add checkbox`
- `npx shadcn@latest add switch`
- `npx shadcn@latest add table`
Accept default paths under `src/components/ui/`. Do not add non-official registries.
  </action>
  <acceptance_criteria>
    - Files exist: `src/components/ui/checkbox.tsx`, `src/components/ui/switch.tsx`, `src/components/ui/table.tsx`
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement PropertyPage (list, Sheet, milestones, liability)</name>
  <files>src/pages/PropertyPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/pages/BankSavingsPage.tsx
    - /Users/a.mahindrakar/personal/fin/src/types/data.ts
    - /Users/a.mahindrakar/personal/fin/src/context/AppDataContext.tsx
    - /Users/a.mahindrakar/personal/fin/.planning/phases/04-property/04-UI-SPEC.md
  </read_first>
  <action>
1. **List:** For each `data.assets.property.items` entry, show label (`text-sm font-semibold`), secondary line with agreement in INR and **balance due to builder** = `roundCurrency(agreementInr - sum of amountInr for paid milestones)` (D-05), and milestone hint e.g. “`paidCount` / `totalCount` paid” (optional Badge or `text-xs`).
2. **Sheet (add/edit):** Required **property label**; **agreement (INR)** with string → `parseFinancialInput` + `roundCurrency` on save.
3. **Milestones:** Dynamic rows array — each: label, amount (INR) string/number per project convention, `isPaid` (Checkbox). **Add milestone** button appends a row with `createId()`; include remove for a row if UX allows (Claude’s discretion) without breaking D-04.
4. **Derived in Sheet:** Show total paid to builder and balance due; use `aria-live="polite"` on that region (UI-SPEC).
5. **Reconciliation:** If `roundCurrency(sum of milestone amountInr) > agreementInr`, show one line: `Milestone total exceeds agreement. Check amounts.` (`text-destructive text-sm` per UI-SPEC; exact string may be equivalent copy).
6. **Liability:** `Switch` for `hasLiability`; when on, show **outstanding loan (INR)** money field (optional in data — can save 0 or omit if empty per schema).
7. **Save/Delete:** Mirror `BankSavingsPage` — `saveData` with immutable updates to `assets.property.items` and `updatedAt: nowIso()`; destructive Delete in footer; `saveError` + `role="alert"`.
8. **Empty state:** “No properties yet” / body per UI-SPEC; primary **Add property** in header and empty state.
9. **INR display:** `toLocaleString('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 })` or 2 for amounts — be consistent with Bank section.
  </action>
  <acceptance_criteria>
    - `src/pages/PropertyPage.tsx` imports `useAppData` and `PropertyItem` (or equivalent) from `@/types/data` or `@/context/...` path used elsewhere
    - `src/pages/PropertyPage.tsx` contains the substring `parseFinancialInput` and `roundCurrency` and `createId`
    - `src/pages/PropertyPage.tsx` contains `agreementInr` and `hasLiability` (or schema-identical field names) as persisted fields
    - `cd /Users/a.mahindrakar/personal/fin && npm run build && npm run lint` both exit 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run build` → exit 0
- `npm run lint` → exit 0
- **Manual UAT (short):** Add a property, two milestones, mark one paid, set agreement so it exceeds staged paid total (check balance readout); toggle loan; save; hard refresh; edit; delete. Confirm `data.json` (if visible) has no computed `balance` or `totalPaid` keys under property items
</verification>

## PLANNING COMPLETE
