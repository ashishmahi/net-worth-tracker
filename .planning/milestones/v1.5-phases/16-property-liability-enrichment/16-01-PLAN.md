---
phase: 16-property-liability-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/data.ts
  - src/pages/PropertyPage.tsx
autonomous: true
requirements:
  - PROP-01
  - PROP-02
  - PROP-03

must_haves:
  truths:
    - "PropertyItemSchema includes optional lender and emiInr; DataSchema still parses createInitialData()"
    - "When hasLiability is on, optional Lender and EMI fields appear and persist to data.json when non-empty"
    - "Disambiguation hint copy appears above liability fields without requiring the toggle"
    - "No react-router-dom usage — app navigation remains SectionKey-based"
  artifacts:
    - path: "src/types/data.ts"
      provides: "PropertyItemSchema extended with lender?, emiInr?"
      contains: "emiInr"
    - path: "src/pages/PropertyPage.tsx"
      provides: "Hint + lenderStr/emiStr state + conditional fields + save spread"
      contains: "For loans not tied to a specific property"
  key_links:
    - from: "PropertyPage"
      to: "PropertyItemSchema"
      via: "saved property items include optional lender and emiInr"
      pattern: "lender"
---

<objective>
Extend the property liability UX: add **optional `lender` and `emiInr`** to `PropertyItemSchema` and the property sheet; show an **always-visible disambiguation hint** (exact copy in tasks) **above** the conditional liability fields. Align with **section-based navigation** (no React Router — CONTEXT D-04 is superseded by `16-UI-SPEC.md` for this repo).

Output: updated `src/types/data.ts` and `src/pages/PropertyPage.tsx`; `npm test` and `npx tsc -b --noEmit` green.
</objective>

<threat_model>
## Trust boundaries

Local-only React app; data in `data.json`; no network from these edits.

## STRIDE (ASVS L1)

| Threat ID | Category | Component | Severity | Mitigation |
|-----------|----------|-----------|----------|------------|
| T-16-01 | Spoofing | — | — | N/A |
| T-16-02 | Tampering | Saved JSON | Low | Existing Zod load path; new optional fields must validate |
| T-16-03 | XSS | `lender` string in UI | Low | React default escaping; no `dangerouslySetInnerHTML` |

**Block policy:** No HIGH threats identified for this phase.
</threat_model>

<execution_context>
@.planning/phases/16-property-liability-enrichment/16-CONTEXT.md
@.planning/phases/16-property-liability-enrichment/16-UI-SPEC.md
@.planning/phases/16-property-liability-enrichment/16-RESEARCH.md
@.planning/phases/16-property-liability-enrichment/16-PATTERNS.md
@.planning/phases/16-property-liability-enrichment/16-VALIDATION.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Extend PropertyItemSchema with lender and emiInr</name>
  <files>src/types/data.ts</files>
  <read_first>
    - src/types/data.ts (full PropertyItemSchema and LiabilityItemSchema block)
    - .planning/phases/16-property-liability-enrichment/16-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
    In `PropertyItemSchema` (after `outstandingLoanInr` optional line), add:
    - `lender: z.string().optional()`
    - `emiInr: z.number().nonnegative().optional()`
    Do not change other schemas. `PropertyItem` type updates via inference.
  </action>
  <acceptance_criteria>
    - `grep "PropertyItemSchema = BaseItemSchema.extend" -A15 src/types/data.ts` shows both `lender` and `emiInr` lines
    - `grep "emiInr: z.number().nonnegative().optional()" src/types/data.ts` returns at least 1 match (property line)
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Property sheet — hint, state, lender/EMI fields, save/load</name>
  <files>src/pages/PropertyPage.tsx</files>
  <read_first>
    - src/pages/PropertyPage.tsx (full file — especially openAdd, openEdit, onSubmit, liability block ~396–427)
    - .planning/phases/16-property-liability-enrichment/16-UI-SPEC.md
    - src/types/data.ts (PropertyItem shape after Task 1)
  </read_first>
  <action>
    1. **State:** Add `lenderStr` and `emiStr`, both `useState('')`. In `openAdd`, reset both to `''`. In `openEdit`, set `lenderStr` to `item.lender ?? ''` and `emiStr` to `item.emiInr != null ? String(item.emiInr) : ''`.

    2. **Hint (PROP-03):** Below the toggle row, **before** `{hasLiability && (`, insert a paragraph with exact copy: `For loans not tied to a specific property (personal, car, etc.), use the ` then wrap **`Liabilities page`** in `<span className="font-medium text-foreground">` (inside the same `text-sm text-muted-foreground` paragraph per UI-SPEC). End with a period after the span.

    3. **Fields (PROP-01, PROP-02):** Inside the existing `{hasLiability && (` block, **after** the Outstanding loan field + helper, add:
       - **Lender:** `<Label htmlFor="property-lender">Lender</Label>`, `<Input id="property-lender" type="text" value={lenderStr} onChange=… placeholder="e.g. HDFC Bank" />`
       - **EMI:** `<Label htmlFor="property-emi">EMI (₹/month)</Label>`, input `type="text" inputMode="decimal"`, placeholder `e.g. 25,000`, helper `<p className="text-sm text-muted-foreground mt-1" id="emi-helper">` with short copy in the same tone as the outstanding-loan helper (e.g. monthly payment to the lender, not the builder).

    4. **Save (D-10):** When building `nextItem`, extend the `hasLiability` spread object:
       - Always include `outstandingLoanInr: roundCurrency(parseFinancialInput(loanStr))` when `hasLiability` (existing).
       - Add `...(lenderStr.trim() ? { lender: lenderStr.trim() } : {})`
       - Add `...(emiStr.trim() ? { emiInr: roundCurrency(parseFinancialInput(emiStr)) } : {})`
       Do **not** add `lender` or `emiInr` when `hasLiability` is false (omit entire liability financial fields as today).

    5. Do **not** add `react-router-dom`. Do **not** add `Link` to `/liabilities`.
  </action>
  <acceptance_criteria>
    - `grep -F "For loans not tied to a specific property" src/pages/PropertyPage.tsx` returns 1 match
    - `grep "font-medium text-foreground" src/pages/PropertyPage.tsx` returns at least 1 match
    - `grep "property-lender" src/pages/PropertyPage.tsx` returns at least 1 match
    - `grep "property-emi" src/pages/PropertyPage.tsx` returns at least 1 match
    - `grep "lenderStr" src/pages/PropertyPage.tsx` returns at least 2 matches
    - `grep "emiStr" src/pages/PropertyPage.tsx` returns at least 2 matches
    - `grep "react-router" src/pages/PropertyPage.tsx`; exit code 1 (no matches)
    - `npm test` exits 0
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `npm test` — full Vitest suite passes
2. `npx tsc -b --noEmit` — no TypeScript errors
3. Grep checks in task acceptance criteria
4. Manual spot-check (optional): open Property sheet — hint visible; toggle liability — Lender + EMI appear
</verification>

<success_criteria>
- PROP-01, PROP-02, PROP-03 satisfied per ROADMAP success criteria
- No React Router dependency introduced
- Optional fields persist only when liability toggle on and values provided (per spread rules)
</success_criteria>

<output>
After completion, create `.planning/phases/16-property-liability-enrichment/16-01-SUMMARY.md`
</output>

## PLANNING COMPLETE
