---
phase: 06-dark-mode
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01-PLAN.md
files_modified:
  - src/components/AppSidebar.tsx
autonomous: true
requirements:
  - DM-01
  - DM-02
must_haves:
  truths:
    - "Theme toggle in SidebarFooter; Sun/Moon; aria-labels per UI-SPEC; calls setTheme from useTheme"
    - "No raw white full-viewport in dark on all 9 surfaces after token audit or fixes"
  artifacts:
    - path: "src/components/AppSidebar.tsx"
      provides: "SidebarFooter, ThemeToggle, useTheme"
      contains: "SidebarFooter|useTheme|Moon|Sun"
  key_links:
    - from: "src/components/AppSidebar.tsx"
      to: "src/context/ThemeContext.tsx"
      via: "useTheme()"

---

<objective>
Add the **visible Sun/Moon theme control** in the sidebar **footer** (D-03, **DM-01**), wired to `useTheme` from plan 01 (**DM-02** sync on toggle). Use **lucide-react** `Sun` and `Moon`, shadcn **Button** `variant="ghost"`, **44px** min hit target, **aria-label** strings from **06-UI-SPEC**. Run a **source audit** (grep) for disallowed hardcoded light-only colors; fix if found so all **9 pages** meet success criterion 3.
</objective>

<threat_model>
**Scope:** UI + grep audit only.

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-06-02 | Inaccessible theme control (no name) | Medium | `aria-label` "Switch to dark mode" / "Switch to light mode" from UI-SPEC |
| T-06-03 | Primary button style steals focus hierarchy | Low | `variant="ghost"`, not `default` (UI-SPEC) |

**Block on:** high — N/A.
</threat_model>

<context>
@.planning/phases/06-dark-mode/06-UI-SPEC.md
@.planning/phases/06-dark-mode/06-CONTEXT.md
@.planning/phases/06-dark-mode/06-PATTERNS.md
@src/components/AppSidebar.tsx
@src/context/ThemeContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: SidebarFooter + theme toggle</name>
  <files>src/components/AppSidebar.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/components/AppSidebar.tsx
    - /Users/a.mahindrakar/personal/fin/src/context/ThemeContext.tsx
    - /Users/a.mahindrakar/personal/fin/.planning/phases/06-dark-mode/06-UI-SPEC.md
    - /Users/a.mahindrakar/personal/fin/src/components/ui/button.tsx
  </read_first>
  <action>
1. Import `SidebarFooter` from `@/components/ui/sidebar` (same file as `SidebarContent`).
2. Import `useTheme` from `@/context/ThemeContext'`.
3. Import `Button` from `@/components/ui/button'`.
4. Import `Sun`, `Moon` from `lucide-react'`.
5. After `&lt;/SidebarContent&gt;`, add `&lt;SidebarFooter className="p-4"&gt;` (or `px-4 py-3` to mirror header) containing a single `Button` with:
   - `variant="ghost"`
   - `className` including `min-h-[44px] min-w-[44px]` (or `w-full` row with icon + text if showing labels "Light" / "Dark" per UI-SPEC — both patterns allowed; if icon-only, `aria-label` is mandatory)
   - `onClick`: call `setTheme(current === 'light' ? 'dark' : 'light')` or `setTheme(theme === 'light' ? 'dark' : 'light')` using values from `useTheme()`
   - `aria-pressed` optional; **`aria-label`**: when `theme === 'light'` use exactly **`Switch to dark mode`**; when `theme === 'dark'` use exactly **`Switch to light mode`**
6. **Icon display:** show **Moon** when current theme is light (action goes to dark) and **Sun** when current theme is dark (action goes to light), or the inverse with labels — be consistent with common “click sun for light” mental model. UI-SPEC: Sun + Moon; typical pattern: **on light** show **Moon** icon (go dark) — if confusing, use icon matching **destination** (Moon icon + label toward dark) — **acceptance**: icon and `aria-label` agree.
7. Use `&lt;span className="sr-only"&gt;` for visible text if icon-only, duplicating the aria text, or show visible "Light" / "Dark" text next to icon per UI-SPEC.
  </action>
  <acceptance_criteria>
    - `grep -E "SidebarFooter|useTheme|setTheme" /Users/a.mahindrakar/personal/fin/src/components/AppSidebar.tsx` — all three substrings present in file
    - `grep "Switch to dark mode" /Users/a.mahindrakar/personal/fin/src/components/AppSidebar.tsx` returns a match
    - `grep "Switch to light mode" /Users/a.mahindrakar/personal/fin/src/components/AppSidebar.tsx` returns a match
    - `grep "variant=\"ghost\"" /Users/a.mahindrakar/personal/fin/src/components/AppSidebar.tsx` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Grep audit for light-only / gray raw tokens in src</name>
  <files>src/**/*.{tsx,ts,css}</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/index.css
    - /Users/a.mahindrakar/personal/fin/CLAUDE.md
  </read_first>
  <action>
1. From repo root, run: `rg "bg-white|#fff|#ffffff|#FFF|text-gray-[0-9]+|bg-gray-(50|100|200)|border-gray" src --glob '!**/*.test.*'` (or `grep -rE` if `rg` unavailable in executor env).
2. For **each** match: if it is a **necessary** exception (none expected), document in task notes; else replace with semantic `bg-background`, `bg-card`, `text-foreground`, `text-muted-foreground`, `border-border` as appropriate per file context.
3. Re-run until **zero** matches in `src` for the pattern above, **or** only matches inside comments with explanation.
4. If `src/index.css` alone defines `:root/.dark` — **do not** remove HSL values there.
  </action>
  <acceptance_criteria>
    - `cd /Users/a.mahindrakar/personal/fin && (rg "bg-white|#fff|#ffffff|text-gray-" src 2>/dev/null || true) | head -5` — output empty, OR executor documents explicit waivers
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Document manual 9-page checklist (UAT preflight)</name>
  <files>.planning/phases/06-dark-mode/06-02-PLAN.md</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/App.tsx
  </read_first>
  <action>
No file change required if audit clean. Executor adds one comment block to **UAT** file if exists, else skip — **or** the verifier runs manual pass: with dark mode on, visit **Dashboard, Gold, Mutual Funds, Stocks, Bitcoin, Property, Bank Savings, Retirement, Settings** — all readable.

For **planning completeness**, acceptance is: executor confirms in verification notes the **nine** `SectionKey` / dashboard route names from `App.tsx` were checked in dark.
  </action>
  <acceptance_criteria>
    - `grep "dashboard" /Users/a.mahindrakar/personal/fin/src/App.tsx` and nav keys — executor checklist covers `dashboard` + eight keys from `NAV_ITEMS` in `AppSidebar.tsx`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Run: `cd /Users/a.mahindrakar/personal/fin && npm run build` → exit 0
- Manual: Click toggle — theme flips, `localStorage.getItem('theme')` is `light` or `dark`
- Manual: Dark mode, hard reload — no long flash of all-light shell (D-06)
- Manual: All 9 routes readable in dark (contrast, no full white background)
</verification>

## PLANNING COMPLETE
