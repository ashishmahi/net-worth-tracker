---
phase: 13-commodities-product-ux
plan: 02
type: execute
wave: 2
depends_on: ['13-01-PLAN.md']
files_modified:
  - src/pages/DashboardPage.tsx
autonomous: true
requirements: [COM-04, COM-06]

must_haves:
  truths:
    - "Dashboard Commodities row navigates to the Commodities section, not Settings"
    - "Gold category totals and labels remain semantically unchanged (still Gold)"
    - "Optional dashboard copy/visual tweaks stay cosmetic for the Gold row"
  artifacts:
    - path: "src/pages/DashboardPage.tsx"
      provides: "NAV_KEY.otherCommodities → commodities; refined wayfinding copy if needed"
      contains: "otherCommodities: 'commodities'"
  key_links:
    - from: "DashboardPage NAV_KEY"
      to: "AppSidebar SectionKey"
      via: "must match registered section key"
      pattern: "commodities"
---

<objective>
Fix **Dashboard** navigation so the **Commodities** summary row opens the new **Commodities** section (COM-04). Optionally improve **exclusion / helper copy** when commodity totals are incomplete. Apply **cosmetic-only** adjustments to the **Gold** dashboard row so it visually pairs with the Commodities row (COM-06 — no semantic or calculation changes).

Output: Updated `DashboardPage.tsx`; `npm test` and `npx tsc -b --noEmit` green.
</objective>

<threat_model>
**Surface:** Local dashboard navigation and display strings only.

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Misleading financial totals | Low | No changes to `calcCategoryTotals` inputs or aggregation |

No HIGH findings.
</threat_model>

<execution_context>
@.planning/milestones/v1.4-phases/13-commodities-product-ux/13-CONTEXT.md
@.planning/milestones/v1.4-phases/13-commodities-product-ux/13-UI-SPEC.md
@.planning/milestones/v1.4-phases/13-commodities-product-ux/13-VALIDATION.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/components/AppSidebar.tsx
</context>

<tasks>

<task type="execute">
  <name>Task 1: Point NAV_KEY.otherCommodities at commodities section</name>
  <files>src/pages/DashboardPage.tsx</files>
  <read_first>
    - src/pages/DashboardPage.tsx (NAV_KEY, ROW_LABEL, exclusion note block ~209–215)
    - src/components/AppSidebar.tsx (confirm SectionKey includes `'commodities'`)
  </read_first>
  <action>
    1. In `NAV_KEY`, change `otherCommodities: 'settings'` to `otherCommodities: 'commodities'`.
    2. Update the Card exclusion helper text (lines referencing Settings-only guidance) so that when **Commodities** are excluded, users are directed to **Commodities** (manual items / holdings) and/or **Settings** / live rates as appropriate — keep one short sentence; must mention managing commodities via the **Commodities** section when `otherCommodities` appears in `excludedNames`.
    3. Optionally add a muted secondary line under the Commodities row label when both silver and manual items exist (e.g. counts or “includes silver & manual”) — skip if redundant with existing silver error line.
  </action>
  <acceptance_criteria>
    - `src/pages/DashboardPage.tsx` contains exactly `otherCommodities: 'commodities'` inside `NAV_KEY` (grep-able)
    - `src/pages/DashboardPage.tsx` does not contain `otherCommodities: 'settings'`
    - Exclusion note text includes substring `Commodities` (capital C) when referring to the section for commodity pricing/holdings (grep `Commodities` in the exclusion CardContent block)
    - `npx tsc -b --noEmit` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Gold row cosmetic alignment only</name>
  <files>src/pages/DashboardPage.tsx</files>
  <read_first>
    - src/pages/DashboardPage.tsx (category button map — `isGoldRow` branch ~287–297)
    - .planning/milestones/v1.4-phases/13-commodities-product-ux/13-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
    1. For `isGoldRow` only: adjust presentation — e.g. wrap label in `flex items-center gap-2` and add a small **lucide-react** icon (`Coins` or `CircleDot`) with `aria-hidden` next to the word **Gold**. Do not change `ROW_LABEL.gold`, `totals.gold`, `percentOfTotal`, or navigation target (`NAV_KEY.gold` stays `'gold'`).
    2. Do not alter commodity row math or gold price helper semantics beyond existing strings.
  </action>
  <acceptance_criteria>
    - `NAV_KEY.gold` remains `'gold'` in `DashboardPage.tsx`
    - Gold row render block imports an icon from `lucide-react` OR documents inline SVG — if using lucide, `DashboardPage.tsx` import line includes `Coins` or `CircleDot`
    - `calcCategoryTotals` is not edited in this task (grep `function calcCategoryTotals` only in `src/lib/dashboardCalcs.ts`)
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

## PLANNING COMPLETE
