---
phase: 30-bullion-import-uplift-settings-ux-disclosure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/bullionUpliftDisclosure.ts
  - src/components/settings/SettingsGoldPricingCard.tsx
  - src/components/settings/SettingsSilverPricingCard.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - BLN-04
must_haves:
  truths:
    - "Settings Gold and Silver pricing cards show read-only ballpark uplift (~10% / ~8%), one parity explanation line, and per-card footnote: approximate, not legal/tax/customs advice"
    - "No Settings UI edits uplift rates (D-01); disclosure strings centralized or duplicated consistently per 30-UI-SPEC"
    - "REQUIREMENTS.md BLN-04 or traceability note updated if product omits in-app tuning (D-02 tension addressed)"
  artifacts:
    - path: "src/lib/bullionUpliftDisclosure.ts"
      provides: "exported disclosure + educational copy constants"
    - path: "src/components/settings/SettingsGoldPricingCard.tsx"
      provides: "gold card uplift disclosure UI"
    - path: "src/components/settings/SettingsSilverPricingCard.tsx"
      provides: "silver card uplift disclosure UI"
  key_links:
    - from: "src/components/settings/SettingsGoldPricingCard.tsx"
      to: "src/lib/bullionUpliftDisclosure.ts"
      via: "import disclosure strings"
      pattern: "single source of truth for legal/educational copy"
---

<objective>
Implement **BLN-04**: Settings **`SettingsGoldPricingCard`** / **`SettingsSilverPricingCard`** show **read-only** ballpark uplift copy (~10% gold / ~8% silver), **one-line** parity explanation, and **per-card** approximation / **not legal or tax advice** footnotes — per [**30-CONTEXT.md**](30-CONTEXT.md) and [**30-UI-SPEC.md**](30-UI-SPEC.md). **No** uplift tuning controls.
</objective>

<threat_model>
**Assets:** User understanding of net-worth hints (trust).

| Threat | Mitigation |
|--------|------------|
| **T30-01** Copy implies customs/tax authority | Explicit **not legal, customs, or tax advice** wording on **both** cards (**BLN-04**). |
| **T30-02** Users believe uplift % is exact/personalized | **Ballpark** wording only (**D-03**); no live binding of `resolveGoldImportUpliftRate` decimals in UI. |

**Block severity:** High — none beyond accurate disclosure strings.

</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-bullion-import-uplift-settings-ux-disclosure/30-CONTEXT.md
@.planning/phases/30-bullion-import-uplift-settings-ux-disclosure/30-RESEARCH.md
@.planning/phases/30-bullion-import-uplift-settings-ux-disclosure/30-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>30-01-01 — Disclosure copy module + Settings cards (Wave 1)</name>
  <files>src/lib/bullionUpliftDisclosure.ts, src/components/settings/SettingsGoldPricingCard.tsx, src/components/settings/SettingsSilverPricingCard.tsx, .planning/REQUIREMENTS.md</files>
  <read_first>
    - src/components/settings/SettingsGoldPricingCard.tsx
    - src/components/settings/SettingsSilverPricingCard.tsx
    - .planning/phases/30-bullion-import-uplift-settings-ux-disclosure/30-CONTEXT.md
    - .planning/phases/30-bullion-import-uplift-settings-ux-disclosure/30-UI-SPEC.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    1. **Add `src/lib/bullionUpliftDisclosure.ts`** exporting string constants (names illustrative — adjust if preferred):
       - **`BULLION_UPLIFT_BALLPARK_GOLD`** — mentions ~**10%** import-style uplift as **illustrative default** (not editable).
       - **`BULLION_UPLIFT_BALLPARK_SILVER`** — mentions ~**8%** likewise.
       - **`BULLION_UPLIFT_PARITY_LINE`** — single sentence: uplift applied **on top of** ₹/g derived from **spot × USD→INR parity** (same export used on both cards or split into gold/silver variants if copy needs metal-specific grammar).
       - **`BULLION_UPLIFT_DISCLAIMER_FOOTNOTE`** — **approximate** figures; **not legal, customs, or tax advice** (verbatim substance required for acceptance).

    2. **`SettingsGoldPricingCard.tsx`** — Inside **`CardContent`**, **before** the closing `</CardContent>` (footnote **last** per UI-SPEC): render ballpark line + parity line + disclaimer using **`text-sm text-muted-foreground`** paragraphs. Do **not** add inputs for **`goldImportUpliftRate`**.

    3. **`SettingsSilverPricingCard.tsx`** — Mirror gold: silver ballpark constant + shared parity/disclaimer exports.

    4. **`.planning/REQUIREMENTS.md`** — Under **BLN-04** bullet or **Traceability** table: add a short clause that **in-app uplift tuning is optional** / shipped decision for **v2.2** is **disclosure-only** if product locks **no tuning UI** — so verification matches [**30-CONTEXT.md**](30-CONTEXT.md) **D-01/D-02** without contradicting living requirements.

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run && npx tsc -b --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/lib/bullionUpliftDisclosure.ts` exits 0
    - `grep -q "bullionUpliftDisclosure\\|BULLION_UPLIFT" src/components/settings/SettingsGoldPricingCard.tsx` exits 0
    - `grep -q "bullionUpliftDisclosure\\|BULLION_UPLIFT" src/components/settings/SettingsSilverPricingCard.tsx` exits 0
    - `grep -q "not legal\\|tax advice\\|legal.*tax" src/lib/bullionUpliftDisclosure.ts` exits 0
    - `grep -q "10%" src/lib/bullionUpliftDisclosure.ts` exits 0
    - `grep -q "8%" src/lib/bullionUpliftDisclosure.ts` exits 0
    - `grep -qi "BLN-04\\|disclosure\\|tuning\\|optional" .planning/REQUIREMENTS.md` exits 0
    - `npm test -- --run` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>BLN-04 Settings disclosure implemented + requirements trace updated</done>
</task>

</tasks>
