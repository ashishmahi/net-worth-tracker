---
phase: 19-crypto-utilities
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/cryptoUtils.ts
  - src/lib/__tests__/cryptoUtils.test.ts
autonomous: true
requirements:
  - ENC-02
  - ENC-03

must_haves:
  truths:
    - "encryptData returns EncryptedEnvelope with encrypted: true, version: 1, base64 salt, iv, and data"
    - "decryptData restores original plaintext on round-trip with same passphrase"
    - "decryptData throws CryptoError with code WRONG_PASSPHRASE when passphrase does not match"
    - "Vitest tests cover round-trip, successful decrypt, and wrong-passphrase rejection"
    - "npm test passes"
  artifacts:
    - path: "src/lib/cryptoUtils.ts"
      provides: "encryptData, decryptData, EncryptedEnvelope, CryptoError, isCryptoError"
      exports:
        [
          "encryptData",
          "decryptData",
          "isCryptoError",
          "EncryptedEnvelope",
          "CryptoError",
        ]
      min_lines: 80
    - path: "src/lib/__tests__/cryptoUtils.test.ts"
      provides: "Unit tests for ENC-02/ENC-03 success criteria"
      min_lines: 40
  key_links:
    - from: "src/lib/__tests__/cryptoUtils.test.ts"
      to: "src/lib/cryptoUtils.ts"
      via: "import encryptData, decryptData, isCryptoError"
      pattern: "from '@/lib/cryptoUtils'"
---

<objective>
Add a dependency-free `src/lib/cryptoUtils.ts` using the Web Crypto API (PBKDF2-SHA-256 at 600,000 iterations, AES-256-GCM) with `EncryptedEnvelope` as specified in `19-CONTEXT.md`, plus Vitest coverage for round-trip, correct decrypt, and wrong-passphrase `CryptoError`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-crypto-utilities/19-CONTEXT.md
@.planning/phases/19-crypto-utilities/19-RESEARCH.md

<interfaces>
Envelope (from CONTEXT D-06):

```typescript
export interface EncryptedEnvelope {
  encrypted: true
  version: 1
  salt: string // base64, 16-byte salt
  iv: string // base64, 12-byte IV for GCM
  data: string // base64 ciphertext (includes GCM tag)
}

export interface CryptoError extends Error {
  code: 'WRONG_PASSPHRASE'
}

export async function encryptData(
  plaintext: string,
  passphrase: string
): Promise<EncryptedEnvelope>

export async function decryptData(
  envelope: EncryptedEnvelope,
  passphrase: string
): Promise<string>

export function isCryptoError(e: unknown): e is CryptoError
```

PBKDF2: **600000** iterations, **SHA-256**, salt from envelope / random 16 bytes. AES-GCM **256-bit** key, **12-byte** IV.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for encrypt/decrypt contract</name>
  <files>src/lib/__tests__/cryptoUtils.test.ts, src/lib/cryptoUtils.ts</files>
  <read_first>
    - src/lib/__tests__/liabilityCalcs.test.ts (Vitest import style and structure)
    - .planning/phases/19-crypto-utilities/19-CONTEXT.md (D-01–D-10, envelope types)
    - vitest.config.ts (resolve alias `@/`)
  </read_first>
  <behavior>
    Three required behaviors per ROADMAP success criteria:
    1. Round-trip: `await decryptData(await encryptData(plain, pass), pass)` equals `plain`
    2. After encrypt, decrypt with same passphrase returns original string (may overlap with round-trip — keep explicit `it` for traceability)
    3. Wrong passphrase: `decryptData(envelope, 'wrong-passphrase')` rejects; caught error satisfies `isCryptoError(e)` and `e.code === 'WRONG_PASSPHRASE'`

    Additional assertions for ENC-03:
    - Result of `encryptData` has `encrypted === true`, `version === 1`, non-empty base64 strings for `salt`, `iv`, `data`
  </behavior>
  <action>
    1. Create `src/lib/cryptoUtils.ts` exporting types `EncryptedEnvelope`, `CryptoError`, functions `encryptData`, `decryptData`, `isCryptoError` — implementations **must throw `new Error('Not implemented')`** or equivalent stub so tests fail until Task 2.

    2. Implement `isCryptoError` fully (not stubbed): return `true` only when `e instanceof Error && 'code' in e && (e as CryptoError).code === 'WRONG_PASSPHRASE'` so the type guard is accurate for Phase 20.

    3. Create `src/lib/__tests__/cryptoUtils.test.ts`:
       - `import { describe, expect, it } from 'vitest'`
       - Import `encryptData`, `decryptData`, `isCryptoError` from `'@/lib/cryptoUtils'`
       - Use at least one shared passphrase constant e.g. `'test-passphrase-123'` and plaintext e.g. `'{"hello":"world"}'`
       - **Round-trip** `it`: call encrypt then decrypt, `expect(result).toBe(plaintext)`
       - **Correct passphrase** `it`: encrypt then decrypt with same passphrase, compare to plaintext
       - **Wrong passphrase** `it`: encrypt with passphrase A; call `decryptData(envelope, 'different-wrong-passphrase')` with `await expect(...).rejects.toThrow()` or try/catch; assert `isCryptoError(caught)` and `caught.code === 'WRONG_PASSPHRASE'`
       - **Envelope shape** `it`: after encrypt, assert `envelope.encrypted === true`, `envelope.version === 1`, and that `salt`, `iv`, `data` are non-empty strings

    4. Run `cd /Users/a.mahindrakar/personal/fin && npx vitest run src/lib/__tests__/cryptoUtils.test.ts` — tests that exercise encrypt/decrypt MUST fail (Not implemented). Tests that only check `isCryptoError` on a manually constructed `CryptoError` should not be used to fake green.

    5. Commit: `test(19-01): add failing cryptoUtils tests`
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx vitest run src/lib/__tests__/cryptoUtils.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/cryptoUtils.ts` exists with `export async function encryptData`, `export async function decryptData`, `export interface EncryptedEnvelope`, `export interface CryptoError`, `export function isCryptoError`
    - `grep -c "it(" src/lib/__tests__/cryptoUtils.test.ts` returns at least 4
    - `grep "WRONG_PASSPHRASE" src/lib/__tests__/cryptoUtils.test.ts` returns at least 1 match
    - `grep "encryptData" src/lib/__tests__/cryptoUtils.test.ts` returns at least 1 match
    - `grep "decryptData" src/lib/__tests__/cryptoUtils.test.ts` returns at least 1 match
    - `npx vitest run src/lib/__tests__/cryptoUtils.test.ts` exits non-zero OR shows failing tests for encrypt/decrypt (RED phase)
  </acceptance_criteria>
  <done>Stub module throws not-implemented for encrypt/decrypt; test file has round-trip, correct-passphrase, wrong-passphrase, and envelope-shape coverage; suite fails until Task 2</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement Web Crypto encrypt/decrypt</name>
  <files>src/lib/cryptoUtils.ts</files>
  <read_first>
    - src/lib/cryptoUtils.ts (current stub)
    - src/lib/__tests__/cryptoUtils.test.ts (tests to satisfy)
    - .planning/phases/19-crypto-utilities/19-RESEARCH.md (deriveKey/encrypt/decrypt steps)
  </read_first>
  <action>
    Replace stubs with full implementation:

    **Helpers:** Implement `bytesToBase64(bytes: Uint8Array): string` and `base64ToBytes(b64: string): Uint8Array` without new dependencies (use `btoa`/`atob` + binary string mapping, or standard chunking — keep UTF-8 safe for base64 binary).

    **deriveKey(passphrase: string, salt: Uint8Array): Promise<CryptoKey>**
    - `const enc = new TextEncoder()`
    - `const keyMaterial = await crypto.subtle.importKey('raw', enc.encode(passphrase), 'PBKDF2', false, ['deriveKey'])`
    - `return crypto.subtle.deriveKey({ name: 'PBKDF2', salt, iterations: 600000, hash: 'SHA-256' }, keyMaterial, { name: 'AES-GCM', length: 256 }, false, ['encrypt', 'decrypt'])`

    **encryptData:**
    - Generate `salt = new Uint8Array(16)` and `iv = new Uint8Array(12)`; `crypto.getRandomValues(salt); crypto.getRandomValues(iv);`
    - `const key = await deriveKey(passphrase, salt)`
    - `const ciphertext = await crypto.subtle.encrypt({ name: 'AES-GCM', iv }, key, enc.encode(plaintext))`
    - Return `{ encrypted: true, version: 1, salt: bytesToBase64(salt), iv: bytesToBase64(iv), data: bytesToBase64(new Uint8Array(ciphertext)) }`

    **decryptData:**
    - Decode salt, iv, data from base64; validate `envelope.encrypted === true` and `envelope.version === 1` (throw `CryptoError` if malformed envelope)
    - `const key = await deriveKey(passphrase, salt)`
    - Try `await crypto.subtle.decrypt({ name: 'AES-GCM', iv }, key, ciphertextBytes)`; on success return `new TextDecoder().decode(result)`
    - On failure (e.g. `DOMException` or any decrypt error): throw **`Object.assign(new Error('Wrong passphrase or corrupted data'), { code: 'WRONG_PASSPHRASE' as const })`** or `class` extending `Error` with `code` so `isCryptoError` passes

    Do **not** `console.log` passphrase or derived keys.

    Run `npx vitest run src/lib/__tests__/cryptoUtils.test.ts` then `npm test`. Commit: `feat(19-01): implement cryptoUtils AES-GCM + PBKDF2`
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx vitest run src/lib/__tests__/cryptoUtils.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep "600000" src/lib/cryptoUtils.ts` returns at least 1 match (PBKDF2 iterations)
    - `grep "AES-GCM" src/lib/cryptoUtils.ts` returns at least 2 matches (encrypt + decrypt paths)
    - `grep "PBKDF2" src/lib/cryptoUtils.ts` returns at least 1 match
    - `grep "deriveKey" src/lib/cryptoUtils.ts` returns at least 1 match
    - `grep "console.log" src/lib/cryptoUtils.ts` returns 0 matches
    - `npx vitest run src/lib/__tests__/cryptoUtils.test.ts` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>All crypto tests green; full suite green; envelope matches ENC-03; wrong passphrase yields CryptoError with WRONG_PASSPHRASE</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- **Inputs:** Passphrase and plaintext stay in-memory in the browser (or Node during tests). No network I/O in this module.
- **Outputs:** `EncryptedEnvelope` is intended for JSON serialization in Phase 20 — ciphertext integrity is enforced by AES-GCM.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information disclosure | Passphrase / key material | mitigate | No logging of passphrase or keys; review `grep console` in review |
| T-19-02 | Tampering | Ciphertext or envelope fields | mitigate | AES-GCM authentication; decrypt failure maps to `WRONG_PASSPHRASE` per product decision |
| T-19-03 | Denial of service | Very large plaintext | accept | Local-only app; no explicit size cap in Phase 19 (same as plain JSON export limits) |
</threat_model>

<verification>
1. `npx vitest run src/lib/__tests__/cryptoUtils.test.ts` — all tests pass
2. `npm test` — full suite passes
3. `grep -E "export (async )?function (encryptData|decryptData)|export function isCryptoError" src/lib/cryptoUtils.ts` — all present
4. ROADMAP success criteria 1–4 satisfied by tests and implementation
</verification>

<success_criteria>
- `encryptData` / `decryptData` match CONTEXT and ENC-02 / ENC-03
- Typed wrong-passphrase error with `isCryptoError`
- No new npm dependencies
- All Vitest tests pass; `npm test` passes
</success_criteria>

<output>
After completion, create `.planning/phases/19-crypto-utilities/19-01-SUMMARY.md`
</output>
