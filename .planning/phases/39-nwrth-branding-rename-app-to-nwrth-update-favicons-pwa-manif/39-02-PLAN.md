---
phase: 39-nwrth-branding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AppSidebar.tsx
  - src/pages/SettingsPage.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Sidebar displays 3-bar ascending SVG icon in a gradient container, not the old 'W' square"
    - "Sidebar brand text reads 'nwrth' (lowercase), not 'Wealth Tracker'"
    - "Sidebar subtitle 'Net worth . local only' is unchanged"
    - "Export download filename is nwrth-YYYY-MM-DD.zip"
    - "No UI-visible 'Wealth Tracker' string remains anywhere in src/"
  artifacts:
    - path: "src/components/AppSidebar.tsx"
      provides: "nwrth brand mark in sidebar header"
      contains: "nwrth"
    - path: "src/pages/SettingsPage.tsx"
      provides: "nwrth export filename"
      contains: "nwrth-"
  key_links:
    - from: "src/components/AppSidebar.tsx"
      to: "CSS variables"
      via: "gradient background using var(--accent)"
      pattern: "var\\(--accent\\)"
---

<objective>
Update React components to display the nwrth brand: sidebar brand mark SVG + name, and export filename.

Purpose: Replace all UI-visible "Wealth Tracker" references with the nwrth brand identity in React component code.
Output: Updated AppSidebar.tsx with SVG brand mark and "nwrth" text; updated SettingsPage.tsx with nwrth export filename.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-nwrth-branding-rename-app-to-nwrth-update-favicons-pwa-manif/39-CONTEXT.md
@.planning/phases/39-nwrth-branding-rename-app-to-nwrth-update-favicons-pwa-manif/39-UI-SPEC.md
@NWRTH-BRANDING-PROMPT.md

<interfaces>
<!-- Current AppSidebar.tsx sidebar header structure (lines 104-121) that will be replaced -->
From src/components/AppSidebar.tsx:
```tsx
<SidebarHeader className="gap-0 border-b border-sidebar-border px-[22px] pb-[18px] pt-[22px]">
  <div className="flex items-center gap-2.5">
    <div
      className="grid size-7 shrink-0 place-items-center rounded-lg bg-gradient-to-br from-primary to-[hsl(var(--studio-saffron))] text-sm font-semibold text-primary-foreground shadow-sm"
      aria-hidden
    >
      W
    </div>
    <div className="min-w-0">
      <div className="truncate text-[15px] font-semibold leading-tight tracking-tight text-sidebar-foreground">
        Wealth Tracker
      </div>
      <div className="mt-0.5 text-[11px] text-muted-foreground">
        Net worth · local only
      </div>
    </div>
  </div>
</SidebarHeader>
```

From src/pages/SettingsPage.tsx (line 199):
```tsx
a.download = `wealth-tracker-${new Date().toISOString().slice(0, 10)}.zip`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace sidebar brand mark and name</name>
  <files>src/components/AppSidebar.tsx</files>
  <read_first>
    src/components/AppSidebar.tsx
    NWRTH-BRANDING-PROMPT.md
    .planning/phases/39-nwrth-branding-rename-app-to-nwrth-update-favicons-pwa-manif/39-UI-SPEC.md
  </read_first>
  <action>
In `src/components/AppSidebar.tsx`, replace the brand mark container (the `<div>` containing "W" at lines 106-111) with the nwrth SVG brand mark. The `<SidebarHeader>` wrapper and the text container `<div className="min-w-0">` stay structurally the same.

**Replace the "W" div (lines 106-111) with this exact JSX (per D-01):**

```tsx
<div
  className="grid size-7 shrink-0 place-items-center rounded-[8px] shadow-sm"
  style={{
    background: 'linear-gradient(135deg, var(--accent), oklch(0.55 0.18 270))',
    color: 'var(--accent-fg)',
  }}
  aria-hidden
>
  <svg viewBox="0 0 28 28" fill="none" xmlns="http://www.w3.org/2000/svg" className="size-4">
    <rect x="5" y="16" width="4" height="8" rx="1" fill="currentColor" opacity="0.4" />
    <rect x="12" y="10" width="4" height="14" rx="1" fill="currentColor" opacity="0.7" />
    <rect x="19" y="4" width="4" height="20" rx="1" fill="currentColor" />
  </svg>
</div>
```

Key changes from old to new:
- `rounded-lg` changes to `rounded-[8px]` (per D-01 spec: border-radius 8px)
- `bg-gradient-to-br from-primary to-[hsl(var(--studio-saffron))]` removed; replaced with inline `style` using `linear-gradient(135deg, var(--accent), oklch(0.55 0.18 270))` (Tailwind cannot do oklch in gradient utilities)
- `text-sm font-semibold text-primary-foreground` removed (no longer text content)
- `color: var(--accent-fg)` set via inline style so SVG inherits via `currentColor`
- The "W" text replaced with the inline SVG at `className="size-4"` (16x16 inside 28x28 container)

**Replace "Wealth Tracker" text (line 114) with "nwrth" (per D-02):**

Change:
```tsx
Wealth Tracker
```
To:
```tsx
nwrth
```

The subtitle "Net worth . local only" on line 116-117 stays UNCHANGED (per D-03).

Do NOT change:
- The `<SidebarHeader>` className (keep `px-[22px] pb-[18px] pt-[22px]`)
- The text container structure or classes
- The subtitle text or classes
- Any other part of AppSidebar.tsx
  </action>
  <verify>
    <automated>grep -c 'nwrth' src/components/AppSidebar.tsx | grep -q '[1-9]' && grep -c 'Wealth Tracker' src/components/AppSidebar.tsx | grep -q '^0$' && grep -c 'viewBox="0 0 28 28"' src/components/AppSidebar.tsx | grep -q '^1$' && grep -c 'oklch' src/components/AppSidebar.tsx | grep -q '^1$' && grep -c 'Net worth' src/components/AppSidebar.tsx | grep -q '^1$' && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'Wealth Tracker' src/components/AppSidebar.tsx` returns NO matches
    - `grep 'nwrth' src/components/AppSidebar.tsx` returns at least 1 match (the brand name)
    - `grep 'viewBox="0 0 28 28"' src/components/AppSidebar.tsx` returns exactly 1 match (inline SVG)
    - `grep 'oklch(0.55 0.18 270)' src/components/AppSidebar.tsx` returns exactly 1 match (gradient)
    - `grep "var(--accent-fg)" src/components/AppSidebar.tsx` returns at least 1 match (icon color)
    - `grep "var(--accent)" src/components/AppSidebar.tsx` returns at least 1 match (gradient start)
    - `grep 'Net worth' src/components/AppSidebar.tsx` returns exactly 1 match (subtitle preserved)
    - `grep 'rounded-\[8px\]' src/components/AppSidebar.tsx` returns exactly 1 match
    - The old "W" text content no longer appears as brand mark content
  </acceptance_criteria>
  <done>Sidebar brand mark shows 3-bar ascending SVG in gradient container. Brand text reads "nwrth" lowercase. Subtitle unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update export filename and verify no remaining UI strings</name>
  <files>src/pages/SettingsPage.tsx</files>
  <read_first>
    src/pages/SettingsPage.tsx
    src/context/AppDataContext.tsx
  </read_first>
  <action>
**Step 1 -- Update export filename (per D-04):**

In `src/pages/SettingsPage.tsx` at line 199, change:
```tsx
a.download = `wealth-tracker-${new Date().toISOString().slice(0, 10)}.zip`
```
To:
```tsx
a.download = `nwrth-${new Date().toISOString().slice(0, 10)}.zip`
```

**Step 2 -- Verify no remaining UI-visible "Wealth Tracker" in src/ (per D-11):**

Run `grep -rn "Wealth Tracker" src/` to find any remaining occurrences. Expected results:
- `src/styles/studio-theme.css` line 2: This is a CSS comment (`/* Studio design system -- Claude Design handoff (Wealth Tracker.html). */`) -- NOT UI-visible. Leave it unchanged.
- There should be zero other matches after Plan 02 Task 1 updated AppSidebar.tsx.

If any OTHER UI-visible occurrences are found (in JSX, string literals rendered to DOM), replace them with "nwrth" (lowercase). Do NOT change:
- CSS comments
- Internal variable names or function names
- `WEALTH_STORAGE_KEY` in AppDataContext.tsx (per D-05)
- TypeScript identifiers
  </action>
  <verify>
    <automated>grep -c 'nwrth-' src/pages/SettingsPage.tsx | grep -q '[1-9]' && grep -rn "Wealth Tracker" src/ | grep -v '\.css:' | grep -v 'WEALTH_STORAGE_KEY' | grep -c '.' | grep -q '^0$' && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'nwrth-' src/pages/SettingsPage.tsx` matches (the download filename)
    - `grep 'wealth-tracker-.*\.zip' src/pages/SettingsPage.tsx` returns NO matches (old filename gone)
    - `grep -rn "Wealth Tracker" src/ | grep -v '\.css:'` returns NO matches (no UI-visible strings remain)
    - `grep 'WEALTH_STORAGE_KEY' src/context/AppDataContext.tsx` still returns a match (key preserved per D-05)
  </acceptance_criteria>
  <done>Export filename is nwrth-YYYY-MM-DD.zip. Zero UI-visible "Wealth Tracker" strings remain in src/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sidebar brand mark | Inline SVG with hardcoded geometry -- no user input |
| Export filename | String template with Date output -- no user input in filename |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04 | Tampering | Inline SVG in AppSidebar.tsx | accept | SVG is hardcoded JSX with no dynamic attributes. Three static `<rect>` elements with fixed coordinates. No user input flows into the SVG. |
| T-39-05 | Information Disclosure | Export filename | accept | Filename contains only the brand name "nwrth" and a date. No sensitive data. Same pattern as before with "wealth-tracker". |
</threat_model>

<verification>
1. `npm run dev` starts without errors
2. Sidebar shows 3-bar ascending icon with gradient background, "nwrth" text, unchanged subtitle
3. Icon adapts correctly across all 3 themes (Ledger, Vault, Studio) in both light and dark modes
4. Settings > Export produces a file named `nwrth-YYYY-MM-DD.zip`
5. `grep -rn "Wealth Tracker" src/ | grep -v '\.css:'` returns zero results
</verification>

<success_criteria>
- AppSidebar.tsx renders inline 3-bar SVG in gradient container (not "W")
- AppSidebar.tsx brand text is "nwrth" (lowercase)
- AppSidebar.tsx subtitle "Net worth . local only" is preserved
- SettingsPage.tsx export filename is nwrth-YYYY-MM-DD.zip
- No UI-visible "Wealth Tracker" strings remain in src/
- AppDataContext.tsx WEALTH_STORAGE_KEY is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/39-nwrth-branding-rename-app-to-nwrth-update-favicons-pwa-manif/39-02-SUMMARY.md`
</output>
