---
phase: 03-live-prices-bitcoin
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/pages/SettingsPage.tsx
  - src/pages/BitcoinPage.tsx
autonomous: true
requirements:
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09

must_haves:
  truths:
    - "Settings shows live rates block and session-only override subsection; session values not written to saveData / data.json"
    - "Bitcoin page uses inline RHF for quantity; shows INR and USD of holding per D-08 when data available or session overrides"
    - "npx tsc --noEmit exits 0"
  artifacts:
    - path: "src/pages/SettingsPage.tsx"
      provides: "Live market rates + session-only inputs wired to useLivePrices"
      contains: "useLivePrices"
    - path: "src/pages/BitcoinPage.tsx"
      provides: "Full bitcoin section replacing stub"
      contains: "parseFinancialInput", "useAppData"
  key_links:
    - from: "src/pages/BitcoinPage.tsx"
      to: "src/context/LivePricesContext.tsx"
      via: "useLivePrices"
      pattern: "btcUsd|effective"
---

<objective>
Extend **SettingsPage** with a read-only **live market rates** block and a **session-only** manual override subsection (D-05, D-06) wired to `useLivePrices()`. Replace **BitcoinPage** stub with an **inline** form (D-07) like Retirement, persisting `data.assets.bitcoin.quantity`, and show **INR** and **USD** value of the holding (D-08) with **loading** UI per D-09. No changes to `data.json` shape for these features except Bitcoin quantity already in schema.
</objective>

<threat_model>
| ID | Threat | Mitigation |
|----|--------|------------|
| T-03-04 | Session-only rates confused with saved settings | UI copy + only call `setSession*` on LivePrices context, never `saveData` for rates |
| T-03-05 | XSRF | N/A — no cookies / local app |

</threat_model>

<context>
@/Users/a.mahindrakar/personal/fin/.planning/phases/03-live-prices-bitcoin/03-CONTEXT.md
@/Users/a.mahindrakar/personal/fin/.planning/phases/03-live-prices-bitcoin/03-UI-SPEC.md
@/Users/a.mahindrakar/personal/fin/src/pages/RetirementPage.tsx
@/Users/a.mahindrakar/personal/fin/src/pages/SettingsPage.tsx
@/Users/a.mahindrakar/personal/fin/src/pages/BitcoinPage.tsx
@/Users/a.mahindrakar/personal/fin/src/lib/financials.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings — live rates + session overrides</name>
  <files>src/pages/SettingsPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/pages/SettingsPage.tsx
    - /Users/a.mahindrakar/personal/fin/.planning/phases/03-live-prices-bitcoin/03-UI-SPEC.md — Settings
    - /Users/a.mahindrakar/personal/fin/src/context/LivePricesContext.tsx
  </read_first>
  <action>
Add a new **Card** section (or equivalent) below existing content with: (1) block title for live market rates; (2) read-only display of effective USD/INR, AED/INR, and BTC/USD with loading/error from `useLivePrices`; (3) a clearly labeled **Session only** subsection with text inputs (string fields, `inputMode="decimal"`) for manual USD/INR, AED/INR, and BTC/USD that **only** update LivePrices context state, **not** `saveData`. Add helper text that session values are lost on full page reload. Use `toLocaleString('en-IN')` for INR-quoted numbers per existing project rules.
  </action>
  <acceptance_criteria>
    - `src/pages/SettingsPage.tsx` imports `useLivePrices` from the LivePrices context module
    - `grep -n "saveData" src/pages/SettingsPage.tsx` shows no new call path that serializes session rate fields to `data.json` (session rates must not appear in `JSON.stringify` for export — `handleExport` must still use `data` from `useAppData` only)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Bitcoin page — inline form + computed INR/USD</name>
  <files>src/pages/BitcoinPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/pages/RetirementPage.tsx
    - /Users/a.mahindrakar/personal/fin/src/pages/BitcoinPage.tsx
    - /Users/a.mahindrakar/personal/fin/.planning/phases/03-live-prices-bitcoin/03-UI-SPEC.md — Bitcoin
  </read_first>
  <action>
Replace the Bitcoin stub with a full page: RHF + zod string schema for **quantity (BTC)**; on submit, `parseFinancialInput` → `roundCurrency` on derived display only, persist `data.assets.bitcoin` via `saveData` with `nowIso()`. **Display** effective BTC/USD from `useLivePrices`, compute **INR** = `quantity * btcUsd * usdInr` (using **effective** rates from the hook) and **USD value** = `quantity * btcUsd`; show D-09 loading state next to readouts. Match Retirement patterns for save error and **Save** button.
  </action>
  <acceptance_criteria>
    - `src/pages/BitcoinPage.tsx` contains `useLivePrices` and `useAppData`
    - `src/pages/BitcoinPage.tsx` contains `parseFinancialInput` and `zodResolver`
    - `src/pages/BitcoinPage.tsx` does **not** contain the substring `fetch(`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` → 0
- Manual: On Settings, type session-only rates → reload page → they **clear** (session)
- Manual: Set bitcoin quantity with dev offline → session rates allow showing numbers or clear error per D-04
</verification>

## PLANNING COMPLETE
