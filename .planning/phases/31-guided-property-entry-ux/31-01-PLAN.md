---
phase: 31-guided-property-entry-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/propertyEntryPath.ts
  - src/lib/propertyEntryPath.test.ts
  - src/pages/PropertyPage.tsx
autonomous: true
requirements:
  - PRP-01
  - PRP-02
  - PRP-03
must_haves:
  truths:
    - "Property add/edit opens with a three-option path control (Fully paid / Milestones / Mortgaged) before name and agreement fields"
    - "Fully paid path hides milestones block and home-loan switch per CONTEXT D-05/D-06; Mortgaged/Milestones follow D-07 ordering"
    - "Changing path clears draft fields from the path being left per D-03 with one consistent confirm-or-immediate rule"
    - "Sheet subtitle is path-neutral (D-09); new helper copy uses second-person voice (D-12) and light net-worth hints (D-10/D-11)"
    - "PRP-03: persist entryKind only if implemented — otherwise inference documented in RESEARCH and no PropertyItem schema change"
  artifacts:
    - path: "src/lib/propertyEntryPath.ts"
      provides: "Entry path union + inferFromPropertyItem + field-reset helpers + exported labels"
    - path: "src/pages/PropertyPage.tsx"
      provides: "Segmented path UI, conditional sections, updated copy"
  key_links:
    - from: "src/pages/PropertyPage.tsx"
      to: "src/lib/propertyEntryPath.ts"
      via: "import path inference and reset helpers"
      pattern: "pure logic in lib; UI stays in page"
---

<objective>
Deliver **Phase 31** guided property entry: **PRP-01** path selection, **PRP-02** conditional sections + helper copy, **PRP-03** explicit persistence only if needed — per [**31-CONTEXT.md**](31-CONTEXT.md), [**31-UI-SPEC.md**](31-UI-SPEC.md), and [**31-RESEARCH.md**](31-RESEARCH.md). Primary implementation in **`PropertyPage`** with optional **`propertyEntryPath`** helpers tested by **Vitest**.
</objective>

<threat_model>
**Assets:** User trust in property equity / loan copy (financial misleading).

| Threat | Mitigation |
|--------|------------|
| **T-31-01** Helper text contradicts dashboard equity math | Copy reviewed against **`dashboardCalcs`** intent — **no new formulas** in Phase 31 (**CONTEXT**). |
| **T-31-02** Path switch silently drops data without user understanding | **D-03**: consistent **immediate clear** or **confirm dialog** — document in task and test manually. |

**Block severity:** Medium — copy accuracy; path-reset transparency.

</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-guided-property-entry-ux/31-CONTEXT.md
@.planning/phases/31-guided-property-entry-ux/31-RESEARCH.md
@.planning/phases/31-guided-property-entry-ux/31-UI-SPEC.md
@.planning/phases/31-guided-property-entry-ux/31-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>31-01-01 — propertyEntryPath helpers + Vitest</name>
  <files>src/lib/propertyEntryPath.ts, src/lib/propertyEntryPath.test.ts</files>
  <read_first>
    - src/types/data.ts
    - .planning/phases/31-guided-property-entry-ux/31-RESEARCH.md
    - .planning/phases/31-guided-property-entry-ux/31-CONTEXT.md
  </read_first>
  <action>
    1. **Add `src/lib/propertyEntryPath.ts`** exporting:
       - **`export type PropertyEntryPath = 'fullyPaid' | 'milestones' | 'mortgaged'`**
       - **`PATH_LABELS`**: record mapping each key to display strings **Fully paid** / **Milestones** / **Mortgaged** (exact casing per **D-04** medium labels).
       - **`inferEntryPathFromPropertyItem(item: PropertyItem): PropertyEntryPath`** implementing **31-RESEARCH** order: `hasLiability` → **mortgaged**; else `milestones.length > 0` → **milestones**; else **fullyPaid**.
       - **`getDraftFieldsToReset(previous: PropertyEntryPath, next: PropertyEntryPath): Set&lt;'milestones' | 'liability'&gt;`** (or equivalent explicit structure) listing which **draft slices** to clear when leaving a path — **milestones** means clear milestone drafts + paid summaries dependency; **liability** means clear `hasLiability`, loan, lender, EMI strings.

    2. **Add `src/lib/propertyEntryPath.test.ts`** with cases:
       - Liability true → **mortgaged** regardless of milestones length.
       - No liability, milestones non-empty → **milestones**.
       - No liability, milestones empty → **fullyPaid**.
       - Reset set includes expected keys for at least **fullyPaid → mortgaged** and **mortgaged → fullyPaid** transitions.

    3. **Do not** add **`entryKind`** to **`PropertyItem`** in this task unless you are also implementing persistence in **31-01-02** per **D-14** (default: **skip** schema).

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run src/lib/propertyEntryPath.test.ts && npx tsc -b --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/lib/propertyEntryPath.ts` exits 0
    - `test -f src/lib/propertyEntryPath.test.ts` exits 0
    - `grep -q "inferEntryPathFromPropertyItem" src/lib/propertyEntryPath.ts` exits 0
    - `grep -q "fullyPaid\\|mortgaged\\|milestones" src/lib/propertyEntryPath.ts` exits 0
    - `npm test -- --run src/lib/propertyEntryPath.test.ts` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>Path inference + reset helpers extracted and tested</done>
</task>

<task type="auto">
  <name>31-01-02 — PropertyPage path UI + conditional layout</name>
  <files>src/pages/PropertyPage.tsx</files>
  <read_first>
    - src/pages/PropertyPage.tsx
    - src/lib/propertyEntryPath.ts
    - .planning/phases/31-guided-property-entry-ux/31-UI-SPEC.md
    - .planning/phases/31-guided-property-entry-ux/31-CONTEXT.md
  </read_first>
  <action>
    1. **State:** Add **`entryPath: PropertyEntryPath`** to sheet state. On **`openAdd`**, initialize to **`fullyPaid`** (or **milestones** if you document why — **RESEARCH** default is fully paid). On **`openEdit`**, set **`entryPath`** from **`inferEntryPathFromPropertyItem(item)`**.

    2. **Segmented control:** Render three segments **first** in the scroll body (**D-02**), wired with **`setEntryPath`**. Use **`PATH_LABELS`** for visible text. Implement **keyboard + radiogroup** (or **tablist**) semantics per **UI-SPEC**.

    3. **Path change handler:** When **`entryPath`** changes, apply **`getDraftFieldsToReset`** (or equivalent) to clear milestone rows / loan fields / **`hasLiability`** per **D-03**. Choose **immediate reset** OR **`window.confirm`** before destructive clear — **one pattern for Add + Edit** (CONTEXT discretion).

    4. **Conditional UI:**
       - **`fullyPaid`:** Do **not** render milestones block (table, add row, summaries, exceed warning area) or liability switch + loan fields (**D-05**, **D-06**).
       - **`milestones`:** Show milestones block; show liability section **below** milestones block **unless** **D-07** requires loan-before-milestones when **both** matter — follow **UI-SPEC**: when loan + milestones, **loan block before milestones** (**D-07**).
       - **`mortgaged`:** Show liability switch ON by default for **Add** (or set **`hasLiability`** true when entering path); show loan fields when switch on; apply **D-07** ordering vs milestones.

    5. **Submit payload:** When saving from **fullyPaid**, persist **`milestones: []`**, **`hasLiability: false`**, and omit optional loan keys (same as today’s object shape). When saving from **milestones** without loan, match existing behavior. Ensure **no** partial stale milestone rows remain in state after path switches.

    6. **Copy:** Replace sheet subtitle (**`property-form-desc`**) with **one neutral line** (**D-09**). Add **1–2** second-person helper lines at anchor points per **D-10–D-12** (agreement, loan, paid-to-builder summary) without duplicating boilerplate.

    7. **PRP-03:** **Default:** **no** **`entryKind`** field. Only add optional **`entryKind?: PropertyEntryPath`** to **`PropertyItem`** + **`PropertyItemSchema`** in **`src/types/data.ts`** **and** persist in **`nextItem`** if manual testing proves inference fails — if added, default missing keys on load to **`inferEntryPathFromPropertyItem`** for display only.

  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run && npx tsc -b --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "entryPath\\|PropertyEntryPath\\|inferEntryPathFromPropertyItem" src/pages/PropertyPage.tsx` exits 0
    - `grep -q "propertyEntryPath" src/pages/PropertyPage.tsx` exits 0
    - `grep -q "Fully paid\\|Milestones\\|Mortgaged\\|PATH_LABELS" src/pages/PropertyPage.tsx` exits 0
    - `npm test -- --run` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>Property sheet matches guided path UX + CONTEXT visibility rules</done>
</task>

</tasks>
