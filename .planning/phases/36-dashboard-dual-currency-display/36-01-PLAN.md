---
phase: 36-dashboard-dual-currency-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dashboardCalcs.ts
  - src/pages/DashboardPage.tsx
  - src/lib/__tests__/dashboardBreakdown.test.ts
autonomous: true
requirements:
  - DSP-01
  - DSP-03
must_haves:
  truths:
    - Breakdown category rows only show dual stacks; hero Gross/Debt/Net worth strips and Total Debt breakdown footer stay single-line per 36-CONTEXT D-01
    - "Effective currency: stored record currency ?? settings.reportingCurrency ?? 'INR' (match migration / DM-01 intent)"
    - "D-02 secondary: omit when zero distinct non-reporting codes; single aggregated original line when exactly one; omit when two+ distinct non-reporting codes"
    - "D-03 primary text-sm font-semibold tabular-nums; secondary text-[11px] or text-xs text-muted-foreground tabular-nums; gap≈gap-px; preserve mobile secondary per 36-CONTEXT specifics"
    - "Degraded: visible copy uses exact substring Rate unavailable when conversion blocked (FX-03)"
    - "calcCategoryTotals INR hub sums per-record stored currency via toReportingCurrency(amount,stored,'INR',snapshot) before roundCurrency aggregation for MF Stocks Bank Property where applicable — bank supports all six CurrencyCode legs not only AED"
    - "DSP-03 when every contributor effective currency equals reportingCurrency hide secondary entirely"
  artifacts:
    - path: "src/lib/dashboardCalcs.ts"
      provides: "Accurate INR hub category totals + breakdown original-line metadata for D-02"
    - path: "src/pages/DashboardPage.tsx"
      provides: "Breakdown row JSX vertical stack for primary + optional secondary"
    - path: "src/lib/__tests__/dashboardBreakdown.test.ts"
      provides: "Regression tests for D-02/D-03/DSP-03 happy paths"
  key_links:
    - from: "src/lib/currencyConversion.ts"
      to: "src/lib/dashboardCalcs.ts"
      via: "toReportingCurrency hub conversions"
      pattern: "ForexRateSnapshot"
---

<objective>
Ship **Phase 36** (**DSP-01**, **DSP-03**): **Breakdown** category rows show **reporting-currency primary** with **optional muted original subline** per **`36-CONTEXT.md` D-01–D-04** and **`36-UI-SPEC.md`**, backed by **honest INR hub totals** from **`calcCategoryTotals`** and **Vitest** coverage.
</objective>

<threat_model>
**Assets:** Local wealth numbers; user trust in dashboard correctness.

| Threat | Mitigation |
|--------|------------|
| **T-36-01** | Primary and secondary disagree | **Single source** walk for per-currency buckets + INR hub sum; **D-02** hides secondary when ambiguous |
| **T-36-02** | Missing FX shows wrong reporting amount | Reuse **`toReportingCurrency`** **`ok: false`** → **Rate unavailable** + **D-04** fallback path; **no invented rates** |

**Block severity:** **T-36-02** **medium** (correctness).

</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/36-dashboard-dual-currency-display/36-CONTEXT.md
@.planning/phases/36-dashboard-dual-currency-display/36-RESEARCH.md
@.planning/phases/36-dashboard-dual-currency-display/36-UI-SPEC.md
@docs/multi-currency.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>36-01-01 — INR hub totals + breakdown metadata</name>
  <files>src/lib/dashboardCalcs.ts</files>
  <read_first>
    - src/lib/dashboardCalcs.ts
    - src/lib/currencyConversion.ts
    - src/types/currency.ts
    - src/types/data.ts
    - .planning/phases/36-dashboard-dual-currency-display/36-CONTEXT.md
  </read_first>
  <action>
    1. **Extend **`calcCategoryTotals`** signature** to accept a third argument **`ctx: { rates: ForexRateSnapshot; reportingLens: CurrencyCode }`** (`ForexRateSnapshot` from **`@/lib/currencyConversion`**). Keep existing **`live`** first argument shape for BTC/silver/gold feeds.
    2. **`sumMutualFunds` / `sumStocks`:** Replace naive sums with **`roundCurrency`** accumulation of **`toReportingCurrency(platform.currentValue, platform.currency ?? ctx.reportingLens, 'INR', ctx.rates)`** — when **`!ok`**, treat that platform as contributing **nothing** toward the INR sum (**same numerical spirit as skipping unpriced AED** when rate missing — document in a one-line comment). If **all** platforms fail conversion and count &gt;0, resulting sum policy: return **`0`** or match product choice — **pick `0`** only if no priced rows; if partial ok, sum ok rows only.
    3. **`sumBankSavingsInr`:** For each account, convert **`toReportingCurrency(a.balance, a.currency, 'INR', ctx.rates)`**; if **`!ok`** and balance &gt;0, **exclude** that balance from sum (preserve prior AED-missing behavior). **INR** accounts use **`from: 'INR'`**.
    4. **`sumPropertyInr`:** For each item, determine **`c = item.currency ?? ctx.reportingLens`**. Convert **equity** (existing **`agreementInr - loan`** or **`agreementInr`**) as **amount in `c`** when **`c !== 'INR'`** via **`toReportingCurrency(equity, c, 'INR', ctx.rates)`**; when **`c === 'INR'`** use numeric fields as today. If conversion fails, treat row as **0** toward sum (and document).
    5. **Gold / commodities / bitcoin / retirement:** At minimum, **thread `ctx`** into file for future; if **optional `currency`** exists on contributing records **and differs from INR**, convert **numeric contribution** equivalent from stored currency — **bitcoin** retains BTC→INR path; **`retirement`** if **`currency` set** converts **each of nps/epf** from stored code to INR. If scope explodes, **limit** Phase 36 to MF/Stocks/Bank/Property correctness + **stub** **`getBreakdownSecondaryDisplay`** buckets for categories still purely INR-internal — **but** PLAN requires MF/Stocks/Bank multi-currency honest hub.
    6. Export **`computeBreakdownOriginalLine`**: `(key: DashboardCategoryKey, data: AppData, ctx: same ctx, totalsEntry: CategoryTotals[typeof key]) → null | { currency: CurrencyCode; amount: number }` implementing **36-CONTEXT D-02**: collect contributing records’ **effective** currencies and **raw balances in stored units** grouped by **`CurrencyCode`**, excluding records that did not contribute to **`totalsEntry`** numeric path; **reportingLens** equivalency removes code from foreign set; **if `foreignDistinct.size === 0` return null**; **if `foreignDistinct.size > 1` return null**; **if exactly one** return **`{ currency: thatCode; amount: sumRawInThatCode }`**. **`totalsEntry === null`** → return **null**.
    7. **Call sites:** **`DashboardPage.tsx`** **`useMemo`** for **`calcCategoryTotals`** MUST pass **`{ rates: forexSnapshot, reportingLens: reportingCurrency }`** inside the dependency array.
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx tsc -b --pretty false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'export function computeBreakdownOriginalLine' src/lib/dashboardCalcs.ts` exits 0
    - `grep -q 'calcCategoryTotals' src/pages/DashboardPage.tsx` exits 0 && `grep -qE 'rates:|reportingLens' src/pages/DashboardPage.tsx` exits 0
    - `grep -q "toReportingCurrency" src/lib/dashboardCalcs.ts` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>Hub totals + secondary-line metadata grounded in CONTEXT D-02.</done>
</task>

<task type="auto">
  <name>36-01-02 — Vitest fixtures for breakdown rules</name>
  <files>src/lib/__tests__/dashboardBreakdown.test.ts</files>
  <read_first>
    - src/lib/dashboardCalcs.ts
    - src/lib/__tests__/wealthFormat.reporting.test.ts
    - .planning/phases/36-dashboard-dual-currency-display/36-CONTEXT.md
  </read_first>
  <action>
    Create **`src/lib/__tests__/dashboardBreakdown.test.ts`** with **`describe`** blocks for **`computeBreakdownOriginalLine`** and **`calcCategoryTotals`** covering:
    1. **All INR / matches reportingLens** → **`null`** secondary metadata (**DSP-03** proxy).
    2. **Two MF platforms USD only** aggregating **`X`**, reporting USD → secondary **null** (all reporting currency).
    3. **One USD MF + INR MF** reporting **INR**, fixed **`rates`** `usdInr: 83` → **original helper returns `{ currency: 'USD', amount: … }`** with summed USD **`currentValue`**, INR hub **`calcCategoryTotals` mutualFunds** reflects converted sum ± **`roundCurrency`**.
    4. **Two foreign codes within category** → **`null`** (D-02 multi-foreign).
    Use minimal **`AppData` factory** literals inline (subset of **`AppData`** typed as **`AppData`** via **`as unknown as AppData`** acceptable if narrowed fields satisfy consumer).
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npm test -- --run src/lib/__tests__/dashboardBreakdown.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/lib/__tests__/dashboardBreakdown.test.ts` exits 0
    - `grep -q 'computeBreakdownOriginalLine' src/lib/__tests__/dashboardBreakdown.test.ts` exits 0
    - `npm test -- --run src/lib/__tests__/dashboardBreakdown.test.ts` exits 0
  </acceptance_criteria>
  <done>Automated protection for DSP-03 + D-02 rules.</done>
</task>

<task type="auto">
  <name>36-01-03 — Dashboard breakdown dual stack JSX</name>
  <files>src/pages/DashboardPage.tsx</files>
  <read_first>
    - src/pages/DashboardPage.tsx
    - src/lib/wealthFormat.ts
    - .planning/phases/36-dashboard-dual-currency-display/36-UI-SPEC.md
  </read_first>
  <action>
    1. After **`computeBreakdownOriginalLine`** (rename if needed) resolves **`original`**, when **`original != null`** **and **`rowFmt` not degraded skeleton****, render amount column wrapper **`span.inline-flex.flex-col.items-end.gap-px.text-right`** (or equivalent) containing:
       - **Primary:** existing **`text-sm.font-semibold.tabular-nums`** string from **`formatRowReporting`** (**`rowFmt.primary`**).
       - **Secondary:** **`text-[11px].text-muted-foreground.tabular-nums`** showing **`fmtCompactForReporting(original.amount, original.currency)`** **or** formatter that includes **ISO/symbol** per **36-CONTEXT D-03** (must include **currency disambiguation** — at least **code** suffix/prefix).
    2. When **`original == null`**, keep **single-line** presentation exactly as today (non-degraded).
    3. **Degraded `rowFmt?.degraded`**: extend per **36-CONTEXT D-04** — preserve **`Rate unavailable`** top hint; primary value line follows **existing `fmtInr0`** fallback **unless** implementing D-04 “single interpretable foreign total” enhancement **inside same branch** documented by comment referencing **D-04**.
    4. **Hero + Total Debt row:** grep ensures **Total Debt** block still **does not** import/use **`computeBreakdownOriginalLine`** metadata.
    5. **Optional:** Factor **`DualCurrencyBreakdownAmount`** into **`src/components/DualCurrencyBreakdownAmount.tsx`** **only if** JSX duplication exceeds ~15 lines — otherwise inline per **36-CONTEXT Claude's Discretion**.
  </action>
  <verify>
    <automated>cd /Users/a.mahindrakar/personal/fin && npx tsc -b --pretty false && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Rate unavailable' src/pages/DashboardPage.tsx` exits 0
    - `grep -qE 'flex-col.*items-end|items-end.*flex-col' src/pages/DashboardPage.tsx` exits 0
    - `test $(grep -c 'computeBreakdownOriginalLine' src/pages/DashboardPage.tsx) -eq 1` exits 0
    - Shell: `COMP=$(grep -n 'computeBreakdownOriginalLine(' src/pages/DashboardPage.tsx | head -1 | cut -d: -f1); DEBT=$(grep -n 'aria-label="Open Liabilities section"' src/pages/DashboardPage.tsx | head -1 | cut -d: -f1); test -n "$COMP" && test -n "$DEBT" && test "$COMP" -lt "$DEBT"` exits 0 (helper only above Total Debt block)
    - `npm test -- --run` exits 0
    - `npx tsc -b --pretty false` exits 0
  </acceptance_criteria>
  <done>Breakdown rows match UI-SPEC; Total Debt untouched for dual line.</done>
</task>

</tasks>

<verification>
<criteria>
- `npm test -- --run` exits 0
- `npx tsc -b --pretty false` exits 0
- Manual: mixed-currency MF row shows primary + muted foreign subline; reporting-only row hides subline; missing rate shows **Rate unavailable** per roadmap item 3
</criteria>
</verification>
