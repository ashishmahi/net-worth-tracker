# Phase 10.1: JSON import - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `10.1-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-04-26
**Phase:** 10.1 — JSON import
**Areas discussed:** Confirm before replace, Import control & feedback, Invalid file / save errors, Import scope & edge cases

---

## Confirm before replace

| Option | Description | Selected |
|--------|-------------|----------|
| Short AlertDialog after validate, before save | Title + 1–2 lines; Cancel / Continue | ✓ |
| No dialog; file pick only | Fast path; errors + success inline |  |
| Claude’s discretion | — |  |

**User's choice:** Short `AlertDialog` after file is valid, before `saveData`.

**Notes:** Aligns IMP-02 optional confirm with explicit replace of current data.

---

## Import control & feedback (layout + success)

| Option | Description | Selected |
|--------|-------------|----------|
| Same row: Export + Import | Wrap on narrow | ✓ (layout) |
| Stacked: Export, then Import | — |  |
| Inline muted success in Data block | Like clear-data success | ✓ (success) |
| No persistent success | UI refresh only |  |

**User's choice:** Same row as Export; inline muted success line after import.

---

## Invalid file / save errors

| Option | Description | Selected |
|--------|-------------|----------|
| Friendly + short Zod/validation hint | First path or count; not full dump | ✓ (invalid) |
| Generic only | — |  |
| Save failure: match Settings | `role=alert`, running-app message | ✓ (save) |

**User's choice:** Friendly line + short hint; save failure message matches other Settings save failures.

---

## Import scope & edge cases

| Option | Description | Selected |
|--------|-------------|----------|
| Any file that passes same migration+Zod as app load | Hand-edited JSON OK if valid | ✓ (source) |
| Empty-equivalent after validate | Allow without extra warning | ✓ (empty) |
| Allow with extra warning in dialog | — |  |

**User's choice:** Broad technical validity; empty slate import allowed without extra warning.

---

## Claude's Discretion

- Exact copy, spinner placement, hint formatting, when success line clears.

## Deferred Ideas

*None recorded.*
