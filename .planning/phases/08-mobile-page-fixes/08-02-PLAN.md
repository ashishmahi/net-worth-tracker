---
phase: 08-mobile-page-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01-PLAN.md
files_modified:
  - src/pages/GoldPage.tsx
  - src/pages/MutualFundsPage.tsx
  - src/pages/StocksPage.tsx
  - src/pages/BankSavingsPage.tsx
  - src/pages/PropertyPage.tsx
autonomous: true
requirements:
  - MB-03
  - MB-04
must_haves:
  truths:
    - "Sheet add/edit forms use flex column with scrollable body so low fields are reachable; Property milestone data readable at 375px via horizontal scroll or card stack"
  artifacts:
    - path: "src/pages/PropertyPage.tsx"
      provides: "scrollable sheet, milestone table narrow layout"
      contains: "min-h-0|overflow|Table"
  key_links:
    - from: "src/pages/GoldPage.tsx"
      to: "src/components/ui/sheet.tsx"
      via: "SheetContent className and form structure"

---

<objective>
Deliver **MB-03** and **MB-04** after **Plan 01** headers: restructure every **Add/Edit `Sheet`** on **Gold, Mutual Funds, Stocks, Bank Savings, and Property** so **`SheetContent`** is a **flex column** with **`max-h-[100dvh] min-h-0`**, a **scrollable** middle (form fields) and **non-scrolling** header/footer as appropriate, preserving **form submit** behavior. On **Property** sheet, make the **milestone `Table` usable at 375px** with **`overflow-x-auto`** (visible scroll) and **`min-w-0`**/inner **`min-w-`** on the table, **or** a narrow-only **card** stack — per **08-UI-SPEC**.
</objective>

<threat_model>
**Scope:** Sheet layout + Property table; no API or data schema changes.

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-08-02a | Form submit broken after DOM move | High | Keep `<form onSubmit=…>`; submit buttons **inside** `<form>`; if footer is restructured, ensure `type="submit"` still submits |
| T-08-02b | `Sheet` focus trap or scroll trap | Medium | `overflow-y-auto` on inner body only; `min-h-0` on flex children |
| T-08-02c | Table data hidden on mobile | Medium | Horizontal scroll or responsive cards; no `truncate` on critical amount cells without alternative |

**Block on:** high — T-08-02a (if build fails or form stops saving)
</threat_model>

<context>
@.planning/phases/08-mobile-page-fixes/08-UI-SPEC.md
@.planning/phases/08-mobile-page-fixes/08-RESEARCH.md
@.planning/phases/08-mobile-page-fixes/08-PATTERNS.md
@.planning/phases/08-mobile-page-fixes/08-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GoldPage — sheet flex + scroll body</name>
  <files>src/pages/GoldPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/pages/GoldPage.tsx
    - /Users/a.mahindrakar/personal/fin/src/components/ui/sheet.tsx
  </read_first>
  <action>
1. On the asset `Sheet`, set `SheetContent` to include **`className`**: at minimum **`flex flex-col p-0 max-h-[100dvh] min-h-0 sm:max-w-lg`** (merge with `cn` if other classes exist).
2. **Structure:** (a) `SheetHeader` in a **`px-6 pt-6` shrink-0** wrapper, (b) `<form className="flex flex-1 min-h-0 flex-col">` with an inner **`<div className="flex-1 min-h-0 overflow-y-auto px-6 space-y-4">`** wrapping all **field** blocks (from first `div` to last field, **not** the footer), (c) `SheetFooter` in **`px-6 pb-6 pt-2 border-t` shrink-0` inside the form** after the scroll `div` so Save/Delete remain visible at bottom of panel.
3. Remove duplicate vertical padding that breaks alignment—keep a single `pb` on the scroll area if needed.
4. Preserve all **labels**, **validation**, **register()** wiring, and **onSubmit** handler.
  </action>
  <acceptance_criteria>
    - `grep "min-h-0" /Users/a.mahindrakar/personal/fin/src/pages/GoldPage.tsx` returns a match
    - `grep "overflow-y-auto" /Users/a.mahindrakar/personal/fin/src/pages/GoldPage.tsx` returns a match
    - `grep "max-h-\[100dvh\]" /Users/a.mahindrakar/personal/fin/src/pages/GoldPage.tsx` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: MutualFundsPage — same sheet pattern</name>
  <files>src/pages/MutualFundsPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/pages/MutualFundsPage.tsx
    - /Users/a.mahindrakar/personal/fin/src/pages/GoldPage.tsx
  </read_first>
  <action>
Apply the **same** `SheetContent` + `form` + **scroll** + **footer** structure as **Gold** (Task 1) to the add/edit `Sheet` for this page: `flex flex-col p-0 max-h-[100dvh] min-h-0 sm:max-w-lg`, form `flex-1 min-h-0`, inner body `flex-1 min-h-0 overflow-y-auto px-6`, `SheetFooter` with buttons **inside** the form, shrink-0 at end.
  </action>
  <acceptance_criteria>
    - `grep "min-h-0" /Users/a.mahindrakar/personal/fin/src/pages/MutualFundsPage.tsx` returns a match
    - `grep "overflow-y-auto" /Users/a.mahindrakar/personal/fin/src/pages/MutualFundsPage.tsx` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: StocksPage</name>
  <files>src/pages/StocksPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/pages/StocksPage.tsx
    - /Users/a.mahindrakar/personal/fin/src/pages/GoldPage.tsx
  </read_first>
  <action>
Same as Task 2 for the Stocks add/edit `Sheet`.
  </action>
  <acceptance_criteria>
    - `grep "overflow-y-auto" /Users/a.mahindrakar/personal/fin/src/pages/StocksPage.tsx` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: BankSavingsPage</name>
  <files>src/pages/BankSavingsPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/pages/BankSavingsPage.tsx
    - /Users/a.mahindrakar/personal/fin/src/pages/GoldPage.tsx
  </read_first>
  <action>
Same as Task 2 for the account add/edit `Sheet`.
  </action>
  <acceptance_criteria>
    - `grep "overflow-y-auto" /Users/a.mahindrakar/personal/fin/src/pages/BankSavingsPage.tsx` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: PropertyPage — sheet scroll + milestone table (MB-04)</name>
  <files>src/pages/PropertyPage.tsx</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/src/pages/PropertyPage.tsx
    - /Users/a.mahindrakar/personal/fin/.planning/phases/08-mobile-page-fixes/08-UI-SPEC.md
  </read_first>
  <action>
1. Replace current `SheetContent` layout with the same **flex column** + **scroll** pattern as **Gold** for the form — remove **only** `overflow-y-auto` from the *outer* `SheetContent` if it is superseded by inner scroll; the sheet root should still be `flex flex-col p-0 max-h-[100dvh] min-h-0 sm:max-w-lg`.
2. The **milestone** section including the `Table` must sit **inside** the **scrollable** `div` (or receive its own `overflow-y-auto` within that region).
3. **MB-04 — Table:** wrap the `Table` (or a fragment containing it) in **`<div className="w-full min-w-0 overflow-x-auto">`**. On `<Table>`, set **`className` `min-w-[36rem]"`** or similar fixed minimum width so **horizontal** scroll can activate at 375px (tune to fit all 4 column groups without clipping).
4. If horizontal scroll is insufficient after implementation, you may add **`md:hidden` card rows** and hide the table on extra-small — only if the scroll approach still clips; default to **scroll** first.
5. Preserve `aria-describedby` on `SheetContent` and all **save/delete** behavior.
  </action>
  <acceptance_criteria>
    - `grep "overflow-x-auto" /Users/a.mahindrakar/personal/fin/src/pages/PropertyPage.tsx` returns a match
    - `grep "min-w-" /Users/a.mahindrakar/personal/fin/src/pages/PropertyPage.tsx` — match on `Table` or child (table or wrapper)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Build</name>
  <files>package.json</files>
  <read_first>
    - /Users/a.mahindrakar/personal/fin/package.json
  </read_first>
  <action>
From repo root: `npm run build` — must exit 0. Optionally: `npm run lint` if ESLint is clean to run.
  </action>
  <acceptance_criteria>
    - `cd /Users/a.mahindrakar/personal/fin && npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- **Manual (UAT / verify-work):** open Property Add sheet on 375px, verify milestone table scrolls **horizontally** and Add/Edit sheets allow **scrolling to bottom fields** (keyboard optional if emulated)
</verification>
